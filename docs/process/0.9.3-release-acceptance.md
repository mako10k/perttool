# `v0.9.3` Release Acceptance

- Document status: Accepted 1.0
- Acceptance date: 2026-08-14
- Task: `RELEASE_093_ACCEPTANCE`
- Release commit and peeled tag:
  `7755d3e5eee59d242402b950bcbc06583df54de4`
- Artifact SHA-256:
  `a86c552bb616a3dda13f12b32c9463d11e6ac4b2caa4c8d8d497855abcad9708`
- Final release-plan source digest:
  `sha256:fd85df378fe6f7a8978bfb325995da958baa29d7dc6ce2534ba572229b422fcd`

## 1. Durable public identity

Independent reads confirmed the exact remote `main`, annotated tag object,
peeled tag, successful Node.js 22 and 24 CI run, GitHub prerelease, npm version,
registry integrity, file count, unpacked size, and dist-tags. Fresh downloads
of the GitHub and npm tarballs both compare byte-for-byte with the retained
candidate. npm reports `beta=0.9.3`, `latest=0.9.0`, and no `alpha`.

Fresh-cache isolated installations resolved exact `perttool@0.9.3` and
`perttool@beta` to `perttool 0.9.3`. The exact rollback pin resolved to
`perttool 0.9.2`. The exact public package exposes 53 commands, 23 root
schemas, 129 root exports, 129 reference-identical Node exports, and 45 Core
exports.

## 2. Emergency behavior replay

- Issue #14: the unchanged Grammar 7 editor-mutations plan returned successful
  `plan-assurance show` and `contract` hash results. Its source digest is
  `sha256:690f65bd5a70594a045b53bdae12178339e6fb6d160eb616ffa0a08150a8b79a`,
  and the selected contract hash is
  `sha256:c37f3d5d5170fa41982ef71f6020c3916d4bdd1bc29fed05973e782d65f4ef6d`.
- Issue #16: the unchanged image-platform commit `627514d` source returned a
  changed valid advance candidate with passed milestone-acceptance and
  plan-assurance guards, preview history not applicable, and no diagnostics.
- Issue #17: the unchanged image-platform commit `651ab2b` source returned the
  same successful guard classification with no diagnostics.

The exact registry package was installed globally and reports
`perttool 0.9.3`. No consumer repository or source plan was modified during
the replays.

## 3. Issue and backlog completion

Issues [#14](https://github.com/mako10k/perttool/issues/14),
[#16](https://github.com/mako10k/perttool/issues/16), and
[#17](https://github.com/mako10k/perttool/issues/17) were each closed once as
completed with one release-evidence comment. Each retains `bug` and no longer
has `priority:P0`. Their durable backlog identities are `ASSURE-002`,
`ADV-004`, and `ADV-005`.

All six release tasks and 21 points are complete. Complete
`Perttool.NextResult.v7` has no ready, recommended, or startable task. The
completed declarations remain in their exact pre-advance state. npm `latest`,
release-plan advance, public VSIX publication, and unrelated work remain
separate.
