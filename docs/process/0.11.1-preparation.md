# `v0.11.1` Source Preparation

- Document status: Accepted 1.0
- Acceptance date: 2026-09-08
- Target version: `0.11.1`
- Task: `RELEASE_0111_PREPARATION` (complete)
- Release plan: [`release-0.11.1.pert`](../../plans/release-0.11.1.pert)
- Release procedure: [`0.11.1-release.md`](0.11.1-release.md)

## 1. Version and compatibility identity

Package and root lockfile identities, LSP and MCP peer constraints, CLI and MCP
server versions, CHANGELOG, README, tests, goldens, self-use, release records,
and current guidance identify `0.11.1`. The prepared source retains Grammar 9,
CLI Contract 10, 71 commands, 29 active root schemas, 139 reference-identical
root and Node runtime exports, and 51 portable Core runtime exports. No public
result identity, governance rule, persistence boundary, or Planning Pool
meaning changed during source preparation.

Published `0.11.0` remains the exact rollback pin. npm still reports
`beta=0.11.0`, `latest=0.10.5`, and no `alpha`; publication remains a later
candidate-bound operation.

## 2. Complete local and installed-package gate

The complete Node.js 22.22.3 repository gate passed on the prepared source
with:

- 1,346 Node.js tests;
- 47 self-use plans under check, analyze, and Next;
- 1,328 English-baseline text files with three exact allowlisted lines and 388
  Markdown documents with seven PERT examples before this record was added;
- pinned jscpd 5.0.15 at 147 clones, 2,743 duplicate lines, and 2.660%;
- pinned Lizard 1.23.0 at 5,147 functions and 168 reviewed legacy entries;
- isolated LSP and MCP package gates;
- the supported VS Code 1.101.0 trusted and untrusted host, replacement, and
  uninstall gate under Xvfb;
- temporary-link installation as `perttool 0.11.1`; and
- the 1,019-file isolated public-package workflow, npm publication dry run,
  Contract 9 file-first compatibility, and plan-assurance compatibility.

The public dependency audit reported zero vulnerabilities. The full
development-tree audit separately reported one high-severity transitive
`fast-uri` advisory group and one moderate transitive `qs` advisory group.
Both are confined to development tooling and are absent from the dependency-
free public package. This compatibility patch does not mix in an unrelated
development dependency update.

The first version-aligned full-test attempt exposed stale current-version and
self-use-count expectations. After those were corrected, a second test pass
exposed the expected version-bound Override artifact digest. Updating that
derived golden produced the final complete passing gate; no runtime or
Override semantics changed.

## 3. Boundaries retained

This preparation performed no Git commit or ref mutation, remote write, GitHub
mutation, npm publication, dist-tag change, Issue mutation, public VSIX
publication, or plan advance. The package workflow retained no release
tarball. The candidate task must next commit the clean source, prove unused
external identities and authenticated routes, and retain one immutable
artifact before candidate-bound publication confirmation.
