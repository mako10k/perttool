# `v0.9.2` Release Acceptance

- Document status: Accepted 1.0
- Acceptance date: 2026-08-14
- Release commit: `b47a0a450f83070340fc52df88a0243f80ef795e`
- Release tag: `v0.9.2`
- Release: [perttool v0.9.2](https://github.com/mako10k/perttool/releases/tag/v0.9.2)
- CI: [Repository checks 31762661717](https://github.com/mako10k/perttool/actions/runs/31762661717)
- Issue: [#15](https://github.com/mako10k/perttool/issues/15)
- Final plan source digest: `sha256:90a46054a99c8288a05a62f4f3daeaae4668c319b1d02029cd51d21de88f3caf`
- Release plan: [`release-0.9.2.pert`](../../plans/release-0.9.2.pert)

## 1. Durable public identity

Remote `main`, the peeled annotated `v0.9.2` tag, and CI head SHA all resolve
to the release commit. CI attempt 2 passed the complete Node.js 22 and 24
matrix. GitHub prerelease `370310054` is published and not a draft.

The retained candidate, independently downloaded GitHub asset, and fresh npm
registry tarball are byte-identical at SHA-256
`5347f4b0e7c38b44f4f6ee34ca71dd2389fcf88da3ad52b85c7b64f82e980edb`.
The GitHub checksum verifies, and npm metadata independently agrees on SHA-1,
integrity, 712 files, and `4626734` unpacked bytes. npm publication occurred
once; the post-publish E404 was treated only as a propagation result and was
not followed by a second publish.

## 2. Installed package and Issue #15 acceptance

Fresh Node.js 22 isolated installations resolved as follows:

| Request | Installed version |
| --- | --- |
| `perttool@0.9.2` | `perttool 0.9.2` |
| `perttool@beta` | `perttool 0.9.2` |
| rollback pin `perttool@0.9.1` | `perttool 0.9.1` |

Both 0.9.2 paths expose CLI Contract 8, 53 commands, 23 root schemas, 129
key- and reference-identical root and Node runtime exports, and 45 Core
exports. The exact 0.9.1 rollback retains 53 commands and 23 root schemas.

The exact and beta paths both accepted `document check`, `project show`, all
three `dag analyze` schedule modes, and file/stdin `dag next` over the real
Issue #15 Point plan without velocity. Analysis retained Point precedence
`4p`, resource `5p`, `velocity=null`, `velocity_forecast=null`, and absent
calendar schedules. Next recommended and made startable only `GATE_NEAR`.

## 3. Local installation and Issue completion

The active global Node.js 25 installation was upgraded from `0.9.1`, and the
maintained Node.js 22 installation was upgraded from `0.8.0`, each to exact
registry `perttool@0.9.2`. Independent CLI reads and the Issue #15 analyze and
Next matrix passed through both executable paths. The unrelated Windows-path
fallback was inspected but not mutated.

Issue #15 received one evidence comment, had `priority:P0` removed while
retaining `bug`, and was closed once as `COMPLETED` at
`2026-08-14T02:20:13Z`. Readback reports one comment, closed state, and only
the `bug` label.

## 4. Final repository and plan state

The final repository-only record state passed the complete Node.js 22.22.3
gate: 1,048 tests, the English baseline over 876 text files, documentation
checks over 262 Markdown files and seven PERT examples, read-only self-use
over 40 plans, isolated LSP and MCP acceptance, the private VSIX and supported
VS Code 1.101.0 host gate, temporary linking, and the 712-file isolated public
package workflow. `git diff --check` also passed.

All six release tasks and 18p are complete with zero precedence and resource
makespans. Complete, non-truncated NextResult v7 has no ready, recommended,
or startable task. The plan remains in its exact pre-advance state.

The compatible `0.9.2` emergency patch is durably accepted. npm
`beta=0.9.2`, `latest=0.9.0`, and no `alpha` remain the accepted channel
state. npm `latest` promotion, plan advance, public VSIX publication, and
unrelated work remain separate boundaries.
