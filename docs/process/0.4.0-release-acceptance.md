# `v0.4.0` Contract 5 Beta Release Acceptance

- Document status: Accepted 1.0
- Accepted on: 2026-07-28
- Release commit: `6b341d1913bd943d872b14d7ef48645ac7b26667`
- Annotated tag: `v0.4.0`
- GitHub prerelease: <https://github.com/mako10k/perttool/releases/tag/v0.4.0>
- PUBLISH record: [`0.4.0-publish.md`](0.4.0-publish.md)
- Release plan: [../../plans/release-0.4.0.pert](../../plans/release-0.4.0.pert)

## 1. Acceptance decision

Accept suffix-free beta `0.4.0` as the first public atomic Grammar 4 and CLI
Contract 5 package. All twelve release requirements are satisfied. The
immutable release artifact remains a beta product and is not a stable-series
declaration.

All six tasks in `plans/release-0.4.0.pert`, totaling 19p over two active
days, are complete. Commit `d016e59` preserves the completed pre-advance
snapshot with zero precedence and heuristic resource makespans and no ready
or recommended task. Canonical governed advance then retained only reached
milestone `RELEASE_040_ACCEPTED` at source digest
`sha256:9abdbbfbd8431d854977af74d1483777f822cb38afecf180022050bc5bbaf342`.

## 2. Immutable release identity

| Evidence | Accepted value |
| --- | --- |
| Release commit and peeled tag target | `6b341d1913bd943d872b14d7ef48645ac7b26667` |
| Annotated tag object | `17bb077c3d37df82642e990b4bc8152a5c6f2712` |
| Package | `perttool@0.4.0` |
| Candidate, GitHub, and npm tarball SHA-256 | `010af9ce2290ade99c191b0c6a9ea485d5ae23ec1241113dfbc1d275b387cc4a` |
| npm integrity | `sha512-HRu8V0WKvVl/llRk4AgCk6nv7XsX52x/Us4yxCpKlUvbewdzPKKE/EHeckRnnWu7cy4f6AsCTJfBlAWkvwgY9w==` |
| npm SHA-1 | `2c74230305e135860c86f68df5bc30262d6f02d2` |
| Package files / unpacked size | `392` / `2273437` bytes |

The release commit, peeled local and remote annotated tag, GitHub prerelease,
and npm registry package agree. The retained candidate, downloaded GitHub,
and independently retrieved npm tarballs are byte-identical. The PUBLISH
record retains the exact commands and the non-retried propagation-time
`E404` observation.

## 3. Distribution state at acceptance

| Dist-tag | Version |
| --- | --- |
| `beta` | `0.4.0` |
| `latest` | `0.3.0` |
| `alpha` | `0.1.0-alpha.2` |

The beta publication correctly changed only `beta`. Acceptance does not
itself mutate npm dist-tags. After this acceptance boundary was selected, the
user separately authorized a later one-time promotion of
`perttool@0.4.0` to `latest`; that operation is not part of this acceptance
snapshot.

## 4. Verification

The exact release commit passed the complete Node.js 22.22.3 repository gate:

```sh
npm ci
npm run check
git diff --check
```

The complete gate ran 585 tests, checked 79 Markdown documents and six
normative PERT examples, checked all nineteen self-use plans, and passed the
local link, package, and installed Contract 5 file-first workflows.

The downloaded GitHub tarball and independently retrieved npm tarball each
passed `scripts/check-package.sh` again during acceptance. Those installed
checks covered:

- `perttool 0.4.0` and CLI Contract 5 help and command discovery;
- Grammar 4 declared/effective owner and delegate metadata;
- Grammar 1/2/3 effective default owner compatibility;
- denied preview, authorized persistence, and stale-digest rejection;
- governed direct, batch, and advance decisions;
- exact unit-migration preservation and complete NextResult v4; and
- the complete file-first workflow.

A separate isolated install from the exact npm registry version also reported
`perttool 0.4.0`. No product failure or unresolved acceptance finding was
observed.

## 5. Remaining boundaries

The accepted release artifact is immutable. The separately authorized npm
`latest` promotion may now proceed as one dist-tag mutation followed by
uncached registry and unqualified-install verification. The release plan must
remain unchanged after that external post-acceptance operation.

Issue #4 closure is still a separate decision. Acceptance does not add
authentication, verified identity, signatures, durable approval audit,
direct-edit prevention, recommendation override apply, MIG-08, Git
integration, LSP, VSIX, MCP, or runtime i18n.
