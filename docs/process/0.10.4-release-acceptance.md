# `v0.10.4` Release Acceptance

Status: Accepted on 2026-08-21.

## Result

The compatible Issue #6 patch is durably published as `perttool@0.10.4`.
Release commit and peeled annotated tag `v0.10.4` both resolve to
`841017e774276d9438a77195ed399d30eca63b16`. GitHub prerelease
`374313865` and npm `beta=0.10.4` expose the same immutable candidate.
npm `latest=0.10.3` remains unchanged and no `alpha` tag exists.

## Evidence

- The complete local Node.js 22 gate passed 1,227 tests, static-analysis
  ratchets, English and documentation checks, 44 self-use plans, private
  adapters, the supported VS Code host, temporary linking, and the isolated
  876-file package workflow.
- GitHub Actions run `32470068538` passed Node.js 22 and 24. The first Node.js
  24 attempt passed the product tests and failed only when the external VS Code
  uninstall subprocess timed out. One bounded failed-job rerun passed with the
  unchanged commit and candidate.
- The retained 876-file, 1,078,630-byte tarball, GitHub asset, and npm download
  agree at SHA-256
  `edee4358312facd5e3854564cf86ad6c5afe62eed2e8f9d6e21e65c39283782e`.
- Exact and `beta` installations resolve to `perttool 0.10.4`, expose 56
  commands and 23 root schemas, and advertise the closed
  `automatic|new-root` history-provenance choice. Exact `0.10.3` remains
  installable as the rollback pin.
- Issue #6 has one release-evidence comment, is closed as completed, retains
  only the `bug` label, and no longer carries `priority:P1`.

## Boundary

Grammar 8, CLI Contract 9, public catalog identities, default fail-closed
automatic history inference, and Git mutation authority remain unchanged.
npm `latest`, public VSIX publication, release-plan advance, `SCM-002`, and
unrelated work remain separate.
