# `v0.7.1` Source Preparation

- Document status: Accepted 1.0
- Preparation date: 2026-08-05
- Target package: `perttool@0.7.1`
- Grammar: Grammar 6
- CLI contract: Contract 7
- Publication state: unpublished
- Predecessor: [`v0.7.1` Self-Review](0.7.1-self-review.md)
- Release procedure: [`v0.7.1`](0.7.1-release.md)
- Release plan: [`release-0.7.1.pert`](../../plans/release-0.7.1.pert)
- Completed-plan source digest: `sha256:25ab07cca3c3a7c4a6e880d3b429023924a2e8de48aa70314f82887d928877b2`

## 1. Prepared boundary

This gate aligns the local version-bearing source for the compatible Help and
Guide correction. Package and lockfile identities, CLI version, CHANGELOG,
README exact-pin examples, release records, tests, goldens, self-use
registration, and shared agent policy identify `0.7.1` together.

The patch retains Grammar 6, CLI Contract 7, all 44 command paths, all 20 root
schemas, public result and payload identities, 121 package-root export names,
governance, safe-write behavior, and recommendation, temporal, and
plan-assurance authority. It remains beta-only through
`publishConfig.tag=beta` and does not recreate `alpha`.

## 2. Documentation boundary

The README uses exact `perttool@0.7.1` examples and durable channel wording:
publication moves only npm `beta`, leaves independently managed
`latest=0.7.0`, and retains `0.7.0` as the rollback pin. Historical `0.7.0`
publication and promotion records remain unchanged.

The CHANGELOG records only the accepted `GUIDE-CONSISTENCY-001` correction:
active Guide meaning, argument-valid Help examples, resolvable diagnostic
navigation, direct `PTCNV-210` coverage, bounded plan-assurance navigation,
and current-versus-historical labels.

## 3. Verification gate

Run under Node.js 22.22.3:

```sh
npm ci
npm audit
npm run check
git diff --check
```

The complete gate passed with:

- 797 tests;
- the 619-file English baseline with three exact allowlisted lines;
- 165 Markdown files and seven normative PERT examples;
- read-only check, analysis, and Next verification for all 33 self-use plans;
- a temporary link reporting `perttool 0.7.1`;
- the 601-file isolated-package file-first and plan-assurance workflows;
- beta-only npm publication normalization for `perttool@0.7.1`; and
- zero known dependency vulnerabilities.

`git diff --check` also passed. The disposable package from this gate was
verification output, not the retained release candidate.

## 4. Authority and remaining work

This preparation performs no push, tag creation, GitHub mutation, npm
publication, dist-tag mutation, plan advance, or Issue mutation. Candidate
acceptance requires a clean committed source, repeated gates, read-only
external and protected-route preflight, and one retained immutable tarball.
The exact candidate remains subject to a separate user boundary before
PUBLISH.

`RELEASE_071_PREPARATION` is complete. Complete NextResult v6 recommends and
authorizes only `RELEASE_071_CANDIDATE`.
