# `v0.9.1` Durable Release Acceptance

- Document status: Accepted 1.0
- Acceptance date: 2026-08-13
- Version: `0.9.1`
- Release commit: `ddb12dc97e8be34965654c84f01bc330a4a2e5cf`
- Annotated tag object: `93547c3f719afbef9d60fd18845864d9513ff556`
- Candidate SHA-256: `27b4d8d60718675961c913771cb319043df8daf8c50b81b22cb70eb79f3e77bf`
- CI run: [`31684442125`](https://github.com/mako10k/perttool/actions/runs/31684442125)
- GitHub Release: [`v0.9.1`](https://github.com/mako10k/perttool/releases/tag/v0.9.1)
- npm version: [`perttool@0.9.1`](https://www.npmjs.com/package/perttool/v/0.9.1)
- Issue: [#8](https://github.com/mako10k/perttool/issues/8)
- Final-plan source digest: `sha256:806ff9a6f838eca24d5c1b26e763332c94ccb658d0a05f07b28bdaf7fee3bb6d`

## 1. Public identity and artifact readback

Independent post-publication reads established that `origin/main`, the local
and remote peeled `v0.9.1`, CI, the GitHub prerelease, and npm all identify
release commit `ddb12dc97e8be34965654c84f01bc330a4a2e5cf` and the selected
suffix-free `0.9.1` patch boundary. CI run `31684442125` is complete and
successful for both Node.js 22 and 24.

The retained candidate, downloaded GitHub asset, and newly downloaded npm
tarball are byte-identical at SHA-256
`27b4d8d60718675961c913771cb319043df8daf8c50b81b22cb70eb79f3e77bf`.
GitHub reports the same digest and 2808175-byte size. npm reports 713 files,
6634333 unpacked file bytes, SHA-1
`0a36b0e5d696a0dfcef5904dae419a4da9fd9ef6`, and integrity
`sha512-62XXV/+FaFOgYJcxsyazrnyl/y2+Q2Uv/MNQodRGrw8mozeWvlUgPTv2mDOBw7M8IIEZwg0BH3bj03CzvouZAg==`.

Registry readback reports `beta=0.9.1`, unchanged `latest=0.9.0`, and no
`alpha`. Publication time is `2026-08-13T09:03:09.925Z`.

## 2. Fresh installed-package verification

Fresh-cache exact and `beta` global installations both resolved to
`perttool 0.9.1`. The exact installation exposed Grammar 7, CLI Contract 8,
53 commands, 23 root schemas, 129 reference-identical root and Node exports,
and 45 Core exports. The `beta` installation independently exposed the same
command and schema counts.

A fresh exact `perttool@0.9.0` installation remained available as the
Contract 8 rollback pin and exposed its expected 53 commands and 23 schemas.

## 3. Installed Issue #8 regression

The exact installed `0.9.1` CLI reproduced the reported workflow in a new
temporary Git repository. It committed one active 4p task with a start event,
wrote an 8h eventful finish through `task finish --write`, and left that
finish uncommitted.

The installed `declared` observation returned the available exact 4p/8h
sample from current digest
`sha256:c508529635b33ce2ae49ba0aae834fe6db89bf4dfc63bcb4abf0c28b624ac0b5`.
Its nested history remained bound to committed digest
`sha256:d36816ed60007cd5d4aaf25ff50ee4b30e9677d45cdd116fe84264ab867c593e`.
The installed `git-recorded` candidate remained unavailable and did not
acquire the uncommitted finish; `all` retained both evidence classes with the
same separate bindings. Observation preserved plan bytes, `HEAD`, and the
stage-0 index.

## 4. Issue completion and plan state

Only after the public and installed evidence above passed, one GraphQL
mutation added the exact 991-byte evidence comment, removed only
`priority:P0`, and closed Issue #8. Readback shows Issue #8 closed at
`2026-08-13T09:08:05Z`, one owner comment at
[`issuecomment-5278308860`](https://github.com/mako10k/perttool/issues/8#issuecomment-5278308860),
and the retained `bug` label. No other Issue was changed.

All five release tasks and 15p are complete and retained before advance.
Precedence and `parallel-sgs` version 1 resource makespans are zero with zero
resource delay. Complete, non-truncated NextResult v7 has no ready,
recommended, or startable task.

npm `latest` promotion, release-plan advance, public VSIX publication, and
unrelated work remain separate and were not performed.
