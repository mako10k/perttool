# `v0.9.2` Release Candidate Acceptance

- Document status: Accepted 1.0
- Review date: 2026-08-14
- Target version: `0.9.2`
- Candidate source commit: `06435cc590bb1028c16be1d1a3b7ce99c94410d9`
- Candidate source tree: `a23b7025823820773d700d07344e8998a7c99b34`
- Completed-plan source digest: `sha256:5989d3c96a9abe139f11d524e41b95dfc0053775a4d18a9c70ec7a5dafc713b9`
- Release plan: [`release-0.9.2.pert`](../../plans/release-0.9.2.pert)
- Release procedure: [`0.9.2-release.md`](0.9.2-release.md)

## 1. Acceptance boundary

This record accepts one immutable local `perttool@0.9.2` tarball for the
compatible Grammar 7 and CLI Contract 8 Point-without-velocity correction. It
fixes the source commit, public-package identity, read-only external baseline,
artifact bytes, and installed Issue #15 behavior for the authorized ordered
PUBLISH batch.

It does not perform a Git push, tag creation or push, GitHub Release, npm
publication, npm dist-tag mutation, durable public acceptance, plan advance,
or Issue mutation. Candidate acceptance and complete NextResult v7 sequencing
do not permit publication before the frozen external mutation order.

## 2. Clean source and repository gate

Commit `06435cc590bb1028c16be1d1a3b7ce99c94410d9` was clean and identified the
package, both lockfile roots, CLI, private adapter peer versions, and MCP
server as `0.9.2`. Its committed tree was
`a23b7025823820773d700d07344e8998a7c99b34`.

From that clean commit, the complete Node.js 22.22.3 `npm run check` and `git
diff --check` passed. The gate included all 1,048 tests, the 873-file English
baseline with three exact allowlisted lines, 259 Markdown files and seven
normative PERT examples, all 40 self-use plans, isolated LSP and MCP
acceptance, the private VSIX supported-host gate, temporary linking, and the
712-file isolated public-package gate. The dependency audit reported zero
vulnerabilities.

## 3. Read-only external and route preflight

Direct reads found no local or remote `v0.9.2`, no GitHub Release `v0.9.2`,
and no npm `perttool@0.9.2`. npm reported `beta=0.9.1`, `latest=0.9.0`, and no
`alpha`. Issue #15 remained open with labels `bug` and `priority:P0`.
`origin/main` remained
`ddb12dc97e8be34965654c84f01bc330a4a2e5cf`.

The process-scoped GitHub and npm identities both resolved as `mako10k`
without displaying credential values. The authenticated `secdat` Git dry-run
projected a fast-forward of `origin/main` through the candidate source commit
without writing. GitHub's branch-protection endpoint returned HTTP 404 for
`main`; therefore this record makes no branch-protection claim. No external
state changed.

## 4. Immutable candidate identity

The fixed-path candidate was generated once from the accepted source commit
and then made read-only:

| Property | Exact value |
| --- | --- |
| Path | `/home/katsumata-m/releases/perttool/0.9.2/candidate/perttool-0.9.2.tgz` |
| Modification time | `2026-08-14 10:59:36.304430785 +0900` |
| Mode | `0444` |
| Packed size | `798088` bytes |
| Unpacked file bytes | `4626734` bytes |
| Files | `712` |
| SHA-1 | `cfd9953710f564aaaf08c8f43a39a11aac6c93cb` |
| SHA-256 | `5347f4b0e7c38b44f4f6ee34ca71dd2389fcf88da3ad52b85c7b64f82e980edb` |
| npm integrity | `sha512-aYupuVoNLi++EX6gwpOm2HKJF/nfjYmHiBuSxQyp16OwWXWgL8uZx2UPOvqIPJdtAw1WpFAHAAwYwOBpmltc9A==` |

The archive manifest identifies public `perttool@0.9.2`, CLI `dist/cli.js`,
the root, `perttool/core`, `perttool/node`, and schema exports, and npm tag
`beta`. It contains 23 root schemas, exposes no private adapter workspace,
and includes no `adapters/` path.

## 5. Candidate-specific package acceptance

Those exact retained bytes passed `scripts/publish-npm.sh --dry-run` for npm
`beta` and the complete explicit-archive `scripts/check-package.sh` workflow
under Node.js 22.22.3. The installed package reported `perttool 0.9.2`,
Contract 8 file-first behavior, plan-assurance compatibility, 53 commands, 23
schemas, 129 reference-identical root and Node exports, and 45 Core exports.

The installed Issue #15 matrix accepted `document check`, `project show`, all
three `dag analyze` schedule modes, and file/stdin `dag next`. Analysis kept
Point precedence `4p`, resource `5p`, `velocity=null`, and
`velocity_forecast=null`; Next recommended and made startable only
`GATE_NEAR`. The retained candidate SHA-256 remained
`5347f4b0e7c38b44f4f6ee34ca71dd2389fcf88da3ad52b85c7b64f82e980edb`
after archive inspection and installed-package verification.

`RELEASE_092_CANDIDATE` is complete at plan source digest
`sha256:5989d3c96a9abe139f11d524e41b95dfc0053775a4d18a9c70ec7a5dafc713b9`.
Complete, non-truncated NextResult v7 recommends and makes startable only
`RELEASE_092_PUBLISH`. The authorized PUBLISH batch remains bounded to the
frozen candidate, release commit, target refs, GitHub prerelease and checksum
asset, one npm `beta` publication, and post-write readback; npm `latest` and
plan advance remain excluded.
