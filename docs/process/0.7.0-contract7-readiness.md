# `v0.7.0` Contract 7 Readiness

- Document status: Accepted 1.0
- Acceptance date: 2026-08-04
- Release target: `0.7.0`
- Release procedure: [`0.7.0-release.md`](0.7.0-release.md)
- Release plan: [`release-0.7.0.pert`](../../plans/release-0.7.0.pert)
- Public-contract acceptance:
  [`plan-assurance-public-contract-acceptance.md`](plan-assurance-public-contract-acceptance.md)
- Final implementation acceptance:
  [`plan-assurance-acceptance.md`](plan-assurance-acceptance.md)

## 1. Decision

Accept `RELEASE_070_CONTRACT_7_READINESS`. The complete ASSURE-001
pre-advance snapshot and both accepted evidence records remain exact. The
standard source, package root, CLI, command registry, help, Guide, schemas,
temporary-link workflow, and isolated-package workflow expose Grammar 6 and
CLI Contract 7 together. No hidden correctness, compatibility, packaging, or
authority finding requires a return to an ASSURE-001 implementation gate.

This decision leaves only `RELEASE_070_PREPARATION` ready and recommended. It
does not authorize or perform version-bearing source preparation, candidate
creation, Git push, tag creation, GitHub or npm publication, npm `latest`
movement, either plan advance, or Issue mutation. Package identity remains
`0.6.0` until the separate preparation task is authorized and completed.

## 2. Accepted input identity

| Input | Accepted state |
| --- | --- |
| Plan assurance plan | Raw/source digest `sha256:51dc8595b6e1306fd391c8c6da5990d9cc6e74ae30c279daecc15f5b565ea563`; all ten tasks and 59p complete before advance; zero precedence and heuristic resource makespans; no active, ready, runnable, blocked, upcoming, or suspended task |
| Recommendation authority | Complete, non-truncated NextResult v6 under `recommendation_v1_plus_release_gate_plus_plan_assurance_v1`; no raw recommended or startable recommended task |
| Public-contract acceptance | Git commit `aea3658` records the atomic Grammar 6, CLI Contract 7, 44-command, 20-root-schema, package-root, help, Guide, link, and installed-package boundary |
| Final implementation acceptance | Git commit `b6026b5` records the fourteen semantic cases, twelve interface cases, complete CLI workflow, real output race, and exact completed plan snapshot |
| Release gate | Git commit `c1d2666` records the selected `0.7.0` gate design; pre-readiness source digest was `sha256:d64d4e9bd3869acd7258f09e2eafcc7e0687117e3aaea38ac49835afce26f61d` |

The twelve `PTDAG-208` diagnostics on the retained ASSURE-001 source describe
milestones reached by completed-task closure and suggest the separately gated
canonical advance. They are not incomplete-work, recommendation, assurance,
or release-readiness findings.

## 3. Public source and package boundary

The accepted boundary remains coordinated:

- Grammar 6 opt-in assurance model 1 and hash model 1 are active through the
  standard parser, validator, formatter, mutation, lifecycle, analysis, next,
  advance, history, unit-migration, and Mermaid profile 2 paths;
- default `both`, explicit `planning_only`, and explicit `execution_only`
  relations keep planning-basis propagation distinct from AoA execution
  precedence;
- task, recursive basis, component-seal, outcome, and frontier-receipt
  commitments reproduce the fixed SHA-256 vectors and complete cause paths;
- all 44 Contract 7 commands, including the ten plan-assurance commands, are
  registry, usage, help, CLI-dispatch, and installed-package projections;
- the closed catalog contains nineteen command-result roots plus public
  library-only `Perttool.OverrideDecision.v1`, including
  `Perttool.PlanAssuranceResult.v1` and every assurance-aware result cut;
- GovernanceDecision v2, assurance impact, and digest-bound candidate
  validation remain composed before safe persistence; and
- the public package root exposes the high-level assurance inspection and
  mutation adapters without exporting the lower-level evaluator, canonical
  hash, or target-capability internals.

The current version-bearing files and packed dry-run still identify
`perttool@0.6.0`. That is the expected input to preparation, not a mixed
Contract 7 state: `RELEASE_070_PREPARATION` owns the atomic package, lockfile,
tool version, CHANGELOG, README, migration, release-test, and publication-
normalization update to `0.7.0`.

## 4. Compatibility and failure boundaries

- Grammar 1 through 5 retain their accepted source meanings and ordinary
  start authority with assurance reported as `not_enabled`.
- Enabled but unsealed, unknown-version, mismatched, stale-outcome, or damaged
  receipt evidence remains analyzable but withholds affected new-start
  authority and returns explicit required actions.
- Status, lifecycle work events, timestamps, trivia, declaration ordering,
  derived analysis and recommendation values, and accepted hashes themselves
  remain excluded from task-plan contract hashes.
- Pinpoint hash inspection returns only an evaluator-derived digest and cannot
  seal, reseal, repair, authorize, or guess unavailable evidence.
- Assurance-preserving advance reconstructs frontier receipts, while
  `--force-history-loss` cannot bypass a blocked assurance guard and assurance
  mutation cannot bypass the independent Git history guard.
- SHA-256 commitments remain semantic drift detection and accepted planning
  basis, not a digital signature, authenticated approval, malicious-edit
  defense, external transparency log, or root of trust.

## 5. Verification

The readiness review ran under Node.js `22.22.3`:

```sh
npm ci
npm audit
node --test test/plan-assurance-*.test.mjs test/json-schema.test.mjs \
  test/guide.test.mjs test/command-registry.test.mjs
npm run check
git diff --check
```

Direct readback also used `document check`, `dag analyze`, `dag next`, and
`project show` for `plans/plan-assurance.pert`; registry-derived JSON `help`,
`guide`, and `schema`; and the standard package-root catalog APIs.

Observed results:

- the focused assurance, registry, Guide, and schema matrix passed all 89
  tests with no failures, skips, or cancellations;
- package-root and CLI readback returned Contract 7, 44 commands, 20 schemas,
  the plan-assurance Guide topic, and `Perttool.PlanAssuranceResult.v1`;
- `npm audit` reported zero known vulnerabilities;
- the complete repository run passed all 791 tests with no failures, skips, or
  cancellations;
- the English baseline checked 604 text files and exactly three allowlisted
  lines;
- documentation checks covered 154 Markdown files and seven normative PERT
  examples;
- read-only check, analyze, and next passed for all 31 self-use plans;
- the temporary-link workflow passed with `perttool 0.6.0`;
- the isolated package gate accepted the 601-file, 656.1 kB
  `perttool@0.6.0` dry-run through both Contract 7 file-first and
  plan-assurance installed workflows; and
- `git diff --check` passed.

The package dry-run retained the `beta` publication target and performed no
registry mutation. The readiness lifecycle closed at
`2026-08-04T17:13:52+09:00` with source digest
`sha256:f3bce588e273089588dbca0fd3040126e1c13fa36fa7e1d2816ee9eff8e1a50d`.

## 6. Result and next gate

Completing the 3p readiness task leaves 15p. Precedence and the
`parallel-sgs` version 1 heuristic resource makespans are both 15p with zero
resource delay. Complete, non-truncated NextResult v6 recommends and starts
only `RELEASE_070_PREPARATION`.

The exact completed readiness pre-advance snapshot is retained by the
readiness completion commit before any later advance. Canonical release-plan
advance, source preparation, release candidate creation, all remote writes,
publication, npm `latest`, ASSURE-001 advance, and Issue mutation remain
separate decisions.
