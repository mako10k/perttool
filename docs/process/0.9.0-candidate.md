# `v0.9.0` Release Candidate Acceptance

- Document status: Accepted 1.0
- Review date: 2026-08-13
- Target version: `0.9.0`
- Candidate source commit: `5cb7bc873ddeb68f8dc27a78efaa61f9519a6f28`
- Candidate source tree: `ac938c0a1c69582c1ce322add922e46c7277586d`
- Completed-plan source digest: `sha256:9bd6fea777dd00eafdbc072e0c8556ad028071ac1b92e2cbe7a8cde394b01e5a`
- Release plan: [`release-0.9.0.pert`](../../plans/release-0.9.0.pert)
- Release procedure: [`0.9.0-release.md`](0.9.0-release.md)

## 1. Acceptance boundary

This record accepts one immutable local `perttool@0.9.0` tarball for the
Grammar 7 and CLI Contract 8 milestone-acceptance beta minor. It fixes the
source commit, public-package identity, read-only external baseline, artifact
bytes, and installed behavior that may be used only by a separately authorized
PUBLISH batch.

It does not authorize or perform a Git push, tag creation or push, GitHub
Release, npm publication, npm dist-tag mutation, public VSIX publication,
durable public acceptance, plan advance, or Issue mutation. Complete
NextResult v7 sequencing evidence does not replace the candidate-specific
external-write instruction.

## 2. Clean source and repository gate

Commit `5cb7bc873ddeb68f8dc27a78efaa61f9519a6f28` was clean and identified the
package, both lockfile roots, CLI, private adapter peer ranges, and MCP server
as `0.9.0`. Its committed tree was
`ac938c0a1c69582c1ce322add922e46c7277586d`.

From that clean commit, Node.js 22.22.3 `npm ci`, zero-finding `npm audit`, the
complete `npm run check`, and `git diff --check` passed. The complete gate
included all 1,041 tests, the 854-file English baseline with three exact
allowlisted lines, 245 Markdown files and seven normative PERT examples, all
38 self-use plans, isolated LSP and MCP acceptance, the private VSIX supported-
host gate, temporary linking, and the 713-file isolated public-package gate.

The repository-record rerun twice reached a five-second isolated LSP response
timeout with empty stderr while reconstructing the accepted historical target.
The same local and packaged server then passed, including the supported VS
Code host. The package-excluded acceptance harness now allows 15 seconds for
each response while retaining the existing five-second process-exit boundary;
this changes no product runtime, protocol meaning, or candidate archive byte.

## 3. Read-only external and route preflight

Direct reads found no local or remote `v0.9.0`, no GitHub Release `v0.9.0`,
and no npm `perttool@0.9.0`. npm reported `beta=latest=0.8.1`, no `alpha`, and
registry metadata modification time `2026-08-12T07:31:05.993Z`.
`origin/main` remained
`b0d8abfaaaf71154766852f614bc0bb66b6f2a26`.

The process-scoped GitHub and npm identities both resolved as `mako10k`
without displaying credential values. The authenticated `secdat` Git dry-run
projected a fast-forward of `origin/main` through
`5cb7bc873ddeb68f8dc27a78efaa61f9519a6f28` without writing. GitHub's branch-
protection endpoint returned HTTP 404 for `main`; therefore this record makes
no branch-protection claim. No external state changed.

## 4. Immutable candidate identity

The fixed-path candidate was generated once from the accepted source commit
and then made read-only:

| Property | Exact value |
| --- | --- |
| Path | `/home/katsumata-m/releases/perttool/0.9.0/candidate/perttool-0.9.0.tgz` |
| Modification time | `2026-08-13 13:57:31.039914061 +0900` |
| Mode | `0444` |
| Packed size | `2805888` bytes |
| Unpacked file bytes | `6619684` bytes |
| Files | `713` |
| SHA-1 | `52b4f4c0b6ada3e3e27f6eac061e6b67ee0f42e3` |
| SHA-256 | `88e51bfee536a62a980c83f2af77af6245a5ce578ec9aac4153b2e8f237345e7` |
| npm integrity | `sha512-0HbOZu+OZM7rprwlzymxaInEaZSl4hXRUVnY/RRN6Wywp1+dg3xbK3mWi9DxqNWKVnrjfGJyXKRxYHzIFAPWyQ==` |

The archive manifest identifies public `perttool@0.9.0`, CLI
`dist/cli.js`, the root, `perttool/core`, `perttool/node`, and schema exports,
and npm tag `beta`. It contains 23 bundled root schemas, exposes no private
adapter workspace, and includes no `adapters/` path. Its bundled README
identifies `0.9.0` as the selected local release source and preserves the
published `beta=latest=0.8.1` baseline and exact rollback pin.

## 5. Candidate-specific package acceptance

Those exact retained bytes passed `scripts/publish-npm.sh --dry-run` for npm
`beta` and an isolated installation under Node.js 22.22.3. The installed
package reported `perttool 0.9.0`, 53 commands, 23 schemas, 129 key- and
reference-identical root and Node exports, and 45 Core exports. The isolated
installation reported zero npm audit findings.

The retained candidate SHA-256 remained
`88e51bfee536a62a980c83f2af77af6245a5ce578ec9aac4153b2e8f237345e7`
after archive inspection and installed-package verification.

After adding this repository-only acceptance record, completed task state,
shared policy, self-use record, focused test, golden update, and bounded harness
stabilization, the complete Node.js 22.22.3 gate passed again: 1,041 tests, 855
English baseline files, 246 Markdown files, seven normative PERT examples, all
38 self-use plans, all adapter gates, temporary linking, and isolated-package
validation. Its disposable public package again reported SHA-1
`52b4f4c0b6ada3e3e27f6eac061e6b67ee0f42e3`; the retained candidate was not
rewritten.

`RELEASE_090_CANDIDATE` is complete at plan source digest
`sha256:9bd6fea777dd00eafdbc072e0c8556ad028071ac1b92e2cbe7a8cde394b01e5a`.
Complete, non-truncated NextResult v7 recommends and makes startable only
`RELEASE_090_PUBLISH`, with 6p precedence and heuristic resource makespans and
zero resource delay. The release procedure still requires the user to
authorize this exact candidate, release commit, Release body and assets,
target refs, npm operation, and maximum write count before any external
mutation.
