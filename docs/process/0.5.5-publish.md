# `v0.5.5` PUBLISH Record

- Status: Complete
- Publication date: 2026-07-30
- Version: `0.5.5`
- GitHub channel: prerelease
- npm channel: `beta`
- Release procedure: [`0.5.5-release.md`](0.5.5-release.md)
- GitHub Release: <https://github.com/mako10k/perttool/releases/tag/v0.5.5>
- Candidate CI: <https://github.com/mako10k/perttool/actions/runs/30543700217>

## 1. Source, tag, and CI identity

The local release commit, `origin/main`, and the peeled annotated tag
`v0.5.5` all resolve to:

```text
04055c9af3d77ef88ede26bea3bc92d09b1ea441
```

The annotated tag object is
`cb9da8b9695bc5f359d291b42d2ecb45f3f691f0`; its local tagger time is
`2026-07-30T21:44:27+09:00`. GitHub Actions run `30543700217` passed for
both Node.js 22 and Node.js 24. The run started at
`2026-07-30T12:42:50Z` and completed at `2026-07-30T12:44:11Z`.

The remote writes used the required protected routes:

```sh
secdat --dir /home/katsumata-m/perttool exec -- git push origin main
secdat --dir /home/katsumata-m/perttool exec -- git push origin refs/tags/v0.5.5
```

Remote readback, not push output alone, established both commit identities.

## 2. GitHub prerelease

The GitHub prerelease was published at `2026-07-30T12:45:01Z`, targets the
release tag, is not a draft, and contains exactly the retained package and its
`SHA256SUMS`.

| Asset | GitHub size | GitHub digest |
| --- | ---: | --- |
| `perttool-0.5.5.tgz` | 522117 bytes | `sha256:1987db1a30ac0bfabd958e1de6da181030960e87b3ae3a5805ade3d60fee5452` |
| `SHA256SUMS` | 85 bytes | `sha256:1b1d5f28bd9afb60f383d188ae7cb94df79e11bb860d4f6e85b69cc9df40ed19` |

The package asset was downloaded into a fresh directory. Its local download
modification time was `2026-07-30 21:45:16.367056971 +0900`; `cmp`,
`sha256sum -c`, and the complete Node.js 22 package acceptance passed.
An isolated installed smoke also confirmed that default PTGOV-104 preview
retains `ok=true`, the candidate, and GovernanceDecision v1, while
`--warnings-as-errors` exits 1 and retains the same candidate and decision.

## 3. npm beta publication

Immediately before publication, npm reported `beta=0.5.4`,
`latest=0.5.1`, no `alpha`, and an E404 for `perttool@0.5.5`. The downloaded
GitHub asset was then supplied exactly once to:

```sh
secdat --dir /home/katsumata-m/perttool exec \
  --inject secret:only=NPM_TOKEN \
  --inject route:prefer=secret \
  --inject final:require=NPM_TOKEN -- \
  bash scripts/publish-npm.sh --publish \
  /home/katsumata-m/releases/perttool/0.5.5/github/perttool-0.5.5.tgz
```

npm accepted the upload as `perttool@0.5.5`. The first separate immediate
version readback still returned a propagation E404 while dist metadata and the
publication time were already visible. The publication was not retried. The
next readback established:

- version publication time: `2026-07-30T12:46:35.534Z`;
- registry metadata modification time: `2026-07-30T12:46:41.537Z`;
- `beta=0.5.5`;
- unchanged `latest=0.5.1`;
- no `alpha`;
- 491 files and 3027398 unpacked file bytes;
- SHA-1 `8324e5b882145952eb21ff42066b50ba72361c63`; and
- integrity
  `sha512-SS05/dDAVUQA6kLOe1C91/l7Rca3fhZamLes/HrhqKOR5VyQEjg4UEMyUyJnDw8DnTvPduNPy3pOLdGb14pNuA==`.

The npm tarball was downloaded into a fresh directory. Its local modification
time was `2026-07-30 21:47:16.761383140 +0900`, size was 522117 bytes, and
SHA-256 was
`1987db1a30ac0bfabd958e1de6da181030960e87b3ae3a5805ade3d60fee5452`.
It was byte-identical to both the retained candidate and GitHub asset and
passed the complete Node.js 22 package acceptance.

## 4. PUBLISH result

The authorized PUBLISH is complete. Git, GitHub, and npm expose the intended
`0.5.5` beta from one immutable tarball. npm `latest` remains `0.5.1`, and no
alpha dist-tag exists. Durable acceptance remains the separate final gate.
