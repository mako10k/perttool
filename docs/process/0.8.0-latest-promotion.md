# `perttool@0.8.0` npm `latest` Promotion

- Document status: Accepted 1.0
- Promotion date: 2026-08-07
- Selected version: `perttool@0.8.0`
- Mutation: npm `latest` dist-tag only
- Final tags: `beta=latest=0.8.0`, no `alpha`
- Registry modification time: `2026-08-07T07:53:28.115Z`

## 1. Authority and precondition

After durable `0.8.0` beta acceptance, the user separately selected
`perttool@0.8.0` and authorized its promotion to npm `latest`. Direct
registry and fresh npm reads immediately before the mutation reported
`beta=0.8.0`, `latest=0.7.1`, no `alpha`, and the accepted `0.8.0` integrity
value
`sha512-emyZisy2T1MBj3OYYwG7s2XGru+6I1S9l7oy+v5NONonJ7S6E1rw5mM8o5Wz91x2E6LsmlD8fwMxWWpdid4QIw==`.
The process-scoped npm identity was `mako10k`.

## 2. Exact mutation and readback

The following logical mutation was executed exactly once through `secdat`:

```sh
npm dist-tag add perttool@0.8.0 latest
```

npm returned `+latest: perttool@0.8.0`. A direct no-cache registry read and a
separate fresh-cache npm read both reported `beta=latest=0.8.0` with no
`alpha`. The immutable `0.8.0` package retained SHA-1
`538a4f89970d5982e7e28de1c1d5ba001c89e97b` and the accepted integrity value.
No package publication or second dist-tag mutation occurred.

## 3. Consumer verification

An unqualified global installation under Node.js `22.22.3` replaced the prior
`0.7.1` installation and resolved to `perttool 0.8.0` at
`/home/katsumata-m/.nvm/versions/node/v22.22.3/bin/perttool`. Its installed
`package.json` also reports `0.8.0`.

A separate fresh-cache unqualified isolated installation resolved to
`perttool 0.8.0`, exposed 45 commands and 21 root schemas, and returned
`Perttool.SchemaResult.v1` from schema discovery. The existing private local
VSIX remained installed as
`perttool-private.perttool-vscode-private@0.0.0`; the npm tag operation did
not change its independent identity or bytes.

## 4. Remaining boundaries

The promotion changed only npm `latest`. It did not alter `beta`, recreate
`alpha`, publish package bytes, change Git or GitHub, advance a plan, mutate
an Issue, or select a public VSIX release. The immutable publication-time fact
that PUBLISH moved only `beta` while `latest` remained `0.7.1` is retained in
the publication and durable-acceptance records.
