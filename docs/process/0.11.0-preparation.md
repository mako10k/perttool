# `v0.11.0` Source Preparation

- Document status: Accepted 1.0
- Acceptance date: 2026-09-07
- Target version: `0.11.0`
- Task: `POOL_RELEASE_PREPARATION` (complete with conformant Outcome)
- Release plan:
  [`planning-pool-release-readiness.pert`](../../plans/planning-pool-release-readiness.pert)
- Release procedure: [`0.11.0-release.md`](0.11.0-release.md)

## 1. Version and compatibility identity

Package and root lockfile identities, LSP and MCP peer constraints, CLI and MCP
server versions, CHANGELOG, README, tests, goldens, and self-use identify
`0.11.0`. The prepared source retains Grammar 9, CLI Contract 10, 71 commands,
29 root schemas, 139 reference-identical root and Node runtime exports, and 51
portable Core runtime exports. No public result identity or accepted Planning
Pool behavior changed during source preparation.

Published `0.10.6` remains the exact rollback pin and its changelog history is
retained. The preparation does not import the hotfix line's package identity,
generated version artifacts, release-plan lifecycle, or candidate tarball.

## 2. Complete local and installed-package gate

The complete Node.js 22 repository gate passed on the prepared source with:

- 1,341 Node.js tests;
- 46 self-use plans under check, analyze, and Next;
- 1,319 English-baseline text files with three exact allowlisted lines and 382
  Markdown documents with seven PERT examples after this record was added;
- pinned jscpd 5.0.15 at 147 clones, 2,743 duplicate lines, and 2.662%;
- pinned Lizard 1.23.0 at 5,142 functions and 168 reviewed legacy entries;
- isolated LSP and MCP package gates;
- the supported VS Code 1.101.0 trusted and untrusted host, replacement, and
  uninstall gate under Xvfb;
- temporary-link installation as `perttool 0.11.0`; and
- the 1,019-file isolated public-package workflow, npm publication dry run,
  Contract 9 file-first compatibility, and plan-assurance compatibility.

The one initial full-test failure was a stale derived Override artifact ID after
the version change. The golden retained the new `tool_version` but not the hash
that binds it. Updating that single derived SHA-256 value made the focused test
and the complete 1,341-test rerun pass; no override semantics changed.

## 3. Boundaries retained

This preparation performed no Git commit or ref mutation, remote write, GitHub
mutation, npm publication, dist-tag change, Issue mutation, public VSIX
publication, or plan advance. The package workflow retained no release tarball.
The owner accepted Candidate 1.0, after which Preparation finished at
`2026-09-07T18:07:56+09:00` with 1,907 seconds of active time and one-person
effort. Its conformant Outcome is bound to accepted basis
`sha256:f0636f232c12b847f67aeb4f5dbfc8540492acf8b2737eea93cd5b21bf75f04b`.
Only the separately resealed `POOL_RELEASE_CANDIDATE` then became startable and
started at `2026-09-07T18:10:08+09:00`. It must commit clean source and create
and accept one immutable artifact before any publication authorization.
