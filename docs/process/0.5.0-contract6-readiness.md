# `v0.5.0` Contract 6 Readiness

- Document status: Accepted 1.0
- Acceptance date: 2026-07-29
- Release target: `0.5.0`
- Release procedure: [`0.5.0-release.md`](0.5.0-release.md)
- Release plan: [`release-0.5.0.pert`](../../plans/release-0.5.0.pert)
- Implementation acceptance:
  [`project-actuals-acceptance.md`](project-actuals-acceptance.md)
- Repository-language acceptance:
  [`english-baseline-acceptance.md`](english-baseline-acceptance.md)

## 1. Decision

Accept `RELEASE_050_CONTRACT_6_READINESS`. The completed Project Actuals and
English Baseline workstreams retain their reached finish milestones and
accepted evidence. The standard source, package root, CLI, command registry,
help, Guide, schemas, local-link workflow, and isolated-package workflow
activate Grammar 5 and CLI Contract 6 together. No hidden finding requires a
return to an earlier implementation or language-migration gate.

This decision starts only `RELEASE_050_PREPARATION`. It does not change
package identity from `0.4.0`, create a release candidate, perform an external
write, move npm `latest`, or close Issue #4.

## 2. Accepted input identity

| Input | Accepted state |
| --- | --- |
| Project actuals plan | Source digest `sha256:b899fb3f5aa06c2521976a8e0e27fbadb87c74af56b1d5f10efda63425def1d3`; reached `ACTUALS_ACCEPTED`; zero tasks, errors, and warnings |
| Project actuals acceptance | Accepted fourteen-case trace through source, lifecycle, advance, real first-parent history, Core, CLI, schemas, help, package root, link, and installed workflows |
| English baseline plan | Source digest `sha256:160e1af66d7bc8c2c8ea6b7a9f62ad7b533ec74e1ae1adc3b5d9f176745362a4`; reached `ENGLISH_BASELINE_ACCEPTED`; zero tasks, errors, and warnings |
| English baseline acceptance | Accepted cross-surface trace and exact three-line Japanese-script allowlist, with no runtime i18n |
| Release plan | Pre-readiness digest `sha256:914ee9f43985c5150ffc0e22b2a9be6d0d46540c008954179a3e0808d5083701`; reached `RELEASE_050_GATE_ACCEPTED`; complete NextResult v5 starts only `RELEASE_050_CONTRACT_6_READINESS` |
| Release-gate history | Git commit `1641a32` records the exact completed gate-design pre-advance snapshot; `ab79306` records its canonical advance |

The implementation and language plans are consumed as reached inputs. Their
completed tasks are not copied into the release plan.

## 3. Public source and package boundary

The source boundary is complete and coordinated:

- the active parser, semantic validator, formatter, mutation, lifecycle, and
  advance paths accept Grammar 5 while preserving Grammar 1 through 4;
- governed `task start`, `task suspend`, `task resume`, and eventful
  `task finish` use explicit fixed-offset times and source-preserving safe
  writes;
- suspended tasks remain unfinished, have their own NextResult v5 group, and
  release renewable resources;
- `project history` reads a revision-bound first-parent path history and
  returns `Perttool.ProjectHistoryResult.v1` with typed incomplete and
  unavailable causes;
- `project observe-velocity` derives read-only exact observations and never
  changes declared velocity;
- mutation, analysis, next, and unit migration expose
  `Perttool.MutationResult.v3`, `Perttool.AnalysisResult.v4`,
  `Perttool.NextResult.v5`, and `Perttool.UnitMigrationResult.v3`;
- every active JSON envelope identifies CLI Contract 6;
- one immutable registry exposes exactly 33 commands and the complete active
  result-schema inventory; and
- the standard package root, command help, domain Guide, local-link workflow,
  and isolated installed-package workflow use those same public identities.

The current version-bearing source still reports `perttool 0.4.0`. That is an
expected release-preparation input, not a mixed public-contract state:
`RELEASE_050_PREPARATION` owns the atomic package, lockfile, CLI version,
README, CHANGELOG, migration, and release-test update to `0.5.0`.

## 4. Compatibility and failure boundaries

- Grammar 1 through 4 retain their accepted field sets and meanings.
- Status-only `task finish` remains available for Grammar 1 through 4.
- Grammar 5 requires eventful finish and does not accept direct
  `task set --status suspended`.
- Contract 5 runtimes reject Grammar 5 and Contract 6 spellings rather than
  silently interpreting them.
- Contract 6 has no Contract 5 runtime switch, compatibility alias, or
  conditional old envelope.
- Governance authorization, digest locking, target validation, atomic safe
  write, retry identity, and post-write verification remain composed for
  lifecycle persistence.
- History and observation remain read-only. No Git mutation or automatic
  declared-velocity adoption is present.
- REOPEN, ADV-001, override apply and durable audit, MIG-08, Issue
  synchronization, LSP/VSIX/MCP, and runtime i18n remain explicit non-goals.

## 5. Verification

Verification ran on Node.js 25.1.0, above the Node.js 22 baseline; CI retains
Node.js 22 and 24. The gate-design snapshot passed the complete repository
gate with 643 tests, 97 Markdown documents, seven normative PERT examples,
all twenty-one self-use plans, the temporary-link workflow, package
normalization dry-run, and isolated Contract 6 file-first installation.
After canonical gate advance, the focused release, documentation, language,
and all-plan self-use checks passed again. The complete readiness snapshot
then passed all 643 tests, 98 Markdown documents, seven normative PERT
examples, all twenty-one self-use plans, the temporary-link workflow, and the
468-file isolated Contract 6 package workflow.

```sh
npm run check
node dist/cli.js project show plans/project-actuals.pert --format=json
node dist/cli.js project show plans/english-baseline.pert --format=json
node dist/cli.js document check plans/release-0.5.0.pert --format=json
node dist/cli.js dag analyze plans/release-0.5.0.pert --format=json
node dist/cli.js dag next plans/release-0.5.0.pert --format=json
node dist/cli.js help --format=json
git diff --check
```

The release-plan precedence and `parallel-sgs` version 1 heuristic resource
makespans remain 16p with zero resource delay before readiness completion.
The inherited `19p/2d` velocity produces exact `32/19d` forecasts.

## 6. Result and next gate

Completing the 2p readiness task leaves 14p. Precedence and heuristic resource
makespans become 14p with zero resource delay; inherited forecasts become
`28/19d`. Complete, non-truncated NextResult v5 under
`recommendation_v1_plus_release_gate` recommends and starts only
`RELEASE_050_PREPARATION`.

Preparation may now align version-bearing source at `0.5.0` and run the
complete local release-source gates. The user's named authorization remains
available for later candidate and PUBLISH tasks only after every predecessor
gate passes. npm `latest` promotion and Issue #4 closure remain separate
decisions.
