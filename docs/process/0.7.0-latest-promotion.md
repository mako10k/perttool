# `v0.7.0` npm `latest` Promotion Record

- Status: Complete
- Promotion date: 2026-08-04
- Version: `0.7.0`
- Registry identity: `perttool@0.7.0`
- Authorized tag mutation: `latest`
- npm principal: `mako10k`
- Repository HEAD at promotion: `92cc8dece687be580a1fcefddf95d218f4c602cf`
- Release artifact effect: none

The user separately authorized promotion after `v0.7.0` publication. This
operation was outside PUBLISH and durable acceptance. It did not republish the
package, alter `beta`, recreate `alpha`, change Git or GitHub, advance either
plan, or mutate an Issue.

## Preflight

Fresh-cache npm reads established `beta=0.7.0`, `latest=0.6.0`, no `alpha`,
and the existing immutable `perttool@0.7.0` identity:

- 601 files and 3837266 unpacked file bytes;
- SHA-1 `ad18e2e9af97b9926b14210a7afe80c9e9a9ff0c`; and
- integrity `sha512-RykvVa0U9wawqtbVLz1zWisaYWnwW3SPC2xmELV/KYmVCOD+Uypr27hskwsD1Xx+8rBDgr2vrN+JeTqfosN25A==`.

Remote `main` matched the local repository HEAD. Annotated `v0.7.0` retained
tag object `923d239027a5b269418eeec1e4bfc9bd92b3cb99` and peeled release commit
`1279e3cbdf8f018e84380e62ac7516a2c17aa86e`. The GitHub prerelease remained
published and non-draft. Process-scoped npm authentication returned
`mako10k` without displaying credentials.

## One controlled mutation

The exact mutation was executed once:

```sh
npm dist-tag add perttool@0.7.0 latest --userconfig=/dev/stdin
```

`NPM_TOKEN` was injected only into that `secdat`-protected process. npm
returned `+latest: perttool@0.7.0`. Registry metadata modification time after
the operation was `2026-08-04T10:12:16.615Z`.

## Readback and consumer verification

Fresh-cache readback established `beta=latest=0.7.0` with no `alpha`. An
unqualified isolated installation under Node.js `v25.1.0` and npm `11.18.0`
resolved `perttool 0.7.0` and exposed:

- CLI Contract 7;
- the 44-command registry;
- the 20-root schema catalog; and
- the same registry integrity and SHA-1 as the published immutable package.

No retry or second dist-tag mutation occurred. The repository worktree
remained clean immediately after registry and isolated-install verification.

## Acceptance boundary

The publication record remains the authority for the publication-time fact
that `latest` stayed at `0.6.0` while PUBLISH moved only `beta` to `0.7.0`.
Durable acceptance must verify both that historical fact and the separately
authorized post-publication state `beta=latest=0.7.0`. Acceptance itself must
not change npm tags, the immutable artifact, either plan frontier, or an Issue.
