# `v0.11.0` Immutable Candidate 1.0

- Document status: Accepted 1.0
- Candidate date: 2026-09-07
- Acceptance date: 2026-09-07
- Task: `POOL_RELEASE_CANDIDATE` (complete with conformant Outcome)
- Release plan:
  [`planning-pool-release-readiness.pert`](../../plans/planning-pool-release-readiness.pert)
- Release procedure: [`0.11.0-release.md`](0.11.0-release.md)
- Candidate source commit: `6ad44db8aa833e5f7fbdc49adfef39419151984a`
- Candidate source tree: `8a469730aa017f289a94865b838ff127a61eaa88`
- Exact rollback pin: `perttool@0.10.6`

## 1. Candidate meaning

Candidate 1.0 proposes the exact committed source and retained tarball below as
the first public Grammar 9 and CLI Contract 10 Planning Pool beta. It includes
Work, project-owned Event and Activity AoA, global Work order, planning-only
dependencies, audited reshape, guided intent requests, projection and narrow
deferral, active Windows, independent observations, bounded first-parent
history, and the accepted Grammar 9 and Next-result corrections.

The public surface is 71 commands, 29 active root schemas, 139
reference-identical root and Node runtime exports, and 51 portable Core runtime
exports. Private LSP, VSIX, and MCP workspaces are not distributed in the npm
tarball. Grammar 1 through 8 remain readable for their supported operations;
Planning Pool mutation requires Grammar 9. Exact `perttool@0.10.6` remains the
rollback pin, but it cannot edit Grammar 9 Planning Pool documents.

## 2. Clean source and complete gate

The source was committed locally as
`6ad44db8aa833e5f7fbdc49adfef39419151984a`. The worktree was clean before the
candidate gate and tarball creation. The complete Node.js 22 `npm run check`
gate passed from that commit with:

- 1,341 Node.js tests;
- pinned jscpd 5.0.15 at 147 clones, 2,743 duplicate lines, and 2.662%;
- pinned Lizard 1.23.0 at 5,142 functions and 168 reviewed legacy entries;
- 1,319 English-baseline files with three exact allowlisted lines;
- 382 Markdown documents and seven PERT examples;
- 46 self-use plans;
- isolated LSP and MCP package gates;
- the supported VS Code 1.101.0 trusted and untrusted host, replacement, and
  uninstall gate under Xvfb;
- temporary-link installation as `perttool 0.11.0`; and
- the 1,019-file isolated-package workflow, npm publication dry run, Contract
  9 file-first compatibility, and plan-assurance compatibility.

## 3. Immutable artifact

Exactly one tarball was retained outside the worktree:

- retained path:
  `/home/katsumata-m/perttool-release-candidates/0.11.0-candidate-1/perttool-0.11.0.tgz`;
- filename: `perttool-0.11.0.tgz`;
- files: 1,019;
- packed bytes: 3,273,360;
- unpacked bytes: 11,362,352;
- npm SHA-1: `ed3da009198375d0be88a6294e2aeb38c7103ec4`;
- SHA-256:
  `fa43e222fa6a53c0a5287a1cca7700b7808179513743db0a9a5790c2192fecf4`;
- npm integrity:
  `sha512-cvAe+E7lHECSKWKwG2lTqA3/cRUXRXzMrUjR5zNWMt5JiBcjyWNzn7pekkTg9qJFI6uD8orW1MGU1wbbsF/zMg==`;
- retained mode: `0444`.

The complete package gate and the retained pack independently produced the
same npm SHA-1 and integrity. The retained bytes were then passed directly to
`scripts/check-package.sh`; that exact file passed the npm publication dry run,
fresh global-prefix installation, Contract 9 file-first compatibility, and
plan-assurance compatibility. The installed CLI reports `perttool 0.11.0`, and
the installed package exposes the candidate's 71 commands, 29 schemas, 139
root and Node exports, and 51 Core exports without repository-only or private
adapter content.

## 4. External identity and route preflight

Fresh authenticated read-only preflight established:

- remote branch `origin/codex/planning-pool-0.11-wip` remained at
  `2c7fb62761bac098ce9f8385b0faa6191e4618ca`;
- Candidate 1.0 was zero commits behind and six commits ahead;
- an authenticated Git push dry run accepted the exact fast-forward from
  `2c7fb627` to `6ad44db`, without performing the push;
- local and remote `v0.11.0` tags were absent;
- GitHub Release `v0.11.0` was absent;
- `perttool@0.11.0` returned npm `E404`, so the version was unused; and
- npm reported `beta=0.10.6`, `latest=0.10.5`, and no `alpha` tag.

The GitHub commands used the repository's established protected credential
route and a process-local GitHub CLI credential helper. No credential value was
printed, and no persistent Git configuration was changed.

## 5. Acceptance and publication boundary

The owner accepted only the exact commit-and-tarball pair above as the
immutable `POOL_RELEASE_CANDIDATE` result. The task finished at
`2026-09-07T18:43:45+09:00` with 2,017 seconds of active time and one-person
effort. Its conformant Outcome is bound to accepted basis
`sha256:c3200795a64a2e6172ee0a3a75b50522836441f3cdaf263601855ebdb7f56029`.
The resulting plan source digest is
`sha256:95d98b2d6ef1c99da2d1831c805484e5a154f3c468c6944bd5af7486477932bd`.
This acceptance did not publish anything.

The owner later separately authorized only the causal selected reseal for
`POOL_RELEASE_PUBLISH`. That reseal accepted computed basis
`sha256:140ff8d4fe78a697ba34da6fe82d279838344fdb8a1346a17b416d0482e905e5`
with the Candidate Outcome as its exact input and produced plan source digest
`sha256:98d9810574ac77ad84bb44c0dca435288bcf4d72fef2c7425b134d377af49ed1`.
PUBLISH is now verified, ready, recommended, and startable, but it has not
started. Downstream `POOL_RELEASE_ACCEPTANCE` remains intentionally stale
until publication produces its own accepted Outcome.

The owner then separately authorized only the local PUBLISH lifecycle start.
`POOL_RELEASE_PUBLISH` became active at `2026-09-07T19:14:17+09:00` through
work event
`WE-03901625df271d4e3642e4673e030cd2daf4cde0a37f8bf1a1842a1c7f5f2d8b`,
producing plan source digest
`sha256:b9801bacd32ac8a8fe949c8daa800dce317454563abd19e7b1a6313bfaff3f32`.
That local start did not authorize or perform an external write.

`POOL_RELEASE_PUBLISH` remains a separate boundary. A later authorization must
name this exact source commit, SHA-256, and the external write batch fixed by
the release procedure: push and verify the release commit and annotated tag,
require Node.js 22 and 24 hosted CI, create the GitHub prerelease with
`SHA256SUMS`, and publish the byte-identical tarball once to npm `beta`.
Publication leaves npm `latest` unchanged. Issue mutation, public VSIX
publication, plan advance, release-plan advance, and later `latest` promotion
remain separate.
