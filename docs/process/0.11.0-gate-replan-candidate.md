# `v0.11.0` Release Gate P1 Replan Record

- Status: Candidate 1.0 applied exactly and independently read back; the
  selected-frontier reseal completed separately
- Date: 2026-08-31
- Source PERT digest:
  `sha256:80aea315154da1aa810a8366f21b3fa5150ed105641e23050fa372a7d80fd59d`
- Current PERT digest:
  `sha256:6c0592deaa94395325fffc817e855a1732db3d13f0bc5b36c3cb0ecb4fe7b3b5`
- External inputs: open Issues #37 and #38, both labeled `bug` and
  `priority:P1`
- Release effect: none

## 1. Applied Issue readback

The owner-authorized external mutation batch completed once and was read back:

- Issue #38 was created open with the exact 5,045-byte body SHA-256
  `8e372de505defea91735ebf666335159141e3b5fa0077b95653c43f79515d7d9`
  and labels `bug` and `priority:P1`.
- Issue #37 remained open with its title and body preserved and gained only
  labels `bug` and `priority:P1`.
- Neither Issue has a comment, assignee, milestone, or project item.
- Issue #21, Issue #35, PERT, Git, npm, and release state were not mutated.

## 2. Applied mutation sequence

The PERT requires two separately bound mutations because task lifecycle events
are not atomic-batch members.

### Step 1: suspend the active gate task

The reviewed and applied operation was exactly:

```text
operation: task.suspend
task: POOL_RELEASE_GATE_DESIGN
event: EV_POOL_RELEASE_GATE_DESIGN_SUSPEND_P1_REPLAN_001
occurred_at: 2026-08-31T16:54:37+09:00
reason: Release gate blocked by independently reproduced Issues #37 and #38 pending owner-approved correction replan
actor: codex
```

The lifecycle mutation is not governance-applicable. It changes only task
status `active` to `suspended` and appends that exact event. Source digest
`sha256:80aea315...59d` becomes intermediate digest
`sha256:4a96c30e0f81d11e27c03f3a713e17491a04d7d9901e24de9cd833740af95871`.

### Step 2: apply one DAG-governed atomic replan

The exact request
[`0.11.0-gate-replan-request.json`](0.11.0-gate-replan-request.json) to the
intermediate source was applied once. The request is 2,972 UTF-8 bytes with
SHA-256
`b7535d14b0d5150872a3c5d8f66e93688ecedf37cf5d91bfd4a5855113343787`.
It:

1. adds planned 5p `POOL_GOVERNANCE_BINDING_FIX` for Issue #38;
2. adds planned 8p `POOL_NEXT_SIGNAL_CONSISTENCY` for Issue #37;
3. gives each task its own acceptance milestone;
4. joins both acceptance milestones into `POOL_RELEASE_GATE_ACCEPTED`; and
5. updates the suspended Gate Design contract to name both prerequisites.

The final candidate is valid, contains 19 tasks, has 31,121 UTF-8 bytes, and
has digest
`sha256:6c0592deaa94395325fffc817e855a1732db3d13f0bc5b36c3cb0ecb4fe7b3b5`.
The batch affects only the `dag` governance scope, requires owner `user`, and
is not write-authorized without an assertion bound to that exact candidate.
The owner authorized that exact candidate. Optimistic locking bound the first
write to the source digest and the second write to the intermediate digest.
Readback and a separate non-authoring replay found the current PERT byte-
identical to the final candidate.

An earlier preview that moved the suspended Gate Design task behind a new
unreached join failed with `PTDAG-207` and is rejected. The retained candidate
keeps the task's reached source unchanged and adds the two correction paths as
independent incoming requirements of the existing gate-accepted milestone.

## 3. Assurance and execution effect

The applied candidate deliberately leaves the new tasks unsealed, makes the
changed Gate Design contract review-required, and makes release preparation
and its successors review-required through the new planning inputs. It reports
`replan_and_reseal`; it does not silently reuse the old accepted bases.

Structurally, both new tasks are ready, the Gate Design task is suspended, and
release preparation remains upcoming. Shared `ACCEPTANCE_REVIEWERS` capacity
allows only one correction to run at a time. The current ranking recommends
`POOL_NEXT_SIGNAL_CONSISTENCY`, but plan assurance withholds start authority
until the exact new and changed contracts are separately accepted and sealed.

The PERT replan write is complete. A later, separately reviewed and owner-
authorized write sealed only `POOL_NEXT_SIGNAL_CONSISTENCY`, as recorded in
[`0.11.0-gate-replan-reseal-candidate.md`](0.11.0-gate-replan-reseal-candidate.md).
The other initial seal, changed-contract reseals, task starts, task outcomes,
Gate Design resume, criterion, receipt, release preparation, commit, push,
publication, and Issue closure remain separate decisions.

## 4. Owner decision boundary

The owner accepted this candidate and authorized only the exact two-step PERT
mutation sequence above: one lifecycle suspension against source digest
`sha256:80aea315...59d`, followed by the exact atomic request against
intermediate digest `sha256:4a96c30e...5871`. Both writes are complete. That
authorization did not include any seal, task start, implementation, release,
or external write.

The later selected-frontier reseal had its own independent review and owner
authorization. It does not retroactively broaden this replan authorization.
