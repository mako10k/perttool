# Version 0.10.6 Release-Plan Independent Review

Review date: 2026-08-28
Review status: `ACCEPT_WITH_NOTES`
Owner approval status: Accepted exact candidate
Candidate document ID: `RELEASE_0106`
Candidate digest: `sha256:1498c0628400d1c5d97e52864c0e6aea19693292f206ee775e6e93bcb53ef043`
Candidate size: 4,979 UTF-8 bytes
Candidate base: peeled `v0.10.5` commit `7379870db2ec000243f02cda6d86af514af7feef`

## Candidate boundary

The candidate sequences six serial tasks and 22 points through correction and
release-gate design, the Issue #36 correction, source preparation, one
immutable candidate, separately authorized publication, and durable
acceptance. It retains Grammar 8, CLI Contract 9, public identities,
fail-closed authority, safe persistence, and exact `v0.10.5` rollback.

The correction must be committed independently from version and release
records. Only that correction commit may later be applied to preserved branch
`codex/planning-pool-0.11-wip` at
`56104d993c6e33978edf3d30c6ff63cc044633db`. Publication, npm `latest`, Issue
mutation, public VSIX publication, release-plan advance, and version 0.11
integration remain separate gates.

## Independent review history

The first read-only reviewer returned `REJECT` because its exact-candidate and
external checks did not complete before interruption. It reported no candidate
content defect, but its result is retained as non-authoritative evidence and
was not treated as a pass.

A fresh read-only reviewer independently reproduced the exact candidate and
returned `ACCEPT_WITH_NOTES` with no P0, P1, or P2 finding. It verified:

- candidate digest
  `sha256:1498c0628400d1c5d97e52864c0e6aea19693292f206ee775e6e93bcb53ef043`
  and 4,979 UTF-8 bytes;
- six serial tasks totaling 22 points;
- successful document validation and equal 22-point precedence and resource
  makespans;
- sole recommendation `RELEASE_0106_GATE_DESIGN`;
- affected scopes `goal` and `dag`, required owner `user`, and an unauthorized
  assertion-free preview;
- exact worktree and peeled-tag base
  `7379870db2ec000243f02cda6d86af514af7feef`;
- the affected independent regex in
  `src/milestone-acceptance/mutation.ts`;
- compatible patch, canonical validated pre-change governance, delegate and
  fail-closed case coverage, and correction-only commit separation.

The reviewer's sole P3 note is that it did not independently query the remote
tag, GitHub Release, or npm version identities. The authoring agent's separate
direct preflight found no remote `v0.10.6` tag, no GitHub Release, and npm E404
for `perttool@0.10.6`; npm reported `latest=beta=0.10.5`. This direct preflight
does not replace the candidate-stage independent external readback.

## Review conclusion

The exact candidate is structurally and semantically suitable for owner
approval. Because it changes both goal and DAG scopes, it required one fresh
candidate-bound confirmation from owner `user`. Reviewer acceptance did not
replace that owner approval or authorize implementation, publication, or any
other external mutation.

## Owner approval and governed write readback

The owner separately approved the exact candidate identified above. A fresh
preview with actor `codex` and the candidate-bound `user` assertion reproduced
the same source and candidate digests, affected scopes `goal` and `dag`, and
an authorized governance decision. The candidate was then written once to
`plans/release-0.10.6.pert` through the separate-output safe-write path.

Readback confirmed 4,979 UTF-8 bytes and exact source digest
`sha256:1498c0628400d1c5d97e52864c0e6aea19693292f206ee775e6e93bcb53ef043`.
Document check reported Grammar 6, six tasks, seven milestones, three
resources, and no diagnostic. Precedence and heuristic resource makespans are
both 22 points with zero resource delay. Complete start authority lists only
`RELEASE_0106_GATE_DESIGN` as startable and recommended. The write did not
start that task or authorize implementation, publication, remote mutation,
Issue mutation, plan advance, npm `latest`, public VSIX publication, or 0.11
integration.
