# `v0.9.3` Emergency Self-Review

- Document status: Accepted 1.0
- Acceptance date: 2026-08-14
- Task: `RELEASE_093_SELF_REVIEW`
- Public baseline: `b47a0a450f83070340fc52df88a0243f80ef795e`
- Baseline tag: `v0.9.2`
- Release plan: [`release-0.9.3.pert`](../../plans/release-0.9.3.pert)
- Correction contract:
  [`contract8-emergency-corrections.md`](../specs/contract8-emergency-corrections.md)

## 1. Frozen baseline

The emergency branch and isolated worktree start at the exact commit shared by
`origin/main` and peeled annotated tag `v0.9.2`. npm reported
`beta=0.9.2`, `latest=0.9.0`, no `alpha`, and no published `0.9.3`.

The baseline exposes Grammar 7, CLI Contract 8, 53 commands, 23 root schemas,
129 root and Node runtime exports, and 45 Core runtime exports. These identities
are compatibility constraints for the patch.

## 2. Direct reproductions

The review reproduced all three defects before implementation:

- Issue #14: both plan-assurance commands rejected the active Grammar 7
  `editor-mutations.pert` source with old-grammar `PTDSL-*` diagnostics while
  `dag next` evaluated its complete assurance projection;
- Issue #16: image-platform commit `627514d` exited 70 because the provisional
  acceptance composition reported overlapping `TextEdit` ranges; and
- Issue #17: image-platform commit `651ab2b` exited 70 because the provisional
  planner lost Grammar 7 validation after the verified receipt was appended
  after completed-task events.

The image-platform repository and the original perttool implementation branch
were read only. No workaround, direct plan edit, force option, stash, or reset
was used.

## 3. Compatible patch decision

Suffix-free `0.9.3` is the narrow compatible patch. The correction restores
the already active Contract 8 behavior and does not change grammar syntax,
commands, schemas, result identities, authority, persistence, or force
semantics. A minor or contract-version change would overstate the boundary.

The release may move only npm `beta`. npm `latest`, release-plan advance,
public VSIX publication, and unrelated work remain separate decisions.
