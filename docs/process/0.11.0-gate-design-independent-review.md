# `v0.11.0` Gate Design Candidate 3.0 Independent Review

- Review date: 2026-09-07
- Review mode: read-only repository-wide exact-byte review
- Verdict: `PASS`
- Findings: zero P0, P1, P2, or P3 findings
- Owner disposition: accepted on 2026-09-07 for the exact reviewed Candidate
  3.0 semantics
- Release effect: none

## 1. Exact reviewed candidate

The independent reviewer verified these bytes before review and again before
returning the verdict. The primary agent repeated the final digest readback.

| Artifact | UTF-8 SHA-256 |
| --- | --- |
| `docs/process/0.11.0-gate-design.md` | `5a9bf434e7f9065be31f3046024db88c60c303ab40839bf1972ef0d63ea57d43` |
| `docs/process/0.11.0-release.md` | `ca0d7d42ab73747a4ddb1f50a9624611d84cae4e94d3d772b097e9be61342d44` |
| `docs/adr/0003-beta-versioning.md` | `71cea1f7f72a4de2e7b850d990992a2ed2906f6e68a84c7792be7014406832ae` |
| `plans/planning-pool-release-readiness.pert` | `ae301909e5b2413c5a5a9b3121cddd4a94f4acfd2fd6331cb538d1740686c0cb` |
| `test/release-0.11.0-design.test.mjs` | `0e381d57fda58168ae761b8d111b0176d19c243b5c6b5a5cdd4710366ad5e969` |

This review record is separate from those frozen candidate bytes. It does not
modify them merely to add a backlink.

## 2. Review questions and verdicts

### 2.1 Version and rollback boundary

`PASS`. Requirements item 26 and Post-MVP Slice 8A define Grammar 9, CLI
Contract 10, 71 commands, 29 root schemas, 139 root and Node exports, 51 Core
exports, `Perttool.AdvanceResult.v4`, and
`Perttool.UnitMigrationResult.v5` as one public compatibility boundary.
Suffix-free `0.11.0` represents that breaking pre-1.0 beta boundary.
`0.10.7` would understate it, while `1.0.0` would introduce an unsupported
stable-series promise. Exact published `0.10.6` remains the Grammar 8 and CLI
Contract 9 rollback pin.

Read-only external evidence showed npm `beta=0.10.6`, `latest=0.10.5`, no
published `0.11.0`, no remote `v0.11.0` tag, and no matching GitHub Release.
Those drift-prone facts must be read again at candidate acceptance.

### 2.2 Preserved guards and correction inputs

`PASS`. The reviewer traced complete validation, expected-digest, source and
repository race, regular-file and symlink, atomic rename, post-write
verification, governance, plan-assurance, milestone-acceptance, and history
protection through the Planning Pool contract and current implementation. The
accepted Issue #36 canonical-governance correction, Grammar 9 milestone-
acceptance composition, Issue #37 `R intersection L intersection T
intersection A` start-authority correction, and Issue #38 outer-source
governance rebinding are present on the candidate source line.

Forty-three focused tests covering those corrections and guard paths passed.
They included history-unavailable, destructive-overlap, governance-denial, and
source, `HEAD`, and index-race no-write cases. The primary evidence also
contains a fresh complete 1,341-test pass and the full adapter, Xvfb-supported
VSIX host, temporary-link, and isolated-package gates.

### 2.3 PERT frontier and partial reseal

`PASS`. Runtime readback of the exact PERT bytes reports only
`POOL_RELEASE_GATE_DESIGN` as active. The task is `verified` and `unfinished`
with equal accepted, computed, and exported basis. Ready, runnable-now,
recommended, and startable sets are empty. Preparation, candidate, PUBLISH,
and durable acceptance remain planned and upcoming.

The remaining `replan_and_reseal` action affects those four later tasks. It is
a review queue caused by the changed release frontier, not bulk acceptance or
start authority. Gate Design's selected reseal remains complete.

### 2.4 Separate authority

`PASS`. At review time, ADR 0003 retained the `0.11.0` amendment as `Proposed`.
The owner subsequently accepted the exact reviewed Candidate 3.0 semantics,
making that amendment and Gate Design boundary Accepted. This acceptance
selects only the version boundary and procedure. It does not
authorize task finish or Outcome registration, Preparation, candidate
commitment, push, tag, GitHub Release creation, npm publication, distribution-
tag movement, Issue mutation, or plan advance. Each remains governed by its
separate lifecycle and authority boundary.

## 3. Findings and uncertainty

There are no P0 through P3 findings. No source defect was established, so
there is no source-cause classification.

The cause of the first complete-gate MCP legacy-rejection child timeout remains
unknown. The unchanged MCP file subsequently passed 11 of 11 tests and the
fresh complete run passed 1,341 of 1,341 tests. The reviewer did not classify
the non-reproducing timeout as a blocker to any of the four review questions.

## 4. Coverage and boundaries

The review covered the five exact candidate artifacts, Requirements item 26,
Post-MVP Slices 8 and 8A, the Planning Pool specification and integration
acceptance, the Grammar 9 composition correction, Issues #36, #37, and #38,
relevant implementation and schemas, public catalogs, PERT runtime projection,
Git ancestry and patch identity, and read-only npm and GitHub state. It used
one independent evidence family and did not repeat the complete suite.

This `PASS` verdict satisfied the independent review predecessor. The owner
then accepted the five exact artifact digests in section 1 as Candidate 3.0's
semantic basis. Recording that disposition changes current status prose and
focused assertions after the frozen review; it does not alter the reviewed
PERT bytes. The acceptance itself did not finish `POOL_RELEASE_GATE_DESIGN`,
add a milestone criterion, receipt, or task Outcome, reseal Preparation, commit
or push the worktree, or authorize any release or Issue mutation. A later
separate instruction authorized the exact task finish and conformant Outcome,
producing current PERT digest
`sha256:1d1862bb053912097656b1d493108652711fde26e586846688b7a8faca100c52`.
Preparation reseal and start remain separate lifecycle decisions.
