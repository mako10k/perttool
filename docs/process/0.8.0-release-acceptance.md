# `v0.8.0` Adapter Platform and Historical DAG Release Acceptance

- Document status: Accepted and latest-promoted 1.1
- Acceptance date: 2026-08-07
- Version: `0.8.0`
- Product maturity: beta
- GitHub channel: prerelease
- Release commit: `db5c2f878c3ec62ea6885f40bab1f04c6541077b`
- Annotated tag object: `37ae676807c48cc7320df3d9c2592d7cca3a5d31`
- Final plan digest: `sha256:e5b59c620a2c0f13093a0697b7c4060a767129b14193ed4c3b23f00e5e4298df`
- Candidate identity: 2753740 bytes, SHA-256 `d761e2a159d2d60eb981efda403cc6b00c4eac9e31503b2e857c0b851ac00b28`
- Public verification: complete

## 1. Git, CI, and GitHub

`origin/main`, the local release commit, and the peeled local and remote
`v0.8.0` tag all agree at
`db5c2f878c3ec62ea6885f40bab1f04c6541077b`. Repository-check run
[`31154880011`](https://github.com/mako10k/perttool/actions/runs/31154880011)
passed Node.js 22 and 24 for that exact commit.

GitHub Release [`v0.8.0`](https://github.com/mako10k/perttool/releases/tag/v0.8.0)
is prerelease `366565943`, non-draft, and was published at
`2026-08-07T06:46:55Z`. Its package asset and independently downloaded
`SHA256SUMS` report and validate the accepted SHA-256. The downloaded package
passed the isolated public-package workflow.

GitHub reports no branch protection for `main`; this acceptance therefore
makes no protection claim. It separately records the authenticated push,
exact remote SHA readback, successful CI, and immutable tag identity.

## 2. Common public artifact

The retained candidate, downloaded GitHub asset, and downloaded npm tarball
were pairwise byte-identical. All copies have 679 files, 2753740 packed bytes,
6330315 unpacked file bytes, SHA-1
`538a4f89970d5982e7e28de1c1d5ba001c89e97b`, SHA-256
`d761e2a159d2d60eb981efda403cc6b00c4eac9e31503b2e857c0b851ac00b28`,
and npm integrity
`sha512-emyZisy2T1MBj3OYYwG7s2XGru+6I1S9l7oy+v5NONonJ7S6E1rw5mM8o5Wz91x2E6LsmlD8fwMxWWpdid4QIw==`.

The archive exposes only the root, `perttool/core`, `perttool/node`, and
schema package paths and contains no private `adapters/` workspace path.
Package identity, publication normalization, file-first Contract 7, and plan-
assurance package gates passed against the same immutable bytes.

## 3. Registry and installed behavior

npm publication became durable at `2026-08-07T06:48:55.944Z`. PUBLISH moved
only `beta` to `0.8.0`, retained `latest=0.7.1`, and left `alpha` absent.

Fresh isolated installations resolved as follows:

| Request | Installed version |
| --- | --- |
| `perttool@0.8.0` | `perttool 0.8.0` |
| `perttool@beta` | `perttool 0.8.0` |
| `perttool@latest` | `perttool 0.7.1` |
| rollback pin `perttool@0.7.1` | `perttool 0.7.1` |

Both installed 0.8.0 paths expose 45 commands, 21 root schemas, 122 key- and
reference-identical root and Node runtime exports, and 45 Core exports.
Installed `dag history` returned `Perttool.HistoricalGraphResult.v1`, model
`Perttool.HistoricalDagModel.v1`, ten checkpoints, and seven lineage
occurrences from the accepted repository fixture.

An installed `task add` preview placed exactly one task before the first
`plan_seal`, confirming the declaration-identity correction. The preview did
not change the tracked historical plan bytes. Existing Grammar 6, CLI Contract
7, governance, plan assurance, history safety, source-preserving mutation,
and recommendation and temporal authority remain compatible.

## 4. Acceptance result

The suffix-free `0.8.0` beta is durably accepted as the additive adapter-
platform, historical-DAG, and declaration-identity release. All six tasks and
22p are complete; precedence and `parallel-sgs` version 1 heuristic resource
makespans are zero, and complete NextResult v6 has no ready, recommended, or
startable task.

Durable acceptance performed no further external mutation. At that boundary,
npm `latest` promotion, public VSIX identity or publication, both input-plan
advances, release-plan advance, Issue mutation, and unrelated backlogs
remained separate decisions.

## 5. Post-acceptance `latest` promotion

The user later separately selected `perttool@0.8.0` and authorized one npm
`latest` mutation. Preflight fixed `beta=0.8.0`, `latest=0.7.1`, no `alpha`,
the accepted package integrity, and process-scoped principal `mako10k`. The
exact mutation succeeded once; direct no-cache registry and fresh-cache npm
readback then reported `beta=latest=0.8.0`, no `alpha`, with registry
modification time `2026-08-07T07:53:28.115Z`.

An unqualified global installation under Node.js `22.22.3` and an independent
fresh-cache isolated installation both resolved to `perttool 0.8.0`. The
isolated package exposed 45 commands and 21 root schemas. The complete
operation and no-write boundary are recorded in
[`0.8.0-latest-promotion.md`](0.8.0-latest-promotion.md).

Public VSIX identity or publication, both input-plan advances, release-plan
advance, Issue mutation, and unrelated backlogs remain separate decisions.
