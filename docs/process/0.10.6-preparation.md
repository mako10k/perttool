# `v0.10.6` source preparation

Date: 2026-08-28

Status: accepted source preparation; immutable candidate and publication remain pending

## Prepared source

The source identifies `perttool@0.10.6` from exact published `v0.10.5` base
`7379870db2ec000243f02cda6d86af514af7feef`. The Issue #36 mechanism correction
is isolated in commit `56b7142fe4a6dbffa43847d73ba75854a4c696d2` so the same
source change can later be applied to the preserved `0.11.0` workstream without
the `0.10.6` release identity.

Package, lockfile, CLI, private LSP peer, private MCP peer/server, CHANGELOG,
README, version-bound fixtures, golden projections, release tests, and package
workflow identify `0.10.6`. Grammar 8, CLI Contract 9, 56 commands, 23 root
schemas, 129 root and Node runtime exports, 45 Core runtime exports, result
identities, and package dependencies remain unchanged.

ADR 0003 retains the existing main-only publication gate as the default and
adds a fail-closed older-version hotfix route. That route requires the exact
local and remote release branch, peeled annotated tag, release `HEAD`, and an
approved unchanged `origin/main` commit to agree without force-pushing or
including future-work bytes.

## Complete gate

The clean dependency installation added 337 packages, audited 341 packages,
reported zero vulnerabilities, and built every workspace under Node.js 22.
`TMPDIR=/tmp npm run check` then passed:

- type checking for the root, LSP, VSIX, and MCP workspaces;
- pinned jscpd 5.0.15 duplication ratchet: 146 clones, 2,731 lines, 3.047%;
- pinned Lizard 1.23.0 complexity ratchet: 4,090 functions and 168 retained
  legacy entries;
- 1,235 of 1,235 repository tests with zero failures, cancellations, skips,
  or todos;
- the English baseline over 1,106 text files with three allowlisted lines;
- documentation checks over 342 Markdown files and seven PERT examples;
- read-only check, analysis, and Next self-use over 44 plans;
- isolated LSP and MCP package acceptance;
- VS Code 1.101.0 trusted/untrusted installation, host, replacement, and
  uninstall acceptance;
- temporary npm link acceptance for `perttool 0.10.6`; and
- the 876-file isolated public package, Contract 9 file-first and
  plan-assurance compatibility workflows, and npm `beta` publish dry-run.

`git diff --check`, focused Issue #36 tests, version-bound golden digest
recalculation, shell syntax, English, and documentation checks also passed.

## Claim, evidence, and next action

- `CLM-0106-PREP-001`: the prepared source is a compatible Issue #36 patch and
  not a partial `0.11.0` release.
  - `EVD-0106-PREP-001`: public identity counts and the complete repository and
    installed-package gates remain unchanged apart from version identity and
    the accepted correction.
  - `ACT-0106-PREP-001`: freeze one clean source commit and generate one
    immutable tarball outside the worktree.
- `CLM-0106-PREP-002`: the off-main route preserves the future-work main line
  without bypassing publication checks.
  - `EVD-0106-PREP-002`: the main-only default remains; explicit hotfix mode
    additionally validates the local branch, remote branch, and expected main
    commit before the existing tag, npm identity, unused-version, `beta`, and
    unchanged-`latest` gates.
  - `ACT-0106-PREP-002`: bind the exact candidate approval to the remote branch
    and fresh `origin/main` commit before any external write.

No Git remote, tag, GitHub Release, npm package, dist-tag, Issue, public VSIX,
plan advance, local global installation, or preserved `0.11.0` state was
changed by source preparation.
