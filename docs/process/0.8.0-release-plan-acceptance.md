# `v0.8.0` Release Plan Acceptance

- Document status: Accepted 1.0
- Acceptance date: 2026-08-07
- Target version: `0.8.0`
- Release plan: [`release-0.8.0.pert`](../../plans/release-0.8.0.pert)
- Accepted source digest: `sha256:a26bb205f5eb4ee4bb6616bee2d74877c3162f26be090473d435a4515fadc69c`

## 1. Selected release boundary

The plan selects suffix-free `0.8.0` as the additive public-package boundary
after published `0.7.1`. Its accepted inputs are the retained pre-advance
`ADAPTER-001` and `HIST-DAG-001` workstreams plus the accepted `DECL-ID-001`
correction. The release gate must prove the source transition from 44 to 45
commands, 20 to 21 root schemas, and 121 to 122 root runtime exports while
retaining Grammar 6 and CLI Contract 7 semantics.

The private LSP, VSIX, and MCP workspaces remain excluded from the public npm
package. Public VSIX identity, Marketplace and Open VSX publication, npm
`latest` promotion, plan advance, and Issue mutation remain separate.

## 2. Preview-first creation and authority

Contract 7 `project init` first created the exact 489-byte smallest Grammar 6
document at `plans/release-0.8.0.pert`. Its digest was
`sha256:b2f1ee3aaaba2b03e7bd6b49bcdd33d1c86b3142aad7886548b63c39eae55435`.
The document declared goal and DAG owner `user` and ended at the reached
planning milestone.

An assertion-free preview then composed one 18-mutation atomic batch. It
affected goal and DAG scopes, required owner `user`, changed 100 lines and
removed 9 lines, and produced the exact 6,734-byte candidate with digest
`sha256:a26bb205f5eb4ee4bb6616bee2d74877c3162f26be090473d435a4515fadc69c`.
The user separately confirmed that displayed candidate. One persistent write
used actor `codex`, the candidate-bound `accepted-by-owner user` assertion,
and the exact original digest. GovernanceDecision v2 authorized both scopes,
the write succeeded once, and readback matched the candidate digest. The
assertion is not reusable for any later task or mutation.

## 3. Accepted plan structure

The six serial tasks total 22p:

1. `RELEASE_080_GATE_DESIGN` fixes the version, public compatibility, and
   exclusion boundary.
2. `RELEASE_080_INPUT_READINESS` verifies the three accepted release inputs.
3. `RELEASE_080_PREPARATION` aligns version-bearing source and passes local
   repository and installed-package gates.
4. `RELEASE_080_CANDIDATE` accepts one immutable tarball after clean-source,
   availability, route, and package verification.
5. `RELEASE_080_PUBLISH` requires a later exact-candidate and external-batch
   authorization before any push, tag, GitHub, or npm write.
6. `RELEASE_080_ACCEPTANCE` independently verifies durable public identity and
   behavior without changing npm `latest` or publishing a VSIX.

Five resources serialize engineering, review, release-document, package, and
external-release ownership. Both precedence and `parallel-sgs` version 1
resource makespans are exactly 22p with zero resource delay. The declared
velocity is `29p/2d`.

## 4. Readback and remaining boundary

Document check reports Grammar 6, five resources, seven milestones, six tasks,
no gate declarations, and no diagnostic. Project metadata reports finish
`RELEASE_080_ACCEPTED` and effective goal and DAG owner `user`. Complete
NextResult v6 has no safe-stop reason or required action and recommends and
makes startable only `RELEASE_080_GATE_DESIGN`.

This acceptance does not start or complete that task. It changes no package
version, Git object, ref, index entry, remote, tag, GitHub Release, npm state,
Marketplace or Open VSX state, Issue, or existing plan advance state. Release
gate design and every later task remain separate execution boundaries.

## 5. Repository verification

The accepted plan and its self-use registration passed the following local
checks without changing the accepted plan bytes:

- all 972 Node.js tests;
- all 36 self-use plans under `document check`, `dag analyze`, and `dag next`;
- the English baseline over 788 text files with three exact allowlisted files;
- the documentation gate over 216 Markdown files and seven PERT documents;
- `git diff --check`.

These checks accept plan creation and repository registration only. They do
not establish `RELEASE_080_GATE_DESIGN`, release readiness, package-candidate
identity, or publication authority.
