# `v0.11.0` Release Gate Design

- Document status: Candidate 1.4; owner acceptance pending
- Date: 2026-08-31
- Task: `POOL_RELEASE_GATE_DESIGN`
- Release effect: none
- Candidate source branch: `codex/planning-pool-0.11-wip`
- Issue #36 integration commit:
  `eb6e59da5f058229fa5e2603ce86329821b4e45e`
- Precondition: independently accepted `POOL_GRAMMAR9_ACCEPTANCE_MUTATION`

## 1. Proposed boundary

The candidate selects suffix-free `0.11.0` for the breaking Grammar 9 and CLI
Contract 10 Planning Pool boundary and binds Requirements item 26, ADR 0003,
Post-MVP Slice 8A, the release procedure, the existing release-readiness plan,
and one focused design test.

| Surface | Published rollback `0.10.6` | Target `0.11.0` |
| --- | ---: | ---: |
| Grammar | 8 | 9 |
| CLI Contract | 9 | 10 |
| Commands | 56 | 71 |
| Root schemas | 23 | 29 |
| Root and Node exports | 129 | 139 |
| Core exports | 45 | 51 |
| Advance result | v3 | v4 |
| Unit-migration result | v4 | v5 |

The gate retains older grammar reads and every existing governance, plan-
assurance, milestone-acceptance, history-safety, digest, race, and atomic-
persistence guard. It excludes package version preparation, candidate
commitment, push, tag, GitHub Release, npm publication, `latest`, public VSIX,
Issue closure, plan advance, Issue #24 Goal Coverage and Goal Seal, and the
separate affected-test selector.

## 2. Direct evidence

- `E-011-GATE-001`: the accepted Planning Pool release-integration record
  composes Issues #25 through #34 and directly fixes Grammar 9, CLI Contract
  10, 71 commands, 29 root schemas, 139 root and Node exports, and 51 Core
  exports. The release line must also retain the exact Issue #36 correction;
  rollback coverage alone is insufficient.
- `E-011-GATE-002`: the Planning Pool contract defines the only automatic
  Grammar 8 to 9 entry as an additive version-and-owned-trivia edit, keeps
  Grammar 1 through 8 readable, and inserts no planning declaration.
- `E-011-GATE-003`: npm readback on 2026-08-28 reported `beta=0.10.6` and
  `latest=0.10.5`; npm `0.11.0`, remote tag `v0.11.0`, and the matching GitHub
  Release were unused. Candidate acceptance must repeat these drift-prone
  reads.
- `E-011-HISTORY-001`: correction commit
  `56b7142fe4a6dbffa43847d73ba75854a4c696d2` from the published `0.10.6` line
  and integration commit `eb6e59da5f058229fa5e2603ce86329821b4e45e`
  have stable patch ID `84fe584b8a2895187bcf72df2af289103b49ca88`
  and identical blobs for all four changed files.
- `E-011-HISTORY-002`: the integrated correction deletes the independent
  milestone-acceptance governance regular expression and consumes the shared
  validated canonical governance projection. At the integration snapshot, its
  18 Issue #36 cases, 55 milestone-acceptance cases, and 1,331-test repository
  gate passed without weakening missing/wrong actor, stale-digest,
  invalid-source, or safe-write denial.
- `E-011-COMPOSE-001`: independent review of Candidate 1.0 reproduced a valid
  Grammar 9 milestone criterion replacement returning exit 1 with no
  candidate. The Contract 10 outer composition did not remove and reintegrate
  Planning Pool declarations before invoking the older acceptance planner.
- `E-011-COMPOSE-002`: independent review rejected Candidate 1.1 because its
  single longest-common-prefix/suffix range could span masked Planning Pool
  declarations when an existing acceptance record preceded them. Independent
  review then rejected Candidate 1.2 because governance-denied persistence
  returned a valid inner candidate with `ok=false`, and the outer lift treated
  that result as candidate-less. Candidate 1.3 carries exact deletion and
  append edits through both extension layers whenever a candidate exists,
  retains denial and no-write state independently, rejects an edit touching a
  masked source line, and covers acceptance records before, between, and after
  Planning Pool declarations. The exact Candidate 1.3 complete gate passed
  1,336 tests, English and documentation checks, 46 self-use plans, isolated
  LSP/MCP/VSIX and supported-host checks, temporary linking, and the 1,019-file
  installed-package workflow. Independent exact-byte review then found two
  evidence findings and three lifecycle or wording findings. Candidate 1.4
  corrects those findings by validating the denied installed result against
  the packed schema catalog, asserting its exact mutation content and
  identity, and correcting compatibility, causal, design-selection, and ADR
  lifecycle wording. The exact Candidate 1.4 complete gate passed 1,336 tests,
  English and documentation checks, 46 self-use plans, isolated LSP/MCP/VSIX
  and supported-host checks, temporary linking, and the 1,019-file installed-
  package workflow. Independent exact-byte re-review remains pending before
  owner acceptance.
- `E-011-PLAN-001`: the current release-readiness plan is valid and places
  `POOL_GRAMMAR9_ACCEPTANCE_MUTATION` before `POOL_RELEASE_GATE_DESIGN`. The
  correction task is structurally complete but its Outcome is missing, so
  assurance requires `restore_assurance_evidence`; the gate is ready and
  recommended but start authority is withheld. Preparation, candidate,
  PUBLISH, and acceptance remain precedence-blocked.

## 3. Claim, evidence, and action trace

- `CLM-011-GATE-001 high`: `0.11.0` is the smallest truthful version for the
  accepted public boundary. `0.10.7` would understate its breaking grammar,
  CLI contract, command, result, schema, migration, and authority changes.
  Evidence: `E-011-GATE-001` and `E-011-GATE-002`.
  - `ACT-011-GATE-001 proposed`: accept the exact boundary in ADR 0003,
    Requirements item 26, Post-MVP Slice 8A, and the release procedure.
- `CLM-011-COMPAT-001 high`: exact `0.10.6` is the correct rollback pin, but
  rollback does not downgrade a source that already contains Grammar 9
  declarations. Evidence: `E-011-GATE-002` and `E-011-GATE-003`.
  - `ACT-011-COMPAT-001 proposed`: prepare explicit migration and rollback
    guidance and verify both exact installed versions before candidate
    acceptance.
- `CLM-011-HISTORY-001 high`: the 0.11 line contains the defect-source
  correction rather than only an added check, while release history remains
  separable from old-line version artifacts. Evidence: `E-011-HISTORY-001`
  and `E-011-HISTORY-002`.
  - `ACT-011-HISTORY-001 executed locally`: retain the correction-only patch,
    `0.10.6` changelog entry, and accepted off-main policy history; do not copy
    the old package identity, generated goldens, or candidate lifecycle.
- `CLM-011-COMPOSE-001 high`: Grammar 9 retains the existing milestone-
  acceptance mutation capability only when Contract 10 composes both extension
  layers and projects the outer document binding. Evidence:
  `E-011-COMPOSE-001` and `E-011-COMPOSE-002`.
  - `ACT-011-COMPOSE-001 complete locally; independent acceptance pending`:
    the correction task and full local and installed-package gate passed;
    require independent exact-byte review before this gate can be accepted.
- `CLM-011-AUTH-001 high`: gate design and preparation do not authorize any
  external release write, and accepting this gate does not authorize source
  preparation or candidate commitment. Evidence: `E-011-PLAN-001`.
  - `ACT-011-AUTH-001 prohibited`: do not push, tag, create a GitHub Release,
    publish npm, move a distribution tag, close an Issue, or advance a plan
    without the separately named gate and authority.

## 4. Root-cause and escape boundary

The Issue #36 root cause was the milestone-acceptance mutation layer's second,
regex-based interpretation of `dag_delegates`, which diverged from the
canonical validated governance model for bracketed lists. The integrated
source removes that duplicate interpretation and reuses the canonical model.
The focused regression is recurrence prevention and escape containment; it is
not presented as the source-cause correction.

The Candidate 1.0 composition defect had a different root cause: Contract 10
added the Planning Pool grammar layer, but `runMilestoneAcceptance` still
composed only through the Contract 9 temporal layer. A valid Grammar 9 source
therefore reached a planner whose accepted input ended at the older grammar
instead of being projected to that input and lifted back. The missing Grammar
9 by milestone-acceptance cross-case explains escape; it did not create the
incorrect composition.

Candidate 1.1 then introduced a separate correction defect. Its outer lift
assumed the lowered and updated documents differed in one continuous range.
When an acceptance record was before a masked Planning Pool declaration, that
range included both the owned acceptance edit and unrelated Pool bytes. The
result stayed syntactically valid, so final validation did not expose the data
loss. Candidate 1.2 removes that assumption by carrying normalized owned edits
and failing closed if an edit touches any lowered source line. The placement
matrix and reordered installed-package replay address the escape boundary.

Candidate 1.2 retained one further conflation: `ok=false` meant both “no valid
candidate” and “a valid candidate exists but persistence authority is denied.”
The early return therefore exposed the lowered Grammar 7 candidate for an
unauthorized Grammar 9 write, even though the source file remained unchanged.
Candidate 1.3 uses candidate presence as the lift condition, preserves
`PTGOV-101` and `written=false` independently, and adds CLI plus installed-
package denial replay with exact candidate reconstruction.

## 5. Candidate validity and pending acceptance

The proposed Outcome becomes semantically valid only after the correction
precondition passes because it then packages an already accepted complete
Planning Pool surface, carries the published Issue #36 source-cause correction
exactly, retains milestone-acceptance mutation, defines a truthful migration
and rollback boundary, and leaves every later mutation separately gated.
Before owner acceptance, a fresh independent reviewer must inspect the exact
candidate bytes, complete gate, PERT frontier, and authority exclusions and
report all P0 through P3 findings.

No gate-design task finish, gate milestone criterion, receipt, assurance
outcome, package version, retained tarball, or external write is created by
Candidate 1.4.
