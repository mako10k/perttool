# `v0.4.0` Contract 5 Readiness

- Document status: Accepted 1.0
- Acceptance date: 2026-07-27
- Target version: `0.4.0`
- Release procedure: [0.4.0-release.md](0.4.0-release.md)
- Release plan: [../../plans/release-0.4.0.pert](../../plans/release-0.4.0.pert)
- Implementation plan: [../../plans/governance.pert](../../plans/governance.pert)
- Implementation acceptance:
  [governance-acceptance.md](governance-acceptance.md)
- Design acceptance:
  [governance-design-acceptance.md](governance-design-acceptance.md)

## Decision

Accept the owner-aware governance finish as the complete input for `v0.4.0`
release-source preparation. The governance plan has reached and advanced
`GOVERNANCE_ACCEPTED`; its precedence and heuristic resource makespans are
zero, and its complete NextResult v4 has no recommended or startable task.
The implementation and design acceptance records retain no unresolved
acceptance finding.

The repository source and a package built from that source expose one atomic
Contract 5 boundary: Grammar 1/2/3/4, declared and effective governance
metadata, ProjectResult v3, MutationResult v2 with GovernanceDecision v1,
governed direct/batch/advance planning, authorization-before-safe-write
enforcement, the 28-command registry, text and JSON help, Guide, README,
public root exports, and the installed file-first workflow. Grammar 1/2/3
documents retain effective owner `user`, empty delegates, and source
compatibility.

A read-only review of GitHub Issue #4 on 2026-07-27 found it open with its
accepted goal, threat model, criteria, and non-goals intact and no comments or
labels adding a release-blocking finding. The issue remains open deliberately;
this readiness decision does not close or mutate it.

There are no open Contract 5 readiness findings. This decision starts only
`RELEASE_040_PREPARATION`. It does not change version-bearing source, push
Git, create or push `v0.4.0`, create a GitHub Release, publish npm, promote npm
`latest`, close Issue #4, or complete candidate, PUBLISH, or post-publication
acceptance work.

## Gate trace

| Release readiness condition | Accepted evidence |
| --- | --- |
| Reached governance finish | `plans/governance.pert` retains reached `GOVERNANCE_ACCEPTED`; check, analyze, and Next v4 return zero work |
| Accepted boundary and no hidden finding | The implementation acceptance records `GOV-AC-001` through `GOV-AC-010`; the design acceptance has no open finding; read-only Issue #4 review found no later comment or label |
| Atomic public Contract 5 cutover | Acceptance and focused tests cover Grammar 4, public schemas and root names, registry/help/Guide, generated warning, README, and the isolated installed-package workflow |
| Legacy source compatibility | Public metadata checks cover Grammar 1/2/3 effective owners `user`, empty delegates, and unchanged source |
| Fail-closed write composition | Core, CLI, batch, advance, and safe-write tests cover preview, missing/wrong confirmation, authorized persistence, invalid assertions, pre-change authority, and stale-digest rejection |
| Explicit non-goals | Requirements, specifications, examples, README, Guide, and acceptance retain caller assertions rather than authentication and exclude durable audit, direct-edit prevention, recommendation override apply, Git integration, and Issue synchronization |

## Verification

The readiness snapshot passed under Node.js 22 or later:

```sh
npm run build --silent
node --test test/release-0.4.0-readiness.test.mjs \
  test/governance-design-acceptance.test.mjs \
  test/governance-interface-contract.test.mjs
npm run check
git diff --check
```

The complete check covers 585 tests, 77 Markdown documents, all nineteen
self-use plans, local-link validation, publish normalization dry-run, and the
isolated installed-package Contract 5 file-first workflow. Package identity
remains `0.3.0`; the coherent `0.4.0` version, changelog, and migration update
belong to `RELEASE_040_PREPARATION`.
