# `v0.7.1` Release Candidate Acceptance

- Document status: Accepted 1.0
- Review date: 2026-08-05
- Target version: `0.7.1`
- Candidate source commit: `a05b769a65dcebbf3b543538ba4930e065f3e867`
- Candidate source tree: `36fc16a1e79036930b9e615b71eb5042eeba3d47`
- Completed-plan source digest: `sha256:986e4200fa452535534ad11f31008de62824cfed70c3665cb35ba1c568610a10`
- Release plan: [`release-0.7.1.pert`](../../plans/release-0.7.1.pert)
- Release procedure: [`0.7.1-release.md`](0.7.1-release.md)

## 1. Acceptance boundary

This record accepts one immutable local `perttool@0.7.1` tarball for the
compatible Help and Guide consistency patch. It fixes the exact source commit,
package identity, external preflight, artifact bytes, and installed behavior
that may be used by a separately authorized PUBLISH batch.

It does not authorize or perform a Git push, tag creation, GitHub Release,
npm publication, npm dist-tag mutation, durable public acceptance, plan
advance, or Issue mutation. A complete NextResult v6 recommendation is
sequencing evidence and does not replace the required external-write
instruction.

## 2. Clean source and repository gate

Commit `a05b769a65dcebbf3b543538ba4930e065f3e867` was clean and identified the
package, both lockfile roots, and CLI as `0.7.1`. Its committed tree was
`36fc16a1e79036930b9e615b71eb5042eeba3d47`.

Under Node.js 22.22.3, a clean `npm ci`, zero-finding `npm audit`, all 797
tests, the 619-file English baseline with three allowlisted lines, 165
Markdown files and seven normative PERT examples, all 33 self-use plans,
temporary-link validation, the 601-file isolated package workflow, and
`git diff --check` passed.

## 3. Read-only external and route preflight

Direct reads found no local or remote `v0.7.1`, no GitHub Release `v0.7.1`,
and no npm `perttool@0.7.1`. npm reported `beta=latest=0.7.0`, no maintained
`alpha`, and registry metadata modification time
`2026-08-04T10:12:16.615Z`. `origin/main` remained
`28f439e325313553f3dd4936c83c52ba834ab602`.

The first protected Git dry-run used the workstation's unrelated active
GitHub account and was rejected with HTTP 403 without writing. The route was
then narrowed to the process-scoped `GH_TOKEN_MAKO10K` secret, renamed only
for that process to `GH_TOKEN`; GitHub identity resolved as `mako10k`, and the
protected dry-run projected `28f439e..a05b769` to `main` successfully without
writing. The process-scoped npm route also resolved as `mako10k`. No
credential value was displayed and no external state changed.

## 4. Immutable candidate identity

The fixed-path candidate was generated once from the accepted source commit:

| Property | Exact value |
| --- | --- |
| Path | `/home/katsumata-m/releases/perttool/0.7.1/candidate/perttool-0.7.1.tgz` |
| Modification time | `2026-08-05 11:17:53.916600212 +0900` |
| Packed size | `660003` bytes |
| Unpacked file bytes | `3849741` bytes |
| Files | `601` |
| SHA-1 | `730c5af88c74aed69be956c0613f94e74abfd05c` |
| SHA-256 | `5bf4723131b79f04b501ae02b7585c800ac8dc37f0f3a816eff837d670e4454c` |
| npm integrity | `sha512-pivhC5K3QkA5JOhtycVu9+2An/a7RJ5u/RBG+hJ7la9Wr0JhdFXeB4KhMsAmGGtgYLxm3pEMYwTLr9zWiNJfIQ==` |

The archive manifest identifies public `perttool@0.7.1`, CLI
`dist/cli.js`, and npm tag `beta`. Direct inspection also confirms that the
bundled README uses durable `0.7.1` exact-pin guidance, leaves independently
managed `latest` at `0.7.0`, and retains `0.7.0` as the rollback pin.

## 5. Candidate-specific package acceptance

Those exact retained bytes passed `scripts/publish-npm.sh --dry-run`, isolated
installation, the complete Contract 7 file-first workflow, and the installed
plan-assurance workflow under Node.js 22.22.3. The retained archive was then
independently re-read for its file count, sizes, SHA-1, SHA-256, npm integrity,
manifest identity, and bundled README boundary without rewriting it.

After adding this repository-only acceptance record and the completed task
state, the complete Node.js 22.22.3 gate passed again: 797 tests, 620 English
baseline files, 166 Markdown files, seven normative PERT examples, all 33
self-use plans, temporary-link validation, and isolated-package validation.
Its disposable package again reported SHA-1
`730c5af88c74aed69be956c0613f94e74abfd05c`; the changed record, plan, shared
policy, and tests are excluded from the public package inventory.

`RELEASE_071_CANDIDATE` is complete. Complete, non-truncated NextResult v6
recommends only `RELEASE_071_PUBLISH`, but the release procedure still
requires the user to authorize this exact candidate and external batch before
any push, tag, GitHub Release, or npm publication.
