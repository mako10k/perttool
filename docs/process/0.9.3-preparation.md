# `v0.9.3` Source Preparation

- Document status: Accepted 1.0
- Date: 2026-08-14
- Task: `RELEASE_093_PREPARATION`
- Target version: `0.9.3`

## 1. Version and compatibility alignment

The package, lockfile, CLI, MCP server, and private LSP and MCP peer identities
are aligned to `0.9.3`. CHANGELOG, README, requirements, design, repository
guidance, current-version tests, goldens, and the forty-first self-use plan are
aligned without changing Grammar 7, CLI Contract 8, public result identities,
commands, schemas, or runtime export counts.

## 2. Repository evidence

Node.js `22.22.3` reports zero dependency vulnerabilities. The complete test
run passes all 1,056 tests, including the three emergency regressions, prior
advance corrections, history safety, assurance, governance, adapters, and
safe writes. The English baseline checks 884 text files and preserves its exact
three-line allowlist. All 41 self-use plans remain inputs to document check,
analysis, and NextResult v7 selection.

Static analysis uses the exact separately retained gate versions and ratchets:

- jscpd `5.0.15`: 148 clones, 2,746 duplicated lines, 15,252 duplicated
  tokens, and 3.36% duplicated lines, within the 148/2,746/15,252/3.37 limits;
- lizard `1.23.0`: no new threshold violation at CCN 15, length 100, or six
  parameters; `planMilestoneAcceptanceAdvance` is CCN 14 and 37 lines,
  `composeProvisionalBase` is CCN 11 and 57 lines, and the Contract 8 assurance
  wrapper is CCN 6 and 37 lines.

## 3. Package and gate result

The complete repository command passed documentation over 266 Markdown files
and seven PERT examples, all 41 self-use plans, isolated LSP and MCP packages,
the supported VS Code 1.101.0 host, temporary npm link, and the 712-file public
package. The package is 800.5 kB packed and 4.6 MB unpacked. Its Contract 8
file-first and Grammar 7 plan-assurance installed workflows passed, and npm
publish dry-run selected only `beta`.

No remote, registry, tag, Release, or Issue mutation occurred in this task.
