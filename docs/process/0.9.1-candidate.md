# `v0.9.1` Release Candidate Acceptance

- Document status: Accepted 1.0
- Review date: 2026-08-13
- Target version: `0.9.1`
- Candidate source commit: `b8dba7e3413cba172bb1c2463cff5e63fe84e28e`
- Candidate source tree: `b5c45f7eb2c03b8e39c837a295d0ecd8b41c3b19`
- Completed-plan source digest: `sha256:4ecbc823afe5fa76d9365fa24beacb7983806884285f195dd6aea88114035363`
- Release plan: [`release-0.9.1.pert`](../../plans/release-0.9.1.pert)
- Release procedure: [`0.9.1-release.md`](0.9.1-release.md)

## 1. Acceptance boundary

This record accepts one immutable local `perttool@0.9.1` tarball for the
compatible Grammar 7 and CLI Contract 8 current-velocity observation patch.
It fixes the source commit, public-package identity, read-only external
baseline, artifact bytes, and installed behavior for the authorized ordered
PUBLISH batch.

It does not perform a Git push, tag creation or push, GitHub Release, npm
publication, npm dist-tag mutation, durable public acceptance, plan advance,
or Issue mutation. Candidate acceptance and complete NextResult v7 sequencing
do not permit publication before the frozen external mutation order.

## 2. Clean source and repository gate

Commit `b8dba7e3413cba172bb1c2463cff5e63fe84e28e` was clean and identified the
package, both lockfile roots, CLI, private adapter peer versions, and MCP
server as `0.9.1`. Its committed tree was
`b5c45f7eb2c03b8e39c837a295d0ecd8b41c3b19`.

From that clean commit, the complete Node.js 22.22.3 `npm run check` and `git
diff --check` passed. The gate included all 1,045 tests, the 866-file English
baseline with three exact allowlisted lines, 254 Markdown files and seven
normative PERT examples, all 39 self-use plans, isolated LSP and MCP
acceptance, the private VSIX supported-host gate, temporary linking, and the
713-file isolated public-package gate. The preceding dependency audit
reported zero vulnerabilities.

## 3. Read-only external and route preflight

Direct reads found no local or remote `v0.9.1`, no GitHub Release `v0.9.1`,
and no npm `perttool@0.9.1`. npm reported `beta=latest=0.9.0` and no `alpha`.
Issue #8 remained open with labels `bug` and `priority:P0` and no comment.
`origin/main` remained
`3aca4f0d863407d5f2abd9a09741dae85731c0b0`.

The process-scoped GitHub and npm identities both resolved as `mako10k`
without displaying credential values. The authenticated `secdat` Git dry-run
projected a fast-forward of `origin/main` through the candidate source commit
without writing. GitHub's branch-protection endpoint returned HTTP 404 for
`main`; therefore this record makes no branch-protection claim. No external
state changed.

## 4. Immutable candidate identity

The fixed-path candidate was generated once from the accepted source commit
and then made read-only:

| Property | Exact value |
| --- | --- |
| Path | `/home/katsumata-m/releases/perttool/0.9.1/candidate/perttool-0.9.1.tgz` |
| Modification time | `2026-08-13 17:50:24.586711979 +0900` |
| Mode | `0444` |
| Packed size | `2808175` bytes |
| Unpacked file bytes | `6634333` bytes |
| Files | `713` |
| SHA-1 | `0a36b0e5d696a0dfcef5904dae419a4da9fd9ef6` |
| SHA-256 | `27b4d8d60718675961c913771cb319043df8daf8c50b81b22cb70eb79f3e77bf` |
| npm integrity | `sha512-62XXV/+FaFOgYJcxsyazrnyl/y2+Q2Uv/MNQodRGrw8mozeWvlUgPTv2mDOBw7M8IIEZwg0BH3bj03CzvouZAg==` |

The archive manifest identifies public `perttool@0.9.1`, CLI
`dist/cli.js`, the root, `perttool/core`, `perttool/node`, and schema exports,
and npm tag `beta`. It contains 23 root schemas, exposes no private adapter
workspace, and includes no `adapters/` path.

## 5. Candidate-specific package acceptance

Those exact retained bytes passed `scripts/publish-npm.sh --dry-run` for npm
`beta` and the complete explicit-archive `scripts/check-package.sh` workflow
under Node.js 22.22.3. The installed package reported `perttool 0.9.1`,
Contract 8 file-first behavior, plan-assurance compatibility, 53 commands, 23
schemas, 129 reference-identical root and Node exports, and 45 Core exports.

The retained candidate SHA-256 remained
`27b4d8d60718675961c913771cb319043df8daf8c50b81b22cb70eb79f3e77bf`
after archive inspection and installed-package verification.

After adding this repository-only acceptance record, completed task state,
shared policy, focused test, and self-use golden update, the complete Node.js
22.22.3 gate passed again: 1,045 tests, 867 English baseline files, 255
Markdown files, seven PERT examples, all 39 self-use plans, every adapter and
temporary-link gate, and the isolated public package. Its disposable package
had the same SHA-1 `0a36b0e5d696a0dfcef5904dae419a4da9fd9ef6`; the retained candidate was not
rewritten.

`RELEASE_091_CANDIDATE` is complete at plan source digest
`sha256:4ecbc823afe5fa76d9365fa24beacb7983806884285f195dd6aea88114035363`.
Complete, non-truncated NextResult v7 recommends and makes startable only
`RELEASE_091_PUBLISH`. The authorized PUBLISH batch remains bounded to the
frozen candidate, release commit, target refs, GitHub prerelease and checksum
asset, one npm `beta` publication, and post-write readback; npm `latest` and
plan advance remain excluded.
