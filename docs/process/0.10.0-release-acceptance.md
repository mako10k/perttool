# `v0.10.0` Durable Release Acceptance

- Document status: Accepted 1.0
- Acceptance date: 2026-08-18
- Recovery verification date: 2026-08-21
- Target version: `0.10.0`
- Task: `RELEASE_0100_ACCEPTANCE`
- Release plan: [`release-0.10.0.pert`](../../plans/release-0.10.0.pert)
- Release commit and peeled tag:
  `f0e3d7110171686c085d5de0b595057fc803dff8`
- Artifact SHA-256:
  `b98dad654955b639275ebcccd1871a3a40ce415cf0c5504dd18c681dfa36ce9f`

## 1. Durable public identity

Independent release-time reads confirmed remote `main`, annotated tag object
`b446f718eb522955d330ba1f9151ed3e333aba3f`, the peeled `v0.10.0` tag,
successful Node.js 22 and Node.js 24 CI run `32104943472`, GitHub prerelease
`372121211`, npm version metadata, registry integrity, and dist-tags. The
GitHub asset is 3,082,951 bytes and reports the accepted SHA-256. The npm
tarball was byte-identical to the retained 873-file candidate and had the same
SHA-256, npm SHA-1 `083ec224416243c1b71fbdcfa00a7d2cd8648c1f`, and published
integrity.

The first Node.js 24 attempt completed the unchanged repository checks but
timed out in the external VS Code 1.101.0 host probe. Exactly one failed-job
rerun of the unchanged release commit succeeded. The final run attempt 2
reports both Node.js 22 and Node.js 24 successful.

npm records one `0.10.0` publication at `2026-08-18T06:09:39.491Z`. At
publication it reported `beta=0.10.0`, publication-time `latest=0.9.0`, and no
`alpha`. Publication was not retried, and acceptance performed no dist-tag
mutation. Recovery verification on 2026-08-21 reconfirmed the immutable npm
version, SHA-1, integrity, publication time, local annotated tag, and peeled
release commit. Current dist-tags have since advanced to
`beta=latest=0.10.5`; they do not replace the publication-time evidence.

## 2. Installed public behavior and compatibility

The registry tarball passed the complete isolated package gate under Node.js
22, including publication dry-run, Contract 9 file-first behavior, and
plan-assurance compatibility. Fresh-cache isolated exact and beta
installations both resolved to `perttool 0.10.0`, exposed 56 commands and 23
root schemas, and passed representative Grammar 1 through 8 document checks.
Both installations also passed temporal-plan analysis and Next projection
over the accepted Grammar 8 source.

The package therefore retains Grammar 1 through 7 reads while activating
Grammar 8 and CLI Contract 9. The accepted package inventory and isolated
package gate also retain the 129-name root and Node facades, 45-name Core
facade, and exclusion of the private LSP, VSIX, and MCP workspaces.

A separate fresh-cache installation of the exact rollback pin resolved to
`perttool 0.9.4` with its expected 53 commands and 23 root schemas. No
rollback, consumer, or global-install mutation was performed.

## 3. Boundary retained after acceptance

The accepted artifact is the additive temporal-scheduling beta described by
the release gate and candidate records. The later npm `latest` promotion is a
separate operation recorded in
[`0.10.0-latest-promotion.md`](0.10.0-latest-promotion.md). Public VSIX
publication, Issue mutation, release-plan advance, unrelated plan advance,
and additional remote publication remain separate.
