# `v0.7.1` Help and Guide Consistency Patch Self-Review

- Document status: Accepted 1.0
- Review date: 2026-08-05
- Task: `RELEASE_071_SELF_REVIEW`
- Workstream: `GUIDE-CONSISTENCY-001`
- Release plan: [`release-0.7.1.pert`](../../plans/release-0.7.1.pert)
- Completed-plan source digest: `sha256:ea2d35fee254d4ca34e0810f3020f69d5a31776251d8fc7f7304c80efbc70a34`
- Target version: `0.7.1`
- External mutations: none

## 1. Decision

Accept `0.7.1` as the compatible beta patch version for publishing the
accepted Help and Guide consistency correction. The change is small enough to
require no additional soak gate, but it is not documentation-only: installed
Guide text and related navigation, command Help examples, and diagnostic
`help_topic` values change at runtime.

No public interface identity changes. Grammar 6, CLI Contract 7, command and
option identities, result and schema identities, payload structure,
package-root exports, governance semantics, safe-write behavior, and
recommendation, temporal, and plan-assurance authority remain unchanged.

Leaving the correction unreleased would strand the runtime fixes in the source
checkout. `0.7.1` accurately represents a backward-compatible correction;
`0.8.0` would imply a broader feature or interface boundary that is absent.

## 2. Accepted input and complete scope

The implementation authority is the accepted
[`GUIDE-CONSISTENCY-001` record](help-guide-consistency-acceptance.md), whose
seven HGC cases cover:

- exact Grammar 6, AnalysisResult v5, NextResult v6, and combined authority
  Guide meaning without unrestricted semantic prose replacement;
- argument-valid examples for all 44 registered commands and complete inputs
  for all eight assurance mutations;
- repository-wide literal diagnostic-link closure;
- current-versus-historical labels across README and normative surfaces;
- specified and directly tested `PTCNV-210`; and
- bounded reciprocal navigation for `plan-assurance`.

The `0.7.1` release adds no behavior outside this accepted correction and its
version, changelog, package, and release evidence.

## 3. Direct compatibility comparison

A Node.js 22 comparison installed the published exact baseline
`perttool@0.7.0` into an isolated temporary prefix and compared it with the
current built source. The comparison excluded only the intended human-facing
example and Guide content values and confirmed identical structural surfaces:

| Surface | Result |
| --- | --- |
| Package-root export names | 121, exact set equal |
| Command descriptors | 44, paths, operations, operands, options, input/output, effects, schemas, exits, and example IDs equal |
| Root schema catalog | 20, exact catalog equal |
| Guide result identity | `Perttool.GuideResult.v1`, unchanged |
| CLI contract | 7, unchanged |
| Guide topic IDs | 10, exact set and order equal |

`git diff` also found no change from `HEAD` in `src/index.ts`, `schemas/`,
`package.json`, `package-lock.json`, `src/version.ts`, or the base
`src/command/registry.ts`. The assurance descriptor change supplies corrected
example invocations without changing descriptor structure.

## 4. Version and channel baseline

At `2026-08-05T10:50:54+09:00`, direct local and registry reads established:

- `package.json`, both root lockfile identities, and `src/version.ts` remain
  `0.7.0` during self-review;
- npm reports `beta=latest=0.7.0` and no `alpha` dist-tag;
- an exact `perttool@0.7.1` registry lookup returns `E404`; and
- no local `v0.7.1` tag exists.

These facts are read-only baselines. They do not reserve a version, create a
tag or Release, or authorize publication.

## 5. Verification

The correction input had already passed the complete Node.js 22 gate with all
796 tests, English and documentation checks, 32 self-use plans, temporary
linking, and the 601-file isolated-package workflow. The self-review then
added the release contract, the thirty-third self-use plan, fixed
cross-document tests, and repeated the applicable repository gate. The final
self-review state passes:

```sh
/home/katsumata-m/.nvm/versions/node/v22.22.3/bin/npm run check
git diff --check
```

The complete result includes the full test suite, English and documentation
checks, all 33 self-use plans, temporary linking, and isolated installed
package verification while the package identity remains `0.7.0`.

## 6. Authority and next task

The exact initial plan candidate was written with actor `codex`, owner
assertion `user`, affected scopes `goal` and `dag`, and candidate digest
`sha256:a7a62b4778a2efa9c1e73125f77a1a32819cb1ad68ea498797c72dfa374884e5`.
Completing self-review was governance-not-applicable and reused no owner
assertion.

Complete NextResult v6 recommends only `RELEASE_071_PREPARATION`. That task is
not authorized by this self-review. Version-bearing preparation, candidate
acceptance, PUBLISH, durable acceptance, npm `latest` promotion, both plan
advances, remote writes, and Issue mutation remain separate.
