# `v0.9.2` Source Preparation

- Document status: Accepted 1.0
- Acceptance date: 2026-08-14
- Task: `RELEASE_092_PREPARATION`
- Target version: `0.9.2`
- Prepared-plan source digest: `sha256:64cc0ac04d1ed6849d6049e898b4df3929619f20672a65f92c132b5074b07427`
- Release procedure: [`0.9.2-release.md`](0.9.2-release.md)
- Release plan: [`release-0.9.2.pert`](../../plans/release-0.9.2.pert)

## 1. Prepared source identity

Package, root lockfile entries, CLI, private MCP server, and private LSP and
MCP peer identities are aligned to `0.9.2`. CHANGELOG, README, requirements,
basic design, backlog, release records, plan index, shared agent guidance,
tests, current tool-version goldens, and self-use registration identify the
same selected emergency patch boundary.

The public runtime remains exactly 53 commands, 23 root schemas, 129
reference-identical root and Node exports, and 45 Core exports. Grammar 7,
CLI Contract 8, command and option spellings, result and schema identities,
authority, and persistence behavior are unchanged. The public package
inventory remains `dist`, `schemas`, and `CHANGELOG.md`; private LSP, VSIX,
and MCP workspaces remain excluded.

## 2. Verification

The final complete gate ran with Node.js `22.22.3` and npm `10.9.8` and
accepted:

- type checking and builds for the root and all three private adapter
  workspaces;
- all 1,048 Node.js tests, including file and stdin Issue #15 regressions;
- the English baseline over 872 text files with the unchanged three-line
  allowlist;
- repository documentation over 258 Markdown files and seven PERT examples;
- read-only check, analysis, and Next for all 40 self-use plans;
- isolated LSP, MCP, VSIX, supported VS Code `1.101.0`, temporary-link, and
  installed-package workflows;
- a 712-file `perttool@0.9.2` public package with 798.1 kB packed and 4.6 MB
  unpacked sizes; and
- npm beta publication normalization in dry-run mode.

`npm audit --audit-level=low` reported zero vulnerabilities. `git diff
--check` passed. The package workflow created only disposable artifacts and
performed no registry write.

## 3. Plan state and stopping point

`RELEASE_092_SELF_REVIEW`, `RELEASE_092_CORRECTION`, and
`RELEASE_092_PREPARATION` are complete and retained before advance. Readback
reports the prepared-plan digest above and recommends and makes startable only
`RELEASE_092_CANDIDATE`.

The prepared release diff is the complete source boundary to commit once
before candidate verification. No immutable candidate was retained. No push,
tag, GitHub Release, npm publication, dist-tag movement, plan advance, Issue
mutation, or other external write occurred.
