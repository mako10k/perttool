# `v0.10.3` Release Acceptance

- Status: Accepted
- Acceptance date: 2026-08-21
- Release commit: `5970010e8878b3f8c1f5c7353efa9c411f5a041d`
- CI run: `32454842057`
- GitHub Release: <https://github.com/mako10k/perttool/releases/tag/v0.10.3>
- npm channel: `beta=0.10.3`, `latest=0.10.2`, no maintained `alpha`

## Evidence

The release commit, remote `main`, and peeled annotated `v0.10.3` tag agreed.
The complete Node.js 22 and 24 GitHub Actions jobs passed. The retained
876-file, 1,073,175-byte tarball, GitHub asset, and npm registry tarball were
byte-identical at SHA-256
`bdb57f4b4449ada430ef5cbefba63f1dcbef2681d394e4022725e2cddbd9ae3f`.

Two preliminary npm commands failed before publication with authorization
E404: the first resolved a locked temporary-directory domain, and the second
used a stale ambient npm configuration that did not reference the injected
token. Registry readback after each failure confirmed that `0.10.3` was still
absent. The corrected, preflighted repository-domain route referenced the
injected `NPM_TOKEN` through a disposable npm configuration and performed the
single successful publication. No ambiguous publish was blindly retried.

Fresh exact-candidate and npm `beta` installations reported `perttool 0.10.3`.
The installed CLI accepted the Issue #7 token `7200p/827h` through
`project set --velocity` without rounding or semantic drift. Exact
`perttool@0.10.2` remained installable as the rollback pin.

Issue #7 received one release-evidence comment and was closed as completed.
Its `bug` label remains and its completed-work `priority:P1` label was removed.
npm `latest`, public VSIX publication, release-plan advance, and unrelated
work remain separate.
