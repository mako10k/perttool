# perttool 0.10.1 Candidate Acceptance

Date: 2026-08-20

## Decision

Accept one immutable `perttool@0.10.1` release candidate for the Issue #22
document-migration-path correction. External publication remains separately
authorized.

## Source and artifact identity

- Candidate source commit: `318f9cbd2c8cc6dd7763b4cdb5fd632374d9c601`
- Artifact: `perttool-0.10.1.tgz`
- Packed files: 872
- Packed bytes: 1,071,834
- Unpacked bytes: 8,212,120
- SHA-256: `1d7b4e97815304c54190f8b8a257e6646a4023bbb10453be76ce06489d4eb470`
- npm SHA-1: `162b6c5212ab6c002cbc27ce6392df3b0e9c7746`
- npm integrity: `sha512-et1bccrPpeoMQQitdeXFcwJ6s+1m8kNz1av9r8iTfVHfonEWdrnEkG88aBJq9V968knqcku0IZqigAjmkxGkWA==`

The retained artifact is outside the repository at
`/tmp/perttool-0101-candidate-xCWMGm/perttool-0.10.1.tgz`.

## Verification

- The repeated Node.js 22 complete `npm run check` gate passed from a clean
  worktree: 1,216 tests, static checks, English and documentation checks, 44
  self-use plans, private LSP/MCP/VSIX gates, temporary linking, and the
  isolated 872-file package workflow.
- A fresh installation from the retained tarball reported `perttool 0.10.1`.
- Installed `help document migrate --format json` exposed target grammars `7`
  and `8` and the three possible result schemas.
- A tracked Grammar 5 plan previewed and wrote the repository-bound Grammar 7
  migration with CLI Contract 9. The migrated document then reached
  `dag advance` without `PTMAC-101`.
- The installed Grammar 7 to 8 path and unsupported-target rejection are
  covered by the complete candidate gate.
- Fresh external reads reported npm `beta=latest=0.10.0`, no `alpha`, no
  `perttool@0.10.1`, no GitHub Release `v0.10.1`, and no remote `v0.10.1` tag.
- Remote `main` was `f0e3d7110171686c085d5de0b595057fc803dff8` and is an ancestor
  of the candidate source; the candidate branch is three commits ahead.

## Boundary

This acceptance does not push Git, create or push a tag, create a GitHub
Release, publish to npm, move a dist-tag, close Issue #22, or advance the
release plan. Those mutations require their separately frozen authority.
