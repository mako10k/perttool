# `v0.5.1` Compatible Contract 6 Beta Patch Release Acceptance

- Document status: Accepted 1.0
- Accepted on: 2026-07-30
- Release commit: `31d162adb095479ac268f3f99778bac53e806b4b`
- Annotated tag: `v0.5.1`
- GitHub prerelease: <https://github.com/mako10k/perttool/releases/tag/v0.5.1>
- Candidate CI:
  <https://github.com/mako10k/perttool/actions/runs/30509133874>
- PUBLISH record: [`0.5.1-publish.md`](0.5.1-publish.md)
- Release plan: [../../plans/release-0.5.1.pert](../../plans/release-0.5.1.pert)

## 1. Acceptance decision

Accept suffix-free beta `0.5.1` as a backward-compatible Grammar 5 and CLI
Contract 6 patch. All five release-plan tasks and 17p are complete over one
active day. The completed pre-advance plan has source digest
`sha256:11638290f8b80521a4778593008135b9916508df10cf8a27e8a03f317d763ca6`;
the canonical governed advance remains a repository-record operation and
does not alter the release artifact.

The version is a patch because the public change is additive: it adds the
read-only `schema` command, bundled JSON Schema data, and lookup exports, and
accepts strict Git UTC `Z` metadata. Grammar 5, CLI Contract 6, all 33 prior
command descriptors, all 108 prior public exports, result identities, and
normalized existing payload meanings remain compatible. No Contract 7 or
Grammar 6 boundary is introduced.

## 2. Immutable release identity

| Evidence | Accepted value |
| --- | --- |
| Release commit and peeled tag target | `31d162adb095479ac268f3f99778bac53e806b4b` |
| Annotated tag object | `2e8cb8edb53f1082a0274d44c45672168593af6a` |
| Package | `perttool@0.5.1` |
| Candidate, GitHub, and npm tarball SHA-256 | `93f3e01a22a41a7260792cba8df3ec9e47deedd4647b8c61616bb58886941339` |
| npm integrity | `sha512-46yC12AHVJ1Xu2bsIWFcpTQyPlv/LUgs/FGFhszRM8Cva9epSq8hFWSgQ2T5cwBpvgsxggnlAwNBGYr1eds4Pw==` |
| npm SHA-1 | `548681b7a4b4a0b1115a5ea0c92c46f11a7b0ebb` |
| Package files / unpacked size | `491` / `2874102` bytes |
| Tarball size | `503362` bytes |

The release commit, peeled local and remote annotated tag, public non-draft
GitHub prerelease, and npm registry package agree. The retained candidate,
downloaded GitHub, and independently retrieved npm tarballs are byte-for-byte
identical. The GitHub asset API reports the accepted SHA-256 and size, and
the attached `SHA256SUMS` names the same tarball digest.

## 3. Distribution state at acceptance

| Dist-tag | Version |
| --- | --- |
| `beta` | `0.5.1` |
| `latest` | `0.4.0` |
| `alpha` | `0.1.0-alpha.2` |

Publication changed only `beta`. Acceptance does not mutate npm dist-tags,
promote `latest`, or close Issue #5.

## 4. Independent verification

Acceptance independently reread local and remote main, the release commit,
the local and remote annotated tag, the public GitHub prerelease and both
assets, npm version metadata and integrity, and all three dist-tags. Candidate
CI passed the complete repository workflow on Node.js 22 and 24.
The completed acceptance snapshot also passed `npm run check` under Node.js
22: 652 tests, 107 Markdown documents, seven normative PERT examples, all
twenty-two self-use plans, the temporary-link workflow, and the 491-file
isolated-package workflow passed.

The downloaded GitHub tarball and independently retrieved npm tarball each
passed:

```sh
bash scripts/check-package.sh /absolute/path/to/perttool-0.5.1.tgz
```

Each check performed npm publish-normalization dry run, installed the exact
tarball in an isolated prefix, and passed the complete Contract 6 file-first
workflow. The checks cover version identity, Grammar 5, all 34 Contract 6
commands, lifecycle events, suspended analysis and NextResult v5, available
project history and velocity observation, governance, exact unit migration,
help, Guide, the eighteen-schema catalog, package-root lookup APIs, wildcard
schema import, and the normalized existing result boundary.

The npm publication process had returned `+ perttool@0.5.1`, while its five
bounded immediate readbacks returned propagation-time `E404`. No publish
retry occurred. Subsequent public, authenticated, and direct registry reads
established the durable version, integrity, tarball identity, and dist-tags
before acceptance.

No product failure, compatibility mismatch, artifact mismatch, or unresolved
acceptance finding was observed. No global installation was requested or
performed. npm `latest` promotion and Issue #5 closure remain separate
decisions.
