# `v0.3.0` Contract 4 Readiness

- Document status: Accepted 1.0
- Acceptance date: 2026-07-26
- Target version: `0.3.0`
- Release procedure: [0.3.0-release.md](0.3.0-release.md)
- Release plan: [../../plans/release-0.3.0.pert](../../plans/release-0.3.0.pert)
- Implementation macro:
  [../../plans/scheduling-units.pert](../../plans/scheduling-units.pert)
- SU-M3 acceptance:
  [scheduling-units-m3-acceptance.md](scheduling-units-m3-acceptance.md)
- SU-M5 acceptance:
  [scheduling-units-m5-acceptance.md](scheduling-units-m5-acceptance.md)

## Decision

Accept the scheduling-and-units finish as the complete input for `v0.3.0`
release-source preparation. The macro has reached and advanced
`SCHEDULING_UNITS_ACCEPTED`; its precedence and heuristic resource makespans
are zero and its complete NextResult v4 has no recommendation. SU-M3 and SU-M5
have each reached their accepted detail finish and have no remaining task.

The active source exposes one atomic Contract 4 boundary: Grammar 1/2/3,
CheckResult v2, ProjectResult v2, AnalysisResult v3, NextResult v4,
UnitMigrationResult v2, temporal mutation, exact unit migration, the
28-command registry, text and JSON help, Guide, README, public root exports,
and the installed file-first workflow. A complete known non-truncated
NextResult v4 with policy `recommendation_v1_plus_release_gate` remains the
only normal start authority. The independently ranked recommendation set is
gated by temporal eligibility without being replaced by the heuristic
scheduler's runnable set.

There are no open Contract 4 readiness findings. This decision starts only
`RELEASE_030_PREPARATION`. It does not push Git, create or push `v0.3.0`,
create a GitHub release, publish npm, promote npm `latest`, or complete
post-publication acceptance.

## Gate trace

| Release readiness condition | Accepted evidence |
| --- | --- |
| Reached scheduling-and-units finish | `plans/scheduling-units.pert` retains only reached `SCHEDULING_UNITS_ACCEPTED`; check, analyze, and Next v4 return zero work |
| Temporal and deadline target Core | SU-M3 acceptance traces exact calendar projection, release-aware precedence and heuristic resource schedules, deadline evaluation, AnalysisResult v3, and NextResult v4 through `TUI-001` to `TUI-020` and `TUE-001` to `TUE-020` |
| Atomic public Contract 4 cutover | SU-M5 acceptance traces active Grammar 1/2/3, public result schemas, root exports, mutation, registry, help, Guide, README, and installed-package behavior |
| Normal start authority | Recommendation authority and target-temporal tests cover known complete results, recommendation/scheduler independence, unknown and incomplete safe-stop, and the human-override boundary |
| Exact unit migration | Migration tests cover complete inventory, Decimal-or-Fraction exactness, no-op, repetition, qualified inverse, grammar upgrade, deterministic failure, preview, and digest-locked safe writes |
| No hidden finding | SU-M3 and SU-M5 records both state that no acceptance finding remains; the SU-M5 record identifies and closes the task-clear and recommendation/scheduler defects found during integration |

## Verification

The readiness snapshot passed:

```sh
npm run build --silent
node --test test/release-0.3.0-readiness.test.mjs \
  test/scheduling-units-m3-acceptance.test.mjs \
  test/scheduling-units-m5-acceptance.test.mjs
npm run check:docs
npm run check:self-use
git diff --check
```

The preceding SU-M5 acceptance also passed the complete Node.js 22
`npm run check` gate: 513 tests, documentation checks, all seventeen self-use
plans, local-link validation, publish normalization dry-run, and the isolated
installed-package Contract 4 file-first workflow. Package identity remains
`0.2.0`; the coherent `0.3.0` version and changelog update belongs to
`RELEASE_030_PREPARATION`.
