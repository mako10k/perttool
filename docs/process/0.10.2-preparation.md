# `v0.10.2` Source Preparation

Date: 2026-08-21

The source now identifies suffix-free `0.10.2` across the root package and
lockfile, CLI, private adapter peers, MCP server, CHANGELOG, README, current
fixtures, goldens, and release records. Grammar 8, CLI Contract 9, 56 commands,
23 root schemas, 129 root and Node exports, and 45 Core exports are unchanged.

The complete Node.js 22 gate passed:

- TypeScript, jscpd 5.0.15 and Lizard 1.23.0 static gates;
- 1,219 tests;
- English baseline over 1,070 text files and documentation checks over 326
  Markdown files;
- read-only check, analyze and next over 44 self-use plans;
- isolated LSP and MCP package acceptance;
- trusted and untrusted VS Code 1.101.0 host and isolated VSIX acceptance;
- temporary-link verification as `perttool 0.10.2`;
- isolated 872-file package, npm beta publication dry-run, and installed
  Contract 9 file-first and plan-assurance workflows; and
- `git diff --check`.

The static ratchets passed with 146 duplicate clones over 2,731 lines at
3.067 percent and 4,069 functions with 170 reviewed legacy complexity entries.

No remote, tag, GitHub, npm registry, Issue, dist-tag, or plan-advance write was
performed during preparation.
