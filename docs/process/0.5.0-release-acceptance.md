# `v0.5.0` Contract 6 Beta Release Acceptance

- Document status: Accepted 1.0
- Accepted on: 2026-07-29
- Release commit: `af819b44ab0f138c09dc2c96d35b65bc9aad497c`
- Annotated tag: `v0.5.0`
- GitHub prerelease: <https://github.com/mako10k/perttool/releases/tag/v0.5.0>
- PUBLISH record: [`0.5.0-publish.md`](0.5.0-publish.md)
- Release plan: [../../plans/release-0.5.0.pert](../../plans/release-0.5.0.pert)

## 1. Acceptance decision

Accept suffix-free beta `0.5.0` as the first public atomic Grammar 5 and CLI
Contract 6 package. All twelve release requirements are satisfied. The
immutable release artifact remains a beta product and is not a stable-series
declaration.

All six tasks in `plans/release-0.5.0.pert`, totaling 19p over one active day,
are complete at source digest
`sha256:c63a5335cb3b91a0ac6e812f09062ae8da1b73206c7e7b576fdd0adee78df2af`.
Precedence and the `parallel-sgs` version 1 heuristic resource makespans are
zero with no resource delay. Complete, non-truncated NextResult v5 has no
ready, recommended, or startable task.

## 2. Immutable release identity

| Evidence | Accepted value |
| --- | --- |
| Release commit and peeled tag target | `af819b44ab0f138c09dc2c96d35b65bc9aad497c` |
| Annotated tag object | `29cd4e42c8ce60834b40f32e60330f7e51d06767` |
| Package | `perttool@0.5.0` |
| Candidate, GitHub, and npm tarball SHA-256 | `f3ba9b3f52dd055618084bde5e9fa51e98adf3e0e29e10ac8c7e09fd4142208c` |
| npm integrity | `sha512-T4URT2R09l6OyavvTlAdjJt5OXXPYFofNbJIlu7Nw3VhbByDNrzpbNKopcfQv4TB935yxU4ike2/7RiFhMlLiQ==` |
| npm SHA-1 | `f7e588bf3d0dd3c2f33c4e5a377d5aaca65b9595` |
| Package files / unpacked size | `468` / `2789938` bytes |
| Tarball size | `492510` bytes |

The release commit, peeled local and remote annotated tag, GitHub prerelease,
and npm registry package agree. The retained candidate, downloaded GitHub,
and independently retrieved npm tarballs are byte-identical. The GitHub asset
API reports the accepted SHA-256, and the attached `SHA256SUMS` names the same
tarball digest.

## 3. Distribution state at acceptance

| Dist-tag | Version |
| --- | --- |
| `beta` | `0.5.0` |
| `latest` | `0.4.0` |
| `alpha` | `0.1.0-alpha.2` |

Publication changed only `beta`. Acceptance does not mutate npm dist-tags,
promote `latest`, or close Issue #4.

## 4. Independent verification

Acceptance independently reread the local and remote Git references, the
public non-draft GitHub prerelease and both assets, the exact npm version,
registry integrity and metadata, and all three dist-tags. The downloaded
GitHub tarball and independently retrieved npm tarball each passed:

```sh
bash scripts/check-package.sh /absolute/path/to/perttool-0.5.0.tgz
```

Each public artifact check performed publish normalization dry run, installed
the exact tarball in an isolated prefix, and passed the complete Contract 6
file-first workflow. The checks cover `perttool 0.5.0`, Grammar 5, CLI
Contract 6, lifecycle events, suspended AnalysisResult v4 and NextResult v5,
project history, velocity observation, governed safe writes, exact unit
migration, help, Guide, schemas, and package-root exports. No product failure,
identity mismatch, or unresolved acceptance finding was observed.

## 5. Post-acceptance local installation boundary

The user requested exact local installation only after durable release
acceptance. That installation remains the next ordered step and is not part
of the immutable release artifact. Its resolved executable, package version,
and Contract 6 smoke results will be appended to this record after execution.

npm `latest` promotion, Issue #4 closure, REOPEN, automatic velocity adoption,
Git mutation, override apply and audit, MIG-08, LSP, VSIX, MCP, and runtime
i18n remain outside this acceptance.
