# `v0.10.3` Canonical Velocity Token Patch Release Procedure

- Status: Authorized release in progress
- Decision date: 2026-08-21
- Target version: `0.10.3`
- Product maturity: beta
- GitHub channel: prerelease
- npm publication tag: `beta`
- Accepted input: [`issue-7-canonical-velocity-acceptance.md`](issue-7-canonical-velocity-acceptance.md)

## Decision and scope

Select suffix-free `0.10.3` as a compatible patch after published `0.10.2`.
The release fixes Issue #7 by sharing exact Velocity parsing and canonical
generated display. Every non-null observed token is accepted by
`project set --velocity` without rounding, semantic drift, or automatic
declared-velocity adoption.

Grammar 8, CLI Contract 9, 56 commands, 23 root schemas, 129 root and Node
runtime exports, and 45 Core exports remain unchanged.

## Publication gate

Freeze one clean source commit and tarball, verify the complete Node.js 22
gate, exact installation, unused tag/release/npm version, and authenticated
routes. Push the exact commit and annotated tag, require Node.js 22 and 24 CI,
create one GitHub prerelease with the tarball and `SHA256SUMS`, publish the
same tarball once to npm `beta`, then independently read back every identity.

Issue #7 is closed only after durable release evidence is available. npm
`latest`, public VSIX publication, plan advance, and unrelated work remain
separate.
