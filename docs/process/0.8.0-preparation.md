# `v0.8.0` Source Preparation

- Document status: Accepted 1.0
- Preparation date: 2026-08-07
- Target package: `perttool@0.8.0`
- Grammar: Grammar 6
- CLI contract: Contract 7
- Publication state: unpublished
- Predecessor: [`v0.8.0` Input Readiness](0.8.0-input-readiness.md)
- Release procedure: [`v0.8.0`](0.8.0-release.md)
- Release plan: [`release-0.8.0.pert`](../../plans/release-0.8.0.pert)
- Completed-plan source digest: `sha256:0cce301fc769e276ffc45626147a8d20f38bfcbced1b565e417aee5e858dc457`

## 1. Prepared boundary

Package and both root lockfile identities, CLI version, private LSP and MCP
peer versions, MCP server version, CHANGELOG, README, migration guidance,
release records, tests, goldens, self-use registration, and shared agent
policy identify `0.8.0` together.

The source retains Grammar 6 and CLI Contract 7 while exposing 45 commands,
21 root schemas, 122 key- and reference-identical root and Node runtime
exports, and 45 portable Core runtime exports. The new public package surfaces
are `perttool/core`, `perttool/node`, `dag history`, and
`Perttool.HistoricalGraphResult.v1`. The declaration-identity correction is
included without a new syntax or result identity.

The public package inventory remains `dist`, `schemas`, and `CHANGELOG.md`
plus npm's standard package metadata, README, and license inclusion. Private
LSP, VSIX, and MCP workspaces remain excluded. Publication normalization is
restricted to npm `beta`.

## 2. Documentation and migration boundary

The README uses explicit `perttool@0.8.0` commands, describes the 45-command,
21-schema, 122-root/Node, and 45-Core boundary, and keeps `0.7.1` as the exact
rollback pin. It states the durable publication rule: `0.8.0` moves only
`beta` and leaves independently managed `latest=0.7.1`.

The [`0.7.1` to `0.8.0` migration](0.7.1-to-0.8.0-migration.md) requires no
source rewrite or existing-command migration. It identifies the additive
command, result, schema, and package subpaths, the declaration-identity
correction, the private-adapter exclusion, and exact rollback behavior.

Historical `0.7.1` publication and later `latest`-promotion records remain
unchanged.

## 3. Complete local gate

The complete preparation gate ran under Node.js 22.22.3:

```sh
npm ci
npm audit --audit-level=low
npm run check
git diff --check
```

It passed:

- zero dependency vulnerabilities;
- all 973 Node.js tests;
- the 793-file English baseline with three exact allowlisted lines;
- 220 Markdown files and seven normative PERT examples;
- read-only document check, analysis, and Next verification for all 36
  self-use plans;
- isolated LSP and MCP package acceptance;
- the private VSIX shell, DAG, and supported VS Code 1.101.0 trusted and
  untrusted host gate;
- a temporary link reporting `perttool 0.8.0`;
- the 679-file isolated public-package workflow;
- installed Contract 7 file-first and plan-assurance workflows; and
- beta-only npm publication normalization for `perttool@0.8.0`.

`git diff --check` passed. The disposable 2.8 MB package from this gate is
verification output and is not the retained release candidate.

## 4. Authority and remaining work

This preparation performs no push, tag creation, GitHub mutation, npm
publication, dist-tag mutation, VSIX publication, plan advance, or Issue
mutation. Candidate acceptance still requires a clean committed source,
repeated gates, read-only external and protected-route preflight, and one
retained immutable tarball. The exact candidate remains subject to a separate
user boundary before PUBLISH.

`RELEASE_080_PREPARATION` is complete. Complete NextResult v6 recommends and
authorizes only `RELEASE_080_CANDIDATE`.
