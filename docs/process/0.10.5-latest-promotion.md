# `v0.10.5` npm `latest` Promotion

- Promoted: 2026-08-21
- Authorized target: `perttool@0.10.5`
- Registry result: `beta=latest=0.10.5`
- npm integrity: `sha512-CSFsNI20/TLBZzUqvETUkYIFedRBtawcJd6KOFc5ISU4qyzldceltToU6msgmKySPeeSQIqEoyEjjLliHCdFAQ==`

The pre-write readback established `beta=0.10.5` and `latest=0.10.4`. The
first direct dist-tag command had no npm user configuration, failed with E401
before mutation, and readback confirmed that state remained unchanged. The
established temporary npmrc route authenticated as `mako10k`; one subsequent
`npm dist-tag add perttool@0.10.5 latest` mutation succeeded.

Fresh registry readback established `beta=latest=0.10.5` and unchanged package
integrity. An unqualified user-level global installation under the default
NVM Node.js `v25.1.0` resolved to `perttool 0.10.5` and reported CLI Contract
9. Issue #23 remains open, and Issue mutation is separate.
