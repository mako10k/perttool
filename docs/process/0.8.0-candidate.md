# `v0.8.0` Release Candidate Acceptance

- Document status: Accepted 1.0
- Review date: 2026-08-07
- Target version: `0.8.0`
- Candidate source commit: `f9be1ccdea04d7f029383f398d6b742d8962f09d`
- Candidate source tree: `16319f9048d86c3666bb3eb1428dec99769da0c8`
- Completed-plan source digest: `sha256:d91f752c0a98a7a281a8cf405de8018a36bcf9e66dfdde7a82d8a5ecf3249b3c`
- Release plan: [`release-0.8.0.pert`](../../plans/release-0.8.0.pert)
- Release procedure: [`0.8.0-release.md`](0.8.0-release.md)

## 1. Acceptance boundary

This record accepts one immutable local `perttool@0.8.0` tarball for the
additive adapter-platform, historical-DAG, and declaration-identity release.
It fixes the source commit, public-package identity, read-only external
baseline, artifact bytes, and installed behavior that may be used by a
separately authorized PUBLISH batch.

It does not authorize or perform a Git push, tag creation or push, GitHub
Release, npm publication, npm dist-tag mutation, public VSIX publication,
durable public acceptance, plan advance, or Issue mutation. Complete
NextResult v6 sequencing evidence does not replace the candidate-specific
external-write instruction.

## 2. Clean source and repository gate

Commit `f9be1ccdea04d7f029383f398d6b742d8962f09d` was clean and identified the
package, both lockfile roots, CLI, private adapter peer ranges, and MCP server
as `0.8.0`. Its committed tree was
`16319f9048d86c3666bb3eb1428dec99769da0c8`.

The first clean candidate rerun exposed that three test-harness `spawnSync`
calls retained Node.js's default 1 MiB output buffer while successful
`dag history` output had grown with the first-parent repository history. The
product command returned complete output; the harness processes failed with
`ENOBUFS`. The source commit above explicitly raises only those three bounded
test buffers to 16 MiB. The twelve focused historical CLI, schema, and
acceptance cases then passed.

From that clean commit, Node.js 22.22.3 `npm ci`, zero-finding `npm audit`, the
complete `npm run check`, and `git diff --check` passed. The complete gate
included all 973 tests, the 794-file English baseline with three exact
allowlisted lines, 221 Markdown files and seven normative PERT examples, all
36 self-use plans, isolated LSP and MCP acceptance, the private VSIX supported-
host gate, temporary linking, and the 679-file isolated public-package gate.

## 3. Read-only external and route preflight

Direct reads found no local or remote `v0.8.0`, no GitHub Release `v0.8.0`,
and no npm `perttool@0.8.0`. npm reported `beta=latest=0.7.1`, no `alpha`, and
registry metadata modification time `2026-08-05T02:40:49.566Z`.
`origin/main` remained
`d1578b93d3fbfc6e0ec631e068f5bb207b4a0f4d`.

The process-scoped GitHub and npm identities both resolved as `mako10k`
without displaying credential values. The authenticated `secdat` Git dry-run
projected a fast-forward of `origin/main` through
`f9be1ccdea04d7f029383f398d6b742d8962f09d` without writing. GitHub's branch-
protection endpoint returned HTTP 404 for `main`; therefore this record makes
no branch-protection claim. It records the absent protection separately from
the successful authenticated route check. No external state changed.

## 4. Immutable candidate identity

The fixed-path candidate was generated once from the accepted source commit
and then made read-only:

| Property | Exact value |
| --- | --- |
| Path | `/home/katsumata-m/releases/perttool/0.8.0/candidate/perttool-0.8.0.tgz` |
| Modification time | `2026-08-07 15:23:14.736908753 +0900` |
| Mode | `0444` |
| Packed size | `2753740` bytes |
| Unpacked file bytes | `6330315` bytes |
| Files | `679` |
| SHA-1 | `538a4f89970d5982e7e28de1c1d5ba001c89e97b` |
| SHA-256 | `d761e2a159d2d60eb981efda403cc6b00c4eac9e31503b2e857c0b851ac00b28` |
| npm integrity | `sha512-emyZisy2T1MBj3OYYwG7s2XGru+6I1S9l7oy+v5NONonJ7S6E1rw5mM8o5Wz91x2E6LsmlD8fwMxWWpdid4QIw==` |

The archive manifest identifies public `perttool@0.8.0`, CLI
`dist/cli.js`, the root, `perttool/core`, `perttool/node`, and schema exports,
and npm tag `beta`. It contains the historical result schema and excludes all
private `adapters/` paths. The bundled README identifies the exact `0.8.0`
pin, `dag history`, both public subpaths, independently managed
`latest=0.7.1`, and the `0.7.1` rollback pin.

## 5. Candidate-specific package acceptance

Those exact retained bytes passed `scripts/publish-npm.sh --dry-run` for npm
`beta` and an isolated installation under Node.js 22.22.3. The installed
package reported `perttool 0.8.0`, 45 commands, 21 schemas, 122 key- and
reference-identical root and Node exports, and 45 Core exports. Installed
`dag history` returned `Perttool.HistoricalGraphResult.v1` with ten
checkpoints and seven lineage occurrences from the repository fixture.

An installed `task add` preview inserted exactly one new task before the first
same-namespace `plan_seal`, retained the source file byte-for-byte, and
confirmed the declaration-identity correction. Direct archive inspection
again found 679 public files and no private adapter path. The retained
candidate SHA-256 remained
`d761e2a159d2d60eb981efda403cc6b00c4eac9e31503b2e857c0b851ac00b28`.

After adding this repository-only acceptance record, the completed task state,
shared policy, self-use record, focused test, and golden update, the complete
Node.js 22.22.3 gate passed again: 973 tests, 795 English baseline files, 222
Markdown files, seven normative PERT examples, all 36 self-use plans, all
adapter gates, temporary linking, and isolated-package validation. Its
disposable public package again reported SHA-1
`538a4f89970d5982e7e28de1c1d5ba001c89e97b`; the retained candidate was not
rewritten.

`RELEASE_080_CANDIDATE` is complete. Complete, non-truncated NextResult v6
recommends and makes startable only `RELEASE_080_PUBLISH`, but the release
procedure still requires the user to authorize this exact candidate, release
commit, Release body and assets, target refs, npm operation, and maximum write
count before any external mutation.
