# `v0.7.0` Conditional Plan Assurance Beta Release Procedure

- Status: Planned 1.0
- Decision date: 2026-08-04
- Target version: `0.7.0`
- Product maturity: beta
- GitHub channel: prerelease
- npm publication tag: `beta`
- Expected pre-publication tags: `beta=latest=0.6.0`, no `alpha`
- Versioning: [ADR 0003](../adr/0003-beta-versioning.md)
- Semantic contract: [Conditional Plan Assurance Contract](../specs/plan-assurance.md)
- Interface contract: [Plan Assurance Interface Contract](../specs/plan-assurance-interface.md)
- Public-contract acceptance:
  [Plan Assurance Public Contract Acceptance](plan-assurance-public-contract-acceptance.md)
- Implementation acceptance:
  [Conditional Plan Assurance Acceptance](plan-assurance-acceptance.md)
- Implementation roadmap: [`plan-assurance.pert`](../../plans/plan-assurance.pert)
- Release plan: [`release-0.7.0.pert`](../../plans/release-0.7.0.pert)

## 1. Decision and scope

Select suffix-free `0.7.0` for the first package that publishes Grammar 6,
CLI Contract 7, and conditional plan assurance. The grammar, command registry,
result identities, governance interface, recommendation start authority,
advance semantics, schema catalog, package root, and installed workflow change
together. `0.6.1` would understate this breaking public boundary, while
`1.0.0` remains reserved for a future stable-series decision.

The completed `ASSURE-001` plan and its accepted records remain the
implementation authority. This release plan does not restore or duplicate
their completed task state. `RELEASE_070_CONTRACT_7_READINESS` verifies the
complete pre-advance workstream and accepted source/package boundary before
release-source preparation begins. Canonical advance of either plan is not a
release prerequisite and remains a separate mutation.

This release includes:

- Grammar 6 assurance declarations, default `both`, explicit
  `planning_only` and `execution_only` relations, component seals, outcome
  commitments, and frontier receipts;
- CLI Contract 7 with 44 commands, `plan-assurance inspect`, scalar
  `plan-assurance hash`, preview-first seal/reseal/outcome/relation mutations,
  and complete help and Guide projections;
- `Perttool.PlanAssuranceResult.v1`, `Perttool.CheckResult.v4`,
  `Perttool.ProjectResult.v4`, `Perttool.AnalysisResult.v5`,
  `Perttool.NextResult.v6`, `Perttool.MutationResult.v4`, and
  `Perttool.AdvanceResult.v2`;
- `Perttool.GovernanceDecision.v2`, assurance impact, complete mismatch cause
  paths, required actions, and fail-closed new-start authority;
- assurance-preserving advance contraction with independently composed Git
  history safety and exact frontier-receipt reconstruction;
- Grammar 1 through 5 compatibility with assurance reported as not enabled;
- Grammar 6 compatibility across formatting, lifecycle, history, unit
  migration, atomic mutation, and Mermaid semantic profile 2; and
- 20 complete Draft 2020-12 root schemas, package-root APIs, temporary-link,
  and isolated installed-package coverage.

This gate does not:

- claim that SHA-256 is a digital signature, authenticated approval, malicious
  edit resistance, external transparency log, or root of trust;
- implement the opt-in archived advance backlog, REOPEN, MIG-08, human
  override apply, durable authorization audit, Issue synchronization, LSP,
  VSIX, MCP, or runtime i18n;
- promote `0.7.0` to npm `latest`;
- advance `plans/plan-assurance.pert` or this release plan;
- mutate an Issue; or
- declare stable product maturity or select `1.0.0`.

## 2. Work packages and authority

The dependency order is fixed by `plans/release-0.7.0.pert`:

1. `RELEASE_070_GATE_DESIGN` selects the version and fixes requirements, ADR,
   design, this procedure, the release plan, restart rules, current channel
   facts, and external authority boundaries.
2. `RELEASE_070_CONTRACT_7_READINESS` verifies complete ASSURE-001 acceptance,
   the active public source/package boundary, and the absence of hidden
   acceptance findings.
3. `RELEASE_070_PREPARATION` aligns version-bearing source, migration and
   release documentation, tests, goldens, audit state, and package validation.
4. `RELEASE_070_CANDIDATE` accepts one clean release commit and one immutable
   tarball after complete local and read-only external preflight.
5. `RELEASE_070_PUBLISH` pushes the release commit and annotated tag, waits
   for CI, creates the GitHub prerelease, and publishes the same tarball once
   to npm `beta`.
6. `RELEASE_070_ACCEPTANCE` independently records durable public identity and
   installed behavior without changing the immutable artifact.

The user's 2026-08-04 instruction authorizes only
`RELEASE_070_GATE_DESIGN`. It does not authorize the readiness, source
preparation, candidate, PUBLISH, acceptance, npm `latest`, plan advance, or
Issue-mutation steps. Plan state records sequencing and evidence; it is not a
substitute for a later user instruction.

`RELEASE_070_GATE_DESIGN` is complete and retained in its exact pre-advance
state. Complete NextResult v6 recommends only
`RELEASE_070_CONTRACT_7_READINESS`; that recommendation does not expand the
authorization above.

The gate-design evidence was read-only except for repository-local files and
the task lifecycle record:

- local committed `HEAD` was `e7924f9`, including the narrow `fast-uri`
  `3.1.4` to `3.1.5` lockfile correction; `npm audit` reported zero known
  vulnerabilities after a clean install;
- `origin/main` remained `5f43ce1`, whose Node.js 22 and 24 repository check
  completed successfully in GitHub Actions run `30814333463`; the later
  Contract 7 commits therefore had no remote-CI result yet;
- npm reported `beta=latest=0.6.0`, no maintained `alpha`, and no published
  `perttool@0.7.0`;
- package, lockfile root entries, and `src/version.ts` remained `0.6.0`; and
- no push, tag, GitHub mutation, npm publication, dist-tag operation, plan
  advance, or Issue mutation occurred.

The completed local gate-design verification used Node.js `22.22.3` and
passed a clean `npm ci`, `npm audit` with zero known vulnerabilities, all 789
tests, the 602-file English baseline with three exact allowlisted files, 153
Markdown and seven normative PERT documentation checks, read-only check,
analyze, and next validation for all 31 self-use plans, the temporary-link
workflow, and `git diff --check`. The isolated package gate accepted the
601-file, 656.1 kB `perttool@0.6.0` package through both the Contract 7
file-first and plan-assurance workflows. This evidence validates the release
gate design against the current source; it does not complete or authorize the
separate Contract 7 readiness or source-preparation tasks.

## 3. Contract 7 readiness gate

Before release-source preparation:

1. `plans/plan-assurance.pert` must retain its exact complete pre-advance state
   with zero remaining work and no ready, recommended, or startable task.
2. `docs/process/plan-assurance-acceptance.md` and
   `docs/process/plan-assurance-public-contract-acceptance.md` must remain
   accepted with no hidden finding.
3. Grammar 6, CLI Contract 7, all 44 commands, all 20 root schemas, package
   APIs, help, Guide, temporary link, and isolated installed workflow must
   remain active together.
4. The fixed semantic hash vectors, lifecycle exclusions, outcome
   conformance, cause paths, selected reseal, partial-branch authority, and
   assurance-preserving advance cases must remain unchanged.
5. Grammar 1 through 5 must retain their accepted semantics and report
   assurance as not enabled. Grammar 6 compatibility paths must preserve
   assurance bytes and semantic commitments exactly where required.
6. The Git history-loss force boundary must remain independent from assurance
   reseal and replanning authority.
7. `npm audit` must report no known vulnerability at this gate. A later audit
   change stops preparation for reassessment rather than authorizing an
   automatic major dependency upgrade.

## 4. Source-preparation gate

Update and verify together:

- `package.json`, both root lockfile version entries, and `src/version.ts`;
- a dated `0.7.0` CHANGELOG section and comparison link;
- README current-channel, exact-pin, Contract 7, and migration guidance;
- an explicit `0.6.0` to `0.7.0` migration guide covering command, JSON,
  TypeScript, grammar, governance, recommendation-authority, and Mermaid
  compatibility boundaries;
- release tests, goldens, current agent guidance, and self-use metadata; and
- package inclusion, schema/command/API identity, installed Contract 7 and
  assurance workflows, dependency audit, and beta-only publication
  normalization.

Run under Node.js 22:

```sh
npm ci
npm audit
npm run check
git diff --check
```

No external write is allowed in this gate. The release version remains
`0.6.0` until this separate preparation task is authorized and completed.

## 5. Candidate gate

Before the first external write:

1. Require clean committed source with package, lockfile, CLI version, and
   intended tag equal to `0.7.0` and `v0.7.0`.
2. Repeat the complete Node.js 22 repository, temporary-link, and isolated
   package gates and require zero known audit findings.
3. Confirm local and remote `v0.7.0`, the GitHub Release, and
   `perttool@0.7.0` are absent.
4. Record npm `beta=latest=0.6.0` and confirm `alpha` remains absent.
5. Verify protected Git, GitHub, and process-scoped npm routes without
   displaying credentials or changing remote state.
6. Build one tarball outside the worktree and record its modification time,
   file count, packed and unpacked sizes, SHA-1, SHA-256, and npm integrity.
7. Install that retained candidate in isolation and run complete file-first,
   plan-assurance, compatibility, hash-inspection, and advance workflows.
8. Stop if source, artifact, authorization, route, audit, or external baseline
   changes.

The intended retained path is
`/home/katsumata-m/releases/perttool/0.7.0/candidate/perttool-0.7.0.tgz`.
Later evidence records must not regenerate or silently replace it.

## 6. PUBLISH gate

Only after every predecessor gate and a separate user instruction naming the
unchanged candidate and external batch:

1. push the clean release commit through `secdat exec git` and verify
   `origin/main` readback;
2. create and push annotated `v0.7.0` at that exact commit and verify its
   peeled remote target;
3. require successful Node.js 22 and 24 CI for the release commit;
4. create a GitHub prerelease with the retained tarball and `SHA256SUMS`
   through `secdat exec gh` and verify public asset identity;
5. download, hash, install, and exercise the GitHub asset in isolation;
6. inject `NPM_TOKEN` only into one publication process and publish the
   downloaded GitHub tarball exactly once to npm `beta`; and
7. verify `beta=0.7.0`, unchanged `latest=0.6.0`, and absent `alpha`.

Do not retry an ambiguous GitHub or npm mutation. Inspect durable remote and
registry state first, then resume only a demonstrably missing idempotent step.

## 7. Durable acceptance

Independently re-read the release commit, peeled tag, successful CI, GitHub
and npm metadata, common candidate/GitHub/npm bytes and digests, registry
integrity, and isolated public-package behavior. The record must prove the
exact Grammar 6, CLI Contract 7, plan-assurance, compatibility, hash,
authority, advance, help, Guide, schema, and package-root boundaries.

Do not move `latest`, advance either plan, or mutate an Issue during durable
acceptance.

## 8. Restart and rollback

- Before an external write, rebuild and reverify only from the current clean
  candidate commit and discard any replaced local candidate.
- For an ambiguous Git or GitHub mutation, read the exact remote reference or
  release asset before deciding whether a retry is necessary.
- For an ambiguous npm publication, read exact version, tarball, integrity,
  and dist-tags before any retry.
- If a public artifact differs from the accepted candidate, stop acceptance;
  never overwrite a tag, release asset, or immutable npm version.
- Consumers can roll back by pinning `perttool@0.6.0`. Do not repair a
  `0.7.0` problem by moving `latest` or republishing the same version.
