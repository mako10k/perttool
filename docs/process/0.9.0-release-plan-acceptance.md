# `v0.9.0` Release Plan Acceptance

- Document status: Accepted 1.0
- Acceptance date: 2026-08-13
- Target version: `0.9.0`
- Release plan: [`release-0.9.0.pert`](../../plans/release-0.9.0.pert)
- Accepted source digest: `sha256:104c58d045c1920ec9afe72fb63aeff39306f6880e468a41139372b9b6be0701`

## 1. Selected boundary

The plan selects suffix-free `0.9.0` as the first Grammar 7 and CLI Contract 8
public package after `0.8.1`. It fixes 53 commands, 23 root schemas, 129 root
and Node runtime exports, 45 Core exports, milestone acceptance, committed
migration, historical reconstruction, and accepted read-only adapter
projection as one release boundary.

Private adapters remain excluded from the public tarball. Candidate
acceptance, publication, npm `latest`, plan advance, and Issue mutation remain
separate.

## 2. Preview-first creation and authority

Contract 8 `project init` created the exact smallest Grammar 6 release-plan
document with source digest
`sha256:e4e7c85defb3eb5aaf07576b433ded577454d2fb8d0c1c84687f342b507df745`.
It declared goal and DAG owner `user` and ended at the reached planning
milestone.

One assertion-free 18-mutation preview affected goal and DAG scopes and
produced the exact candidate with digest
`sha256:104c58d045c1920ec9afe72fb63aeff39306f6880e468a41139372b9b6be0701`.
The user's instruction to prepare `0.9.0` authorized this single local plan
candidate. One write used actor `codex`, the candidate-bound owner assertion
`user`, and the exact original digest. Readback matched the candidate digest;
the assertion is not reusable.

## 3. Plan structure and current authority

The six serial tasks total 22p. Five resources serialize engineering, review,
release documentation, packaging, and external mutation. Precedence and
`parallel-sgs` version 1 makespans are both 22p with zero resource delay.

Initial complete NextResult v7 recommends and makes startable only
`RELEASE_090_GATE_DESIGN`. Plan creation changes no package version, remote,
tag, GitHub Release, npm state, dist-tag, VSIX publication state, plan advance,
or Issue.
