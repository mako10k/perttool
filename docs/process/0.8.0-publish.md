# `v0.8.0` Beta Publication Record

- Document status: Accepted 1.0
- Publication date: 2026-08-07
- Version: `0.8.0`
- Release commit: `db5c2f878c3ec62ea6885f40bab1f04c6541077b`
- Release tree: `20ce3056ab65c5072e1aed2dbf4d6262d1c7baa4`
- Annotated tag object: `37ae676807c48cc7320df3d9c2592d7cca3a5d31`
- Candidate SHA-256: `d761e2a159d2d60eb981efda403cc6b00c4eac9e31503b2e857c0b851ac00b28`
- CI run: [`31154880011`](https://github.com/mako10k/perttool/actions/runs/31154880011)
- GitHub Release: [`v0.8.0`](https://github.com/mako10k/perttool/releases/tag/v0.8.0)
- npm publication tag: `beta`
- Completed-plan source digest: `sha256:769e8cf8dd8fbf312ef1ba3c1fe840350ac2e93485923170693a77aa85f6499e`

## 1. Authorized boundary

After candidate acceptance, the user separately authorized the exact release
commit, immutable tarball, annotated tag, GitHub Release name and body, two
assets, npm `beta` operation, and a maximum of six external mutations. The
batch excluded npm `latest`, public VSIX publication, both input-plan
advances, release-plan advance, Issue mutation, and unrelated work.

The completed batch changed one remote `main` ref, one remote tag ref, one
GitHub Release and two assets, and npm publication plus its `beta` tag. No
mutation was resent after an ambiguous response.

## 2. Git and CI

`origin/main` advanced once from
`d1578b93d3fbfc6e0ec631e068f5bb207b4a0f4d` to release commit
`db5c2f878c3ec62ea6885f40bab1f04c6541077b` through the configured `secdat`
route. Remote SHA readback matched immediately.

Repository-check run
[`31154880011`](https://github.com/mako10k/perttool/actions/runs/31154880011)
passed for that exact commit. Node.js 24 job `92792108753` completed in 2m49s,
and Node.js 22 job `92792108783` completed in 3m06s.

Only after both jobs passed, annotated tag object
`37ae676807c48cc7320df3d9c2592d7cca3a5d31` was created with message
`perttool 0.8.0` and pushed once. Local and remote peeled `v0.8.0` both resolve
to the release commit.

## 3. GitHub prerelease

The first Release-create request used a process route whose token lacked the
required `workflow` scope. GitHub rejected it before mutation. Readback found
no Release, so the request was not repeated on that route. The established
process-scoped `GH_TOKEN_MAKO10K` route then resolved as `mako10k` with
`repo` and `workflow` scopes, and the unchanged Release request was submitted
once.

GitHub prerelease `366565943` is non-draft and was published at
`2026-08-07T06:46:55Z`. Its exact name is `perttool 0.8.0`; its body is the
candidate-bound 312-byte `GITHUB_RELEASE.md`. The package asset is 2753740
bytes and reports SHA-256
`d761e2a159d2d60eb981efda403cc6b00c4eac9e31503b2e857c0b851ac00b28`.
The 85-byte `SHA256SUMS` asset reports SHA-256
`1fc06419181963f2b801f01fd04bfc3f4853ac76374e59592ebf571ee89bed12`.

Both downloaded assets were byte-identical to their retained local inputs.
The downloaded checksum passed, and the GitHub package asset passed the full
isolated public-package workflow.

## 4. npm publication

The process-scoped npm identity was `mako10k`. The immutable candidate was
submitted exactly once to npm `beta`; npm acknowledged `+ perttool@0.8.0`.
The publication script's short five-read window still received propagation
`E404` and exited without claiming durable state. The publish operation was
not retried. A direct no-cache packument read and a separate fresh-cache npm
read then confirmed the published version at `2026-08-07T06:48:55.944Z`.

npm reports 679 files, 6330315 unpacked file bytes, SHA-1
`538a4f89970d5982e7e28de1c1d5ba001c89e97b`, and integrity
`sha512-emyZisy2T1MBj3OYYwG7s2XGru+6I1S9l7oy+v5NONonJ7S6E1rw5mM8o5Wz91x2E6LsmlD8fwMxWWpdid4QIw==`.
The newly downloaded npm tarball was byte-identical to the candidate and
GitHub asset.

At the publication boundary, npm reported `beta=0.8.0`, unchanged
`latest=0.7.1`, and no `alpha`. `RELEASE_080_PUBLISH` is complete. npm
`latest` remains an independently authorized operation.
