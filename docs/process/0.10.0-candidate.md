# `v0.10.0` Candidate Acceptance

- Document status: Accepted 1.0
- Acceptance date: 2026-08-18
- Target version: `0.10.0`
- Task: `RELEASE_0100_CANDIDATE`
- Release plan: [`release-0.10.0.pert`](../../plans/release-0.10.0.pert)

## 1. Fixed source and external baseline

Clean prepared source commit `86762a71562bf15cffe746e2aa6160996aa82942`
passed the complete Node.js 22.22.3 gate. Read-only preflight established:

- `origin/main=094132cd1ee1f451e39f56e94200bbc0d880aedd`;
- npm `beta=0.9.4`, `latest=0.9.0`, and no reported `alpha`;
- no published `perttool@0.10.0`;
- no local or remote `v0.10.0` tag and no GitHub Release for that tag; and
- an authenticated `secdat` fast-forward dry run from `origin/main` to the
  fixed source succeeded without changing the remote.

## 2. Immutable package candidate

The retained candidate is:

- filename: `perttool-0.10.0.tgz`;
- files: 873;
- packed bytes: 3,082,951;
- unpacked bytes: 10,221,298;
- SHA-256:
  `b98dad654955b639275ebcccd1871a3a40ce415cf0c5504dd18c681dfa36ce9f`;
- npm SHA-1: `083ec224416243c1b71fbdcfa00a7d2cd8648c1f`; and
- npm integrity:
  `sha512-6IVZUiGvEvsMoCeh4VCSJk4kanU2r9+HQOzpJAHe4zOvilyov3OUy5LtIlA7LK86zxjnYr8ifx7sJ3u0Ji87mQ==`.

The exact artifact is retained outside the worktree at the candidate boundary.
An npm publication dry run accepted it as public suffix-free `0.10.0` on
`beta` without manifest normalization.

## 3. Installed behavior and boundary

An isolated installation from the retained tarball reported `perttool 0.10.0`
and passed the Contract 9 file-first, plan-assurance, repository-clean
advance, actuals, public-facade, schema, Guide, historical DAG, temporal, and
compatibility checks. The package exposes 56 commands, 23 active root schemas,
129 root and Node exports, and 45 Core exports while excluding the private
LSP, VSIX, and MCP workspaces.

No Git ref, branch, GitHub Release, npm package, dist-tag, Issue, consumer
plan, or external repository was changed by candidate acceptance. PUBLISH
remains a separate exact-candidate authorization.
