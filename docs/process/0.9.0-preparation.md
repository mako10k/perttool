# `v0.9.0` Source Preparation

- Document status: Accepted 1.0
- Acceptance date: 2026-08-13
- Task: `RELEASE_090_PREPARATION`
- Target version: `0.9.0`
- Prepared-plan source digest: `sha256:16cdb32b27280fff2c99c2aff10212ee75f512b692dabb255bf7321e74fa9fb3`
- Release procedure: [`0.9.0-release.md`](0.9.0-release.md)
- Release plan: [`release-0.9.0.pert`](../../plans/release-0.9.0.pert)

## 1. Prepared source identity

Package, root lockfile entries, CLI, private MCP server, and private LSP and
MCP peer identities are aligned to `0.9.0`. CHANGELOG, README, migration,
requirements, ADR, basic design, plan index, shared agent guidance, release
tests, current tool-version goldens, and self-use registration identify the
same selected boundary.

The public runtime remains exactly 53 commands, 23 root schemas, 129
reference-identical root and Node exports, and 45 Core exports. The public
package inventory remains `dist`, `schemas`, and `CHANGELOG.md`; private LSP,
VSIX, and MCP workspaces remain excluded.

## 2. Verification

The final complete gate ran with Node.js `22.22.3` and npm `10.9.8` and
accepted:

- type checking and builds for the root and all three private adapter
  workspaces;
- all 1,041 Node.js tests;
- the English baseline over 854 text files with the unchanged three-line
  allowlist;
- repository documentation over 245 Markdown files and seven PERT examples;
- read-only check, analysis, and Next for all 38 self-use plans;
- isolated LSP, MCP, VSIX, supported VS Code `1.101.0`, temporary-link, and
  installed-package workflows;
- a 713-file `perttool@0.9.0` public package with 2.8 MB packed and 6.6 MB
  unpacked sizes; and
- npm beta publication normalization in dry-run mode.

`npm audit --audit-level=low` reported zero vulnerabilities. `git diff
--check` passed. The package workflow created only disposable artifacts and
performed no registry write.

## 3. Plan state and stopping point

`RELEASE_090_GATE_DESIGN`, `RELEASE_090_INPUT_READINESS`, and
`RELEASE_090_PREPARATION` are complete and retained before advance. Readback
reports the prepared-plan digest above and recommends and makes startable only
`RELEASE_090_CANDIDATE`.

The prepared release diff is the complete source boundary to review and commit
once before candidate verification. No immutable candidate was retained. No
push, tag, GitHub Release, npm publication,
dist-tag movement, VSIX publication, plan advance, Issue mutation, or other
external write occurred.
