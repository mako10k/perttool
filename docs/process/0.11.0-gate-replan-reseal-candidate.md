# `v0.11.0` Release Gate P1 Frontier Reseal Candidate

- Status: Candidate 1.1 applied exactly and independently read back; single-
  frontier task
- Withdrawn candidate: Candidate 1.0 seven-task bulk reseal
- Date: 2026-08-31
- Source PERT digest:
  `sha256:6c0592deaa94395325fffc817e855a1732db3d13f0bc5b36c3cb0ecb4fe7b3b5`
- Candidate PERT digest:
  `sha256:23ef7711c89afba91733e540e0bd4a91675cbfd6672af2f4c19f4b1bdfe510b7`
- Candidate size: 31,574 UTF-8 bytes
- Release effect: none

## 1. Corrected review philosophy

`replan_and_reseal.affected_task_ids` is the complete review queue, not an
instruction to accept the entire queue in one mutation. Reseal explicitly
accepts the selected task bases and intentionally leaves unselected mismatching
descendants visible. This preserves task-local review and prevents a hash-
propagation closure from becoming bulk acceptance evidence.

Candidate 1.0 incorrectly selected all seven affected tasks merely to make the
required-action set empty. That use was mechanically valid but contrary to the
review philosophy, so it is withdrawn. Gate Design and the four release tasks
must remain stale until each reaches its own review frontier. The other new
correction task must also retain its independent initial review.

The current execution frontier recommends only
`POOL_NEXT_SIGNAL_CONSISTENCY`. Candidate 1.1 therefore selects only that task.

## 2. Exact mutation candidate

```text
operation: plan-assurance.reseal
task: POOL_NEXT_SIGNAL_CONSISTENCY
reason: Independent review accepted the Issue #37 current-start authority correction contract
actor: codex
```

The assertion-free preview makes one normalized edit. Governance is applicable
only to `plan_assurance`, requires owner `user`, is bound to the exact source
digest, and reports `write_authorized=false` without a candidate-bound
assertion. After independent review, the owner confirmed that exact candidate
and one expected-digest write applied it without changing any other task seal.

The selected commitment is:

- contract hash:
  `sha256:4bc05bcecfad08a2ba7e932e040add94326b44aef8f8b43bee683639182212d6`;
- accepted and computed basis hash:
  `sha256:8f880210a2a04a8b6c65bd3e2f9dfc1e13e91db0fd8220872da1fffe248c1819`;
- reason: `Independent review accepted the Issue #37 current-start authority correction contract`.

## 3. Preserved stale state

The valid candidate deliberately retains `partial` project coverage and the
remaining `replan_and_reseal` action. In particular:

- `POOL_GOVERNANCE_BINDING_FIX` remains `unsealed`;
- `POOL_RELEASE_GATE_DESIGN` remains suspended and `review_required`;
- Release Preparation, Candidate, Publish, and Acceptance remain
  `review_required`; and
- no unselected accepted basis changes.

This is not an incomplete write. It is the intended evidence that each future
task still requires its own review.

## 4. Next effect

Readback reports both correction tasks as structurally ready. Shared
`ACCEPTANCE_REVIEWERS` capacity keeps only `POOL_NEXT_SIGNAL_CONSISTENCY`
runnable and recommended. Its own assurance withholding is removed, so it is
the sole startable recommended task. Starting it remains a separate lifecycle
write.

## 5. Owner decision boundary

The owner accepted only one `plan-assurance.reseal` write for
`POOL_NEXT_SIGNAL_CONSISTENCY` with the exact reason above against source
digest `sha256:6c0592de...7b3b5`, producing candidate digest
`sha256:23ef7711...510b7`. That write is complete and independently read back.
It did not authorize another task's seal or reseal, task start,
implementation, Gate Design resume, Outcome, criterion, receipt, commit, push,
Issue mutation, release preparation, publication, or plan advance.
