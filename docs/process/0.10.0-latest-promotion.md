# `perttool@0.10.0` npm `latest` Promotion

- Document status: Accepted 1.0
- Promotion date: 2026-08-18
- Selected version: `perttool@0.10.0`
- Mutation: npm `latest` dist-tag only
- Result at promotion: `beta=latest=0.10.0`, no `alpha`
- Registry modification time after promotion: `2026-08-18T09:20:13.523Z`

## 1. Authority and precondition

The user separately selected `perttool@0.10.0` and authorized its promotion
to npm `latest`. Immediately before the mutation, npm reported
`beta=0.10.0`, `latest=0.9.0`, no `alpha`, and the accepted `0.10.0`
integrity value
`sha512-6IVZUiGvEvsMoCeh4VCSJk4kanU2r9+HQOzpJAHe4zOvilyov3OUy5LtIlA7LK86zxjnYr8ifx7sJ3u0Ji87mQ==`.

An ambient npm request failed with `E401` before mutation. Readback confirmed
that the tags remained unchanged. The established process-scoped route then
resolved the npm principal as `mako10k` without displaying credentials.

## 2. Exact mutation and readback

The following logical mutation was executed exactly once through the
process-scoped `secdat` route:

```sh
npm dist-tag add perttool@0.10.0 latest
```

npm returned `+latest: perttool@0.10.0`. A direct no-cache registry read and
a separate fresh-cache npm read both reported `beta=latest=0.10.0` with no
`alpha`. The immutable package retained its accepted integrity value. No
package publication or second dist-tag mutation occurred.

This is a historical promotion result. Current dist-tags have subsequently
advanced and are not expected to remain at `0.10.0`.

## 3. Consumer verification

A fresh-cache unqualified isolated installation under Node.js `22.22.3`
resolved to `perttool 0.10.0`, exposed 56 commands and 23 root schemas, and
passed document checking over the accepted Grammar 8 temporal schedule plan.

## 4. Boundary

The promotion changed only npm `latest`. It did not alter `beta`, recreate
`alpha`, publish package bytes, change Git or GitHub, publish a VSIX, advance
a plan, mutate an Issue, or change the publication-time fact that PUBLISH
left `latest=0.9.0`.
