# `v0.9.3` Release Candidate Acceptance

- Document status: Accepted 1.0
- Acceptance date: 2026-08-14
- Task: `RELEASE_093_CANDIDATE`
- Target version: `0.9.3`
- Artifact-producing source commit:
  `c95c83dc80ccee1d0fa2361e6e6892778e212656`
- Artifact-producing source tree:
  `c5e20414fc996e35e215063a8e84c95178afba94`

## 1. Clean source and external baseline

The committed source is clean and is a two-commit fast-forward from
`origin/main` at `b47a0a450f83070340fc52df88a0243f80ef795e`.
Direct reads found no local or remote `v0.9.3`, no GitHub Release `v0.9.3`,
and no npm `perttool@0.9.3`. npm reported `beta=0.9.2`, `latest=0.9.0`, and no
`alpha`. Issues #14, #16, and #17 remained open with `bug` and `priority:P0`.

The authenticated Git dry-run projected the exact fast-forward without a
write. The process-scoped GitHub and npm identities both resolved as `mako10k`.
An ambient-token `npm whoami` returned E401 before the required process-scoped
`NPM_TOKEN` route was selected; the accepted route then succeeded without
exposing the credential. No external state changed.

## 2. Immutable package identity

One tarball was generated from the accepted source commit into the fixed
candidate directory and made read-only:

| Field | Accepted value |
| --- | --- |
| File | `perttool-0.9.3.tgz` |
| Package files | 712 |
| Packed bytes | 800541 |
| Unpacked bytes | 4640660 |
| SHA-256 | `a86c552bb616a3dda13f12b32c9463d11e6ac4b2caa4c8d8d497855abcad9708` |
| SHA-1 | `5c2cf729a5ff7076ab9666c8709b7758256e0d29` |
| SHA-512 integrity | `sha512-8tusKEgD0Fj5N8KVwCdHBt0gqxVt8X/mWgI3kbBWyWDz/wYTdxh/DSshSAAmhXiFSqUUAiUJFgfZ/R2yL5gVfg==` |
| Retained mode | `0444` |

The archive excludes private adapters and repository-only source while
retaining the complete public `dist`, schemas, CHANGELOG, README, and license.

## 3. Installed emergency behavior

An isolated installation from the exact tarball reported `perttool 0.9.3`.
The unchanged Grammar 7 Issue #14 plan produced successful `show` and
`contract` hash results with exact source digest
`sha256:690f65bd5a70594a045b53bdae12178339e6fb6d160eb616ffa0a08150a8b79a`
and contract hash
`sha256:c37f3d5d5170fa41982ef71f6020c3916d4bdd1bc29fed05973e782d65f4ef6d`.

Read-only exports of image-platform commits `627514d` and `651ab2b` both
produced a changed advance candidate with acceptance and assurance guards
passed, preview history not applicable, and no diagnostics. The consumer
repositories and their plan files were not modified.

The candidate is accepted for the separately ordered publication task. npm
`latest`, Issue closure, plan advance, and unrelated work remain unchanged.
