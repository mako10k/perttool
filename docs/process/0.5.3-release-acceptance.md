# `v0.5.3` Release Acceptance

- Document status: Accepted 1.0
- Accepted on: 2026-07-30
- Release commit: `1dc7c055d541272fd4506e21f99ee6a0bdf33c17`
- Annotated tag: `v0.5.3`
- GitHub Release:
  <https://github.com/mako10k/perttool/releases/tag/v0.5.3>
- CI run:
  <https://github.com/mako10k/perttool/actions/runs/30527328527>
- npm package: `perttool@0.5.3`
- npm tags: `beta=0.5.3`, `latest=0.5.1`, no `alpha`

## 1. Decision

The suffix-free `0.5.3` beta release is accepted. No blocking source,
compatibility, artifact, channel, CI, or installed-behavior mismatch remains.
The release publishes the beta-only channel guard and the single-candidate,
scope-bound, human-readable loose owner-confirmation workflow without changing
Grammar 5, CLI Contract 6, command, option, result, schema, or package-root
identities.

## 2. Git and CI identity

The release commit and peeled local and remote annotated tag agree:

```text
1dc7c055d541272fd4506e21f99ee6a0bdf33c17
```

`origin/main` contains the later durable PUBLISH record. Repository checks run
30527328527 passed on the release commit for Node.js 22 and Node.js 24.

## 3. Artifact and channel identity

Fresh GitHub and npm downloads were compared independently with the retained
candidate:

| Property | Accepted value |
| --- | --- |
| Filename | `perttool-0.5.3.tgz` |
| Candidate modified | `2026-07-30 17:33:02 JST` |
| npm published | `2026-07-30 17:38:24.401 JST` |
| Files | `491` |
| Packed bytes | `520876` |
| Unpacked bytes | `3020390` |
| SHA-1 | `68b8f23dce872f53a6e2bc0f83746f3e3ea7a1fa` |
| SHA-256 | `a935b3e7698d81dcbae44a076500664bbb35516659b1a38531379f45cff5f7e4` |
| SHA-512 integrity | `sha512-gB0h9omITjEoDbVcjcsrBw+a0mWbGryS5ziGGg3cpviUOF2D4GXMShtDm4pVYXiLip6GD7D371VCRoJOPIkzpA==` |

All three tarballs are byte-identical. The GitHub Release is a non-draft
prerelease, npm `beta` resolves to `0.5.3`, npm `latest` remains `0.5.1`,
and the obsolete `alpha` dist-tag remains absent.

The human-readable source comparison from `v0.5.2` to `v0.5.3` is 52 files
changed, 1,854 insertions, and 200 deletions. The substantive change adds the
beta-channel guard and scope-bound owner-confirmation guidance; command,
option, result, schema, Grammar 5, CLI Contract 6, and package-root identities
remain unchanged. Cryptographic digests above are supplemental machine
identity, not the primary human explanation.

## 4. Installed behavior

`scripts/check-package.sh` passed independently for fresh GitHub and npm
downloads. Each check covered publication normalization and the complete
isolated Contract 6 file-first workflow.

A separate isolated npm installation then confirmed:

- `perttool --version` returns `perttool 0.5.3`;
- the detailed editing Guide contains modification time, before/after byte
  size, diff counts, and supplemental machine identity guidance;
- `Perttool.NextResult.v5` outline schema lookup returns the requested
  reference-based view; and
- the published CLI validates the Grammar 5 `release-0.5.3.pert` plan.

The first additional Guide smoke used an incorrect caller-side string filter
and exited nonzero after the package checks and `--version` had passed. Reading
the actual structured Guide showed the expected section; the corrected
field-aware query passed. This was a verification-command defect, not a
product failure.

## 5. Publication behavior

npm accepted one publish operation and returned `+ perttool@0.5.3`. Its
immediate internal readback saw one propagation-time `E404`; the publish was
not retried. Independent readback then confirmed the durable version,
integrity, file count, unpacked size, signature, and dist-tags.

No npm `latest` promotion, alpha restoration, issue closure, global
installation, or unrelated external mutation was performed.

## 6. Plan state

All five release-plan tasks and 15p are complete. Precedence and heuristic
resource makespans are zero, and complete NextResult v5 has no recommendation.
The completed declarations are intentionally retained in the current plan
until a separately confirmed, single-candidate `dag advance`; the release
authorization was not widened into that governed history-removal mutation.
