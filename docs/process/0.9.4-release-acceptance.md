# `v0.9.4` Durable Release Acceptance

- Document status: Accepted 1.0
- Acceptance date: 2026-08-14
- Target version: `0.9.4`
- Task: `RELEASE_094_ACCEPTANCE`
- Release plan: [`release-0.9.4.pert`](../../plans/release-0.9.4.pert)
- Release commit and peeled tag:
  `094132cd1ee1f451e39f56e94200bbc0d880aedd`
- Artifact SHA-256:
  `63a12ddfdd8a435674262b8a01e122481a8d02cde5cedc67b6e0b77e1d6fe53b`
- Completed pre-advance plan source digest:
  `sha256:367e2e12c1e838253960af330bbee559e18923e4f15d18e0e9fc8ac17895a3b5`

## 1. Durable public identity

Independent reads confirmed remote `main`, the annotated tag object, the
peeled `v0.9.4` tag, successful Node.js 22 and Node.js 24 CI run
`31786430400`, GitHub prerelease `370462825`, npm version metadata, registry
integrity, file count, unpacked size, publication time, and dist-tags. Fresh
downloads of the GitHub and npm tarballs both have the accepted SHA-256 and
are byte-identical to the retained candidate.

npm reports `beta=0.9.4`, `latest=0.9.0`, and no `alpha`. The publisher's
bounded post-upload checks returned propagation E404 after npm had accepted
the single publication. Publication was not retried; the later independent
read established durable registry acceptance at
`2026-08-14T09:07:49.204Z`.

Fresh-cache isolated installations resolved exact `perttool@0.9.4` and
`perttool@beta` to `perttool 0.9.4`, while the exact rollback pin resolved to
`perttool 0.9.3`. The exact public package exposes 53 commands and 23 root
schemas. The exact registry package was also installed globally and reports
`perttool 0.9.4` with the same command and schema counts.

## 2. Issue #19 behavior and compatibility

The unchanged real `image-platform.pert` source contains nine milestone
criterion sets. The `0.9.4` preview preserves all six sets owned by retained
milestones and removes only the three sets owned by removed milestones. The
same candidate passes `document check --warnings-as-errors` with no
diagnostic. The `0.9.3` rollback preview retains only three sets and therefore
reproduces the corrected defect.

The operation still fails closed on independent `PTMAC-108` acceptance
authority; the correction does not bypass acceptance, assurance, history,
governance, persistence, source-race, or `--force-history-loss` boundaries.
The source SHA-256 remained
`45c9b892984bf5b3125f0a6eb08ed47f09db6f1fe62b9beb4560d784d324067c`
before and after every replay.

Grammar 7, CLI Contract 8, all 53 commands, 23 root schemas, 129 root and Node
exports, 45 Core exports, result identities, authority, and force semantics
remain unchanged. The pinned jscpd and Lizard checks are repository and CI
development gates and do not change the public package contract.

## 3. Post-acceptance operations

Issue [#19](https://github.com/mako10k/perttool/issues/19) was closed once as
completed after durable acceptance with one release-evidence comment. It
retains `bug` and no longer has `priority:P0`.

All six release tasks and 15 points completed before canonical advance. The
committed Grammar 7 evidence snapshot accepted all six post-migration
milestones, and the migration provenance grandfathered only the explicit
planning-start milestone. History- and acceptance-guarded advance retained
only accepted reached `RELEASE_094_ACCEPTED`, its required criterion, and its
verify receipt. Residual source digest
`sha256:2c7a228b1e1f85b53805fd3b699b55c865875faab720aacff5d63454a7723136`
has no diagnostic, task, ready item, recommendation, or startable task.

The exact post-acceptance trace is recorded in
[`0.9.4-post-acceptance-operations.md`](0.9.4-post-acceptance-operations.md).
Work-in-progress branch integration is separately read back. npm `latest`,
public VSIX publication, consumer writes, and unrelated plan advances remain
unchanged and separate.
