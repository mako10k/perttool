# `v0.8.0` Accepted Input Readiness

- Document status: Accepted 1.0
- Verification date: 2026-08-07
- Task: `RELEASE_080_INPUT_READINESS`
- Target package: `perttool@0.8.0`
- Publication state: unpublished
- Release procedure: [`0.8.0-release.md`](0.8.0-release.md)
- Release plan: [`release-0.8.0.pert`](../../plans/release-0.8.0.pert)
- Completed-plan source digest: `sha256:4b3f887a2bf5ab293ade0931cc57a9b83a972c718e201e8629f44ab386628cf5`

## 1. Required accepted inputs

The release consumes three completed inputs without duplicating their
implementation state:

| Input | Current accepted evidence |
| --- | --- |
| `ADAPTER-001` | Retained complete pre-advance plan digest `sha256:8aaeedea6ceb2e300947392cc551e9d6459ee5c66f5a10f8486a336696a31baa` and [integration acceptance](adapter-integration-acceptance.md) |
| `HIST-DAG-001` | Retained complete pre-advance plan digest `sha256:3a1b78e7e7012ebd0fba568cf10f0a0ca23d20fc33fd834f719b1681a64ea3ef` and [historical DAG acceptance](historical-dag-acceptance.md) |
| `DECL-ID-001` | [Declaration identity acceptance](declaration-identity-acceptance.md) and focused task/seal identity regression |

Complete NextResult v6 has no ready, recommended, runnable, or startable task
for either input plan. The historical plan reports complete assurance with all
eight task outcomes conformant and no replan or required action. Both plans
remain intentionally retained before advance.

## 2. Public-package boundary

Direct source and runtime inspection confirms:

- Grammar 6 and CLI Contract 7 remain active;
- the registry has 45 commands and includes only one `dag history` descriptor;
- the root schema catalog has 21 identities and includes only one
  `Perttool.HistoricalGraphResult.v1` root;
- root and Node each expose the same 122 runtime names with reference identity;
- Core exposes exactly 45 platform-neutral runtime names;
- package exports are `.`, `./core`, `./node`, and `./schemas/*`; and
- the public package inventory is limited to `dist`, `schemas`, and
  `CHANGELOG.md` and therefore excludes every private adapter workspace.

The published `0.7.1` package remains the 44-command, 20-schema, 121-root-
export compatibility baseline. Package, lockfile, adapter peer, and CLI
identities remain `0.7.1` during this readiness task.

## 3. Semantic and no-write evidence

The accepted adapter evidence proves exact Core, CLI, LSP, VSIX, and MCP
semantic ownership, inward dependency directions, protocol bindings,
installed private-package behavior, and no document or external write. The
accepted historical evidence proves bounded immutable first-parent Git input,
snapshot, proved-lineage, timeline, gaps, canonical-advance equality,
single-checkpoint analysis, installed CLI behavior, editor source binding, and
no write. The declaration-identity evidence proves task insertion, mutation,
lifecycle, and definition navigation without formatter reorder.

Focused release, adapter, historical, and declaration-identity tests directly
recheck these current inputs. English, documentation, self-use, and complete
repository gates remain mandatory again during source preparation and
candidate acceptance.

## 4. Result and remaining authority

All required inputs are complete, compatible, and available to
`RELEASE_080_PREPARATION`. Complete NextResult v6 recommends and makes
startable only that task. This readiness task changed no package version,
Git object or ref, tag, remote, Release, registry, dist-tag, VSIX publication,
plan advance, or Issue. It supplies no PUBLISH authority and reuses no owner
assertion.
