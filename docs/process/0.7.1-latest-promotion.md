# `perttool@0.7.1` npm `latest` Promotion

- Document status: Accepted 1.0
- Promotion date: 2026-08-05
- Selected version: `perttool@0.7.1`
- Mutation: npm `latest` dist-tag only
- Final tags: `beta=latest=0.7.1`, no `alpha`

## 1. Authority and precondition

After successful beta publication and public-package verification, the user
separately selected `perttool@0.7.1` and explicitly authorized its immediate
promotion to npm `latest`. Direct registry readback immediately before the
mutation reported `beta=0.7.1`, `latest=0.7.0`, no `alpha`, and the accepted
0.7.1 integrity value. The process-scoped npm identity was `mako10k`.

## 2. Exact mutation and readback

The following logical mutation was executed exactly once through `secdat`:

```sh
npm dist-tag add perttool@0.7.1 latest
```

npm returned `+latest: perttool@0.7.1`. Direct registry metadata and a
fresh-cache npm read then both reported `beta=latest=0.7.1`, no `alpha`, with
registry modification time `2026-08-05T02:40:49.566Z`.

A fresh unqualified isolated installation resolved to `perttool 0.7.1`, 44
commands, 20 root schemas, and the combined release, temporal, and
plan-assurance Guide authority. The promotion did not publish a package,
change Git or GitHub, advance a plan, or mutate an Issue.
