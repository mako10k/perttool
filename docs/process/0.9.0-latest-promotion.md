# `perttool@0.9.0` npm `latest` Promotion

- Document status: Accepted 1.0
- Promotion date: 2026-08-13
- Selected version: `perttool@0.9.0`
- Mutation: npm `latest` dist-tag only
- Final tags: `beta=latest=0.9.0`, no `alpha`
- Registry modification time: `2026-08-13T06:49:54.643Z`

## 1. Authority and precondition

After durable `0.9.0` beta acceptance, the user separately selected
`perttool@0.9.0` and authorized its promotion to npm `latest`. Direct
registry and fresh npm reads before the mutation reported `beta=0.9.0`,
`latest=0.8.1`, no `alpha`, and the accepted `0.9.0` integrity value
`sha512-0HbOZu+OZM7rprwlzymxaInEaZSl4hXRUVnY/RRN6Wywp1+dg3xbK3mWi9DxqNWKVnrjfGJyXKRxYHzIFAPWyQ==`.
The process-scoped npm identity was `mako10k`.

## 2. Exact mutation and readback

The following logical mutation was executed exactly once through `secdat`:

```sh
npm dist-tag add perttool@0.9.0 latest
```

npm returned `+latest: perttool@0.9.0`. A direct no-cache registry read and a
separate fresh-cache npm read both reported `beta=latest=0.9.0` with no
`alpha`. The immutable `0.9.0` package retained SHA-1
`52b4f4c0b6ada3e3e27f6eac061e6b67ee0f42e3` and the accepted integrity value.
No package publication or second dist-tag mutation occurred.

## 3. Consumer verification

A fresh-cache unqualified isolated installation under Node.js `22.22.3`
resolved to `perttool 0.9.0`. It exposed CLI Contract 8, 53 commands, 23 root
schemas, 129 key- and reference-identical root and Node runtime exports, and
45 Core exports.

The package bytes and public inventory remain the accepted 713-file artifact
with SHA-256
`88e51bfee536a62a980c83f2af77af6245a5ce578ec9aac4153b2e8f237345e7`.

## 4. Boundary

The promotion changed only npm `latest`. It did not alter `beta`, recreate
`alpha`, publish package bytes, change a Git or GitHub release ref, publish a
VSIX, advance a plan, or mutate an Issue. Publication-time records continue
to preserve the fact that PUBLISH moved only `beta` while `latest` remained
`0.8.1`.
